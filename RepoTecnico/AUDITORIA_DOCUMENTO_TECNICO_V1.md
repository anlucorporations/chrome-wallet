# 🔍 Auditoría del Documento Técnico — TrueKeate Wallet

> **Fase:** 2 — Auditoría · **Versión:** 1.0
> **Objeto auditado:** `RepoTecnico/documento_tecnico.md` v1.0 (1610 líneas, 65 módulos M1..M65, 20 ADR, 13 diagramas Mermaid)
> **Contrastado con:** `requerimientos.md` v1.5 · `casos_uso/casos_uso.md` v1.1 · `casos_uso/diagramas.md` · `diccionario_datos.md` v1.4 · `entornos_globales.md` v1.6 · `identidad_visual.md` v1.2 · `estado_proyecto.md` v1.5 · `INFORME_OPTIMIZACION_V1.md` · `casos_uso/AUDITORIA_CASOS_USO_V1.md`
> **Método:** 3 revisores independientes en paralelo con lentes complementarias (coherencia interna y trazabilidad; completitud y vacíos técnicos; seguridad, riesgos y verificabilidad), consolidación con deduplicación.

---

## 1. Veredicto

| Lente | Veredicto | Métricas clave |
|---|---|---|
| **Coherencia y trazabilidad** | Sustancialmente coherente: **40/40 RF Must asignados a módulo** y matriz RF→CU idéntica a la de los casos de uso (0 discrepancias). No está cerrado: 3 diagramas Mermaid no parsean y hay divergencias con el corpus. | 346 identificadores cruzados verificados · 0 inexistentes · **6 divergencias** · 10/13 diagramas válidos |
| **Completitud y vacíos técnicos** | **No está completo para empezar a codificar**: faltan la especificación del build MV3 (bloqueante del hito H1), el arnés E2E y 6 de los 10 pendientes declarados. | 1/92 requisitos huérfanos (RNF-01) · 0 secciones vacías · 6 vacíos materiales · 15 riesgos declarados frente a 4 familias ausentes |
| **Seguridad y riesgos** | **Apto condicionalmente** para el alcance declarado (100 % local, sin fondos reales), con 25 defensas especificadas y **0 defectos de las auditorías previas reintroducidos**; 3 hallazgos altos afectan garantías centrales. | 25 defensas · 9/15 riesgos con verificación · 8 riesgos ausentes · 3/14 categorías de defensa completas |

**Conclusión:** la arquitectura es sólida y bien trazada, pero el documento **no puede declararse cerrado** hasta especificar el build/empaquetado MV3, el arnés de pruebas y los tres controles de seguridad altos (origen en iframes, fuente de la decodificación del calldata e higiene del revelado de la semilla).

---

## 2. Hallazgos consolidados

Severidades: 🔴 CRITICA · 🟠 ALTA · 🟡 MEDIA · 🔵 BAJA. Lentes: **C** = coherencia/trazabilidad · **V** = completitud/vacíos · **S** = seguridad/riesgos.

### 🔴 Bloqueante

| ID | Sev. | Área | Hallazgo | Evidencia | Recomendación | Origen |
|---|---|---|---|---|---|---|
| **ADT-01** | 🔴 | Build y empaquetado MV3 | El pipeline de build está **nombrado pero no especificado**: no hay bloque `scripts` de `package.json`, ni configuración de Vite para las **6 entradas** (popup, connect, notification, background, content, inject), ni **formato de salida** (ESM obligatorio en el Service Worker `type: module` frente a IIFE obligatorio en `content-script.js`/`inject.js`, que no admiten `import`), ni mecanismo de generación del manifest (¿plugin tipo CRXJS o script propio?), ni mapeo `src/*.ts → dist/*.js`. Las **3 páginas HTML no existen en el árbol `src/`** aunque el build las necesita como entradas. | `documento_tecnico.md:256`, `:305-317`, `:339`, `:1425`, `:1445-1466` · `entornos_globales.md:91-96` · ADR-01 cita `src/components`, que no existe (`:539`) | Añadir **§7.5 «Build y empaquetado»**: árbol de entradas y nombres de salida, `build.rollupOptions.input`, `output.format` por entrada (`es` para el SW, `iife` para content/inject), mecanismo de generación del manifest, plantilla literal de los scripts npm (`build`, `test`, `test:e2e`, `lint:prohibited`, `coverage`) y las 3 páginas HTML en §2.4. | V H-01 |

### 🟠 Altas

| ID | Sev. | Área | Hallazgo | Evidencia | Recomendación | Origen |
|---|---|---|---|---|---|---|
| **ADT-02** | 🟠 | Diagramas | **3 de los 13 diagramas Mermaid no parsean** (10/13 con mermaid 11.17, 8/13 con 10.9). Causa única: el carácter `;` dentro del texto de mensajes de `sequenceDiagram` y `:` dentro de etiquetas de `stateDiagram-v2`. El corpus de casos de uso sí parsea (12/12). | `documento_tecnico.md:583`, `:594`, `:754`, `:776`, `:824`, `:242`, `:630` | Sustituir `;` por `,`/`—`, no entrecomillar etiquetas de estado y añadir una comprobación de CI que ejecute `mermaid.parse()` sobre todos los bloques. | C COH-01 |
| **ADT-03** | 🟠 | Fuente de verdad de errores | La tabla de errores de §5.1 (19 filas, varios mensajes por código) se atribuye a **`requerimientos.md` §2.1**, que solo tiene **8 códigos con un mensaje único**; la tabla «un mensaje por causa» está en **`diccionario_datos.md` §4.3**. El pendiente P-3.2 se construye sobre esa premisa falsa. Además las referencias internas «§2.1» son ambiguas con la sección del propio documento. | `documento_tecnico.md:1180`, `:1204`, `:1521`, `:265`, `:903` · `requerimientos.md:214-223` · `diccionario_datos.md:554-574` | Citar **`diccionario_datos.md` §4.3** como fuente y prefijar siempre el documento en las referencias cruzadas; cerrar P-3.2 como corrección de cita. | C COH-02, V H-05 |
| **ADT-04** | 🟠 | Cobertura declarada | Se afirma «**25/25 RNF · 13/13 RT · RE-01..RE-04**» cubiertos, pero §6.1 solo enlaza **22 RNF**, **11 RT** y RE-01/RE-04. **RNF-01** —el requisito de trazabilidad del 100 %— **no aparece ni una vez** en el documento. | `documento_tecnico.md:54` vs `:1249-1318`, `:1531`; RNF-17/RT-01/RT-07 solo en prosa (`:1430`, `:1399`, `:1547`) | Añadir las filas de RNF-01/17/24, RT-01/07 y RE-02/RE-03 a §6.1, o rebajar la afirmación al conteo real. | C COH-03, V H-04 |
| **ADT-05** | 🟠 | Arnés de pruebas E2E | La suite E2E **no es ejecutable** tal como está descrita: `--load-extension=dist` es un flag de Chrome, no una API de Playwright (hace falta `chromium.launchPersistentContext` con `--disable-extensions-except` + `--load-extension` y ruta absoluta, y descubrir el **ID de la extensión**). No se especifica el aislamiento de estado entre pruebas, ni cómo se **duerme el Service Worker** (RNF-08 lo exige en E2E), ni cómo se prueban los plazos de 120 s/60 s (los fake timers de Vitest no tocan `chrome.alarms`), ni el stub de `chrome.*` para Vitest, ni el patrón de los artefactos de evidencia. | `documento_tecnico.md:1431`, `:1482-1488`, `:1528` | Especificar en §7.4: fixture de `launchPersistentContext` con perfil nuevo por test, helper de descubrimiento del ID desde `context.serviceWorkers()`, suspensión del SW vía CDP, `SIGN_TIMEOUT_MS` inyectable, stub de `chrome` para Vitest y convención `RepoTecnico/evidencia/<fase>/<test>-<fecha>.{json,png,log}`. | V H-02 |
| **ADT-06** | 🟠 | RF-50 sin flujo | **RF-50 (Must) no tiene flujo ni contrato temporal**: §3 no describe la secuencia de confirmación/revelado/ocultado y §3.3 omite el temporizador y el ocultado al perder el foco que sí exige el caso de uso. §8.2 declara «no resuelta» la discrepancia 30 s vs 60 s cuando **DEC-28 ya fijó 30 s**. | `documento_tecnico.md:734`, `:1365`, `:1520` · `estado_proyecto.md:88` · `casos_uso.md:384-390` | Añadir **§3.8 «Revelado y exportación (RF-50)»** con flujo, constante `REVEAL_HIDE_MS = 30000` en M57, ocultado por temporizador **y** por pérdida de foco, y corregir CU-07 a 30 s. | V H-03 |
| **ADT-07** | 🟠 | Origen en iframes | El origen que autentica la sesión **no está resuelto** cuando la petición nace en un iframe: la regla dice «`sender.origin` **/** `sender.tab.url`» sin precedencia. En un iframe cross-origin, `sender.origin` es el origen del frame y `sender.tab.url` el del **top**, de modo que la resolución equivocada haría que un iframe hostil **herede la sesión del sitio anfitrión** y que la ventana anti-phishing muestre un origen distinto del solicitante real. `frameId` se persiste pero nunca se usa como guarda. | `documento_tecnico.md:360`, `:411`, `:921`, `:939` · `diccionario_datos.md:490`, `:502` · `casos_uso.md:1102` | Fijar la regla: el origen se deriva **solo** de `sender.origin`; si `sender.frameId !== 0` queda **prohibido** caer a `sender.tab.url`; persistir `frameId` y responder solo a ese frame. Test negativo con iframe cross-origin. | S S-01 |
| **ADT-08** | 🟠 | Anti-firma-ciega | El control obligatorio de DEC-23 («decodificar el calldata») **no tiene fuente declarada**: `functionName`/`decodedArgs` se definen «decodificados con el ABI disponible», pero no hay módulo, tabla de selectores ni almacén de ABIs. Además `verifyingContractMismatch` se define como «distinto **del declarado**», y lo «declarado» sale del **mismo payload** de la dApp: el aviso es circular e incomputable. | `documento_tecnico.md:431-434`, `:446-453`, `:788-791`, `:300-336`, `:954-966` · `casos_uso.md:703` | Declarar una **tabla local cerrada** de selectores (transfer, approve, setApprovalForAll, permit…) y que fuera de ella `functionName = null` con aviso bloqueante (prohibido consultar servicios externos: RT-03). Redefinir `verifyingContractMismatch` como «dirección cero / contrato no desplegado». | S S-02 |
| **ADT-09** | 🟠 | Higiene del revelado | La semilla sale del Service Worker hacia el popup (necesario para RF-50) y **el documento omite todo el delta de higiene**: portapapeles, pérdida de foco y descarte de la memoria de la UI. No existe **ninguna** regla de limpieza del portapapeles en el corpus. RNF-09 afirma en absoluto que el mnemonic «**nunca** sale del Service Worker», lo que contradice el propio diseño. | `documento_tecnico.md:272`, `:734`, `:1365` · `requerimientos.md:194` · `casos_uso.md:380-410` | Ver **P-20**. Reescribir RNF-09 como «nunca hacia la página ni por `postMessage`». | S S-03 |

### 🟡 Medias

| ID | Sev. | Área | Hallazgo | Evidencia | Recomendación | Origen |
|---|---|---|---|---|---|---|
| **ADT-10** | 🟡 | Bidireccionalidad | Las matrices §6.1 y §6.2 **no son bidireccionales**: en 12 de los 40 RF Must, §6.2 asigna módulos que §6.1 no asocia a ese RF; en 37 RF ocurre lo contrario (amparable por «principales», pero sin declararlo). | `documento_tecnico.md:1249` vs `:1324`; RF-03 en `:1328` vs `:1252-1258` | Declarar la regla «solo módulos principales» y regenerar ambas desde una única relación, o fusionarlas en una sola matriz RF↔módulo↔CU. | C COH-04 |
| **ADT-11** | 🟡 | Duplicidad de fuentes | El documento **reproduce literalmente** el JSON del manifest, la tabla de errores, los defaults de `truekeate_settings`/volumetría y el catálogo RPC + protocolo, que ya viven en el corpus (y la tabla de errores **ya divergió**). | `documento_tecnico.md:1445-1466`, `:1182-1202`, `:970-987`, `:1124-1140`, `:367-385`, `:1159-1178` | Sustituir las copias por **referencia normativa** y dejar solo el delta arquitectónico; donde se mantenga la copia, añadir comprobación mecánica de igualdad. | C COH-05 |
| **ADT-12** | 🟡 | Redacción de logs | Divergencia: el técnico dice «solo `to`, `value`, `dataLength` y el **selector**» (4 bytes); el diccionario exige «los **primeros 10 bytes** de `data`». | `documento_tecnico.md:799`, `:869`, `:900` vs `diccionario_datos.md:310` | Unificar en **10 bytes** (literal del diccionario) y corregir las tres apariciones. | C COH-06 |
| **ADT-13** | 🟡 | Diagrama ER | El diagrama declara `WALLET \|\|--\|\| MNEMONIC` («exactamente uno»), pero el texto y el diccionario admiten que `truekeate_mnemonic` **no exista** si la cartera se creó solo con cuentas importadas. | `documento_tecnico.md:993` vs `:956` · `diccionario_datos.md:39` | Cambiar la cardinalidad a `WALLET \|\|--o\| MNEMONIC` (0..1). | C COH-07 |
| **ADT-14** | 🟡 | Cuota de almacenamiento | La **cuota de `chrome.storage.local`** no se declara (es 10 MB desde Chrome 114, la versión mínima que el proyecto exige) y **falta el modo de fallo**: no hay `code`/mensaje para el rechazo por cuota ni evento para «escritura de log descartada», de modo que el fallo quedaría silencioso. | `documento_tecnico.md:1139`, `:1510`, `:1522`, `:1183-1202`, `:896` | Declarar la cuota objetivo (≥ 5 MB garantizado, 10 MB en el mínimo exigido), decidir que **no** se usa `unlimitedStorage`, y fijar el fallo observable: 1 reintento, evento con `code: -32603` y aviso en el panel. | V H-06 |
| **ADT-15** | 🟡 | Riesgos MV3 ausentes | §8.1 cubre dormición del SW, cuota, Anvil caído, CORS y firma ciega, pero **omite 4 familias**: (a) **vida del puerto** —se afirma que «el puerto mantiene vivo el SW», cuando un SW sin actividad se termina a los **30 s**—; (b) alarmas retardadas o perdidas ante cierre/reinicio del navegador; (c) **ventana huérfana o colisión de dos `notification.html`**; (d) **bundle de ethers + CSP + tamaño del paquete**. | `documento_tecnico.md:199`, `:225-226`, `:618`, `:1494-1512` | Añadir R16–R19 con probabilidad, impacto y mitigación, y corregir la nota de §2.3 sobre el *keep-alive* del puerto (el puerto **no** garantiza la vida del SW: hay que rearmar con `chrome.alarms` y aceptar la suspensión). | V H-07 |
| **ADT-16** | 🟡 | Contratos internos | Los métodos internos `wallet_deriveAccounts`, `wallet_generateMnemonic`, `wallet_importPrivateKey`, `wallet_getNetworks`, `wallet_getLogs` (y la resolución de RF-50) se enumeran **solo por nombre**, sin firma, parámetros, retorno ni errores; el tipo `TruekeateRpcMessage.method: string` no cierra la unión, así que el «catálogo cerrado» no es verificable por tipos. Faltan las interfaces TS de las entidades de storage. | `documento_tecnico.md:1178`, `:378`, `:1525` · `diccionario_datos.md:510-537` | Cerrar en §2.5.2 las interfaces `WalletRequest/Response` por método y tipar `TruekeateSettings`/`ImportedAccount`/`StoredNetwork`; unión cerrada `InternalMethod` en M56. | V H-08 |
| **ADT-17** | 🟡 | Contrato Forge | El contrato verificador EIP-712 está solo esbozado: se da la firma `verify(address,bytes32,bytes) → bool` y «3-4 tests», pero **no se especifica cómo se alimenta** el `digest`/firma, ni el `EIP712Domain`/typehash, ni la visibilidad, ni la versión de `solc`, ni el nombre del contrato de test. | `documento_tecnico.md:1230-1239`, `:1529`, `:1433` · `requerimientos.md:304` | Especificar en §5.4 la firma y visibilidad exactas, el cálculo del `digest` con `TypedDataEncoder.hash` y un **fixture JSON versionado** (firma real + caso de byte alterado), el `contract EIP712VerifierTest` y `solc` fijado en `foundry.toml`. | V H-09 |
| **ADT-18** | 🟡 | Documentación de la rúbrica | Los **10 puntos de «Documentación»** no pueden derivarse del técnico: no hay estructura ni criterio para `README.md`/`INSTRUCCIONES.md`, y §6.1 asigna **CU-36** a módulos que no producen ninguno de esos artefactos. Tampoco hay responsable de `LICENSE`, `NOTICE` ni los `LICENSE-*.txt` de las fuentes. | `documento_tecnico.md:1545`, `:1251`, `:1317-1318`, `:1606` · `casos_uso.md:1894-1950` · `requerimientos.md:353-369` | Añadir a §2.4/§9 los artefactos `README.md`, `INSTRUCCIONES.md`, `LICENSE`, `NOTICE`, `public/fonts/LICENSE-*.txt` con responsable, y remitir CU-36 a ellos más la regla de JSDoc por módulo. | V H-10 |
| **ADT-19** | 🟡 | Identidad del paquete | `PROVIDER_UUID` es «constante fija» pero sin valor, y la `key` del manifest no existe, de modo que el **ID de la extensión cambia en cada equipo** y la allowlist CORS de Anvil (RE-04) y la suite E2E no son reproducibles. El fragmento del manifest omite `manifest_version`, `name`, `version`, `icons`, `action.default_popup` y `minimum_chrome_version`. | `documento_tecnico.md:517-521`, `:1523-1524`, `:1445-1466`, `:256` | **Decidido:** congelar un UUID v4 literal en `src/inject/provider.ts` y añadir una **`key` fija** al manifest para estabilizar el ID; completar M1 con los campos obligatorios. | V H-11 |
| **ADT-20** | 🟡 | Superficie de inyección | El manifest declara `<all_urls>`, `all_frames: true` y `web_accessible_resources` con `<all_urls>`, pero **no hay ninguna fila que lo justifique**; no se usan `exclude_matches` ni `use_dynamic_url`; y RNF-10 afirma que `inject.js` «solo se expone a páginas autorizadas», lo que es falso con `<all_urls>`. | `documento_tecnico.md:1457-1463`, `:1468-1476` · `requerimientos.md:195` · `entornos_globales.md:203-209` | Añadir la fila de justificación, `exclude_matches` para `chrome-extension://*` y orígenes de otras wallets, `use_dynamic_url: true`, corregir RNF-10 y verificar con `manifest.spec.ts` que una página arbitraria no obtiene `inject.js`. | S S-04 |
| **ADT-21** | 🟡 | Tamaño de payload | La regla exige guardar **solo hash y longitud** en los logs, pero la cola persistida guarda el payload **íntegro** en las previews (`TypedDataPreview.message`, `PersonalSignPreview.text`) sin redacción ni **cota de tamaño**: un payload de megabytes se persiste y se renderiza. | `documento_tecnico.md:408`, `:415-416`, `:450`, `:457`, `:460`, `:900`, `:1139-1140` | **Decidido:** tamaño máximo de payload **64 KiB** con `-32602` al excederlo, y preview redactada en reposo (hash + resumen) para los tipos de dato largos. | S S-05 |
| **ADT-22** | 🟡 | Anti-phishing | El favicon de la ventana de decisión **no tiene fuente, permiso ni saneado declarados** (MV3 exige permiso para `chrome://favicon`), y la invariante «una ventana **por origen**» con hasta **8 pendientes globales** permite coexistir 8 ventanas de confirmación, habilitando *approval fatigue* y confusión de ventanas. | `documento_tecnico.md:229`, `:618`, `:943`, `:1212`, `:472` · `diccionario_datos.md:221` | Ver **P-21**. Declarar además la fuente del favicon (servicio del navegador), su permiso y su saneado (sin `data:` ni URLs remotas). | S S-06 |
| **ADT-23** | 🟡 | Estado volátil | La garantía «máximo 1 transacción en vuelo por cuenta» y el limitador de tasa dependen de estado **en memoria** (`fifoByAccount`, `rmwLock`) que la propia §2.3 declara volátil, mientras el axioma A6 dice que el SW puede dormirse en cualquier momento. La reconciliación no reconstruye el vuelo por cuenta ni la ventana de tasa. | `documento_tecnico.md:64`, `:227`, `:229`, `:620`, `:1132-1133`, `:226`, `:954-966` | Persistir la marca de «tx en vuelo» por cuenta y la ventana de tasa por origen, o declarar explícitamente qué garantía cae tras una suspensión; añadir test que suspenda el SW a mitad de una aprobación y verifique que no hay doble difusión. | S S-07 |
| **ADT-24** | 🟡 | Abuso desde la dApp | El control de tasa y cardinalidad se define **solo** para solicitudes aprobables. Los métodos de lectura no tienen límite por origen: una dApp hostil puede saturar `eth_getBalance`/`eth_estimateGas`/`eth_blockNumber` y cargar el nodo y el SW sin abrir ninguna ventana. | `documento_tecnico.md:64`, `:229`, `:1130-1132`, `:1161-1169` | Extender el *token bucket* por origen a **todo** el catálogo RPC, o declarar el residuo y su medición; test con 100 llamadas de lectura seguidas. | S S-08 |
| **ADT-25** | 🟡 | Alcance de la aprobación de red | `wallet_addEthereumChain` **marca la red nueva como activa** en el mismo acto, pero la confirmación solo muestra «los datos de la red»: la garantía de ADR-16 (cambiar la red activa exige aprobación específica) es eludible envolviendo el cambio dentro de un alta. | `documento_tecnico.md:554`, `:830-840`, `:849-850`, `:1175`, `:1212` | Ver **P-22**. | S S-09 |
| **ADT-26** | 🟡 | Modelo de amenaza | Faltan cuatro filas: (a) **captura o grabación de pantalla** con el material revelado; (b) **extensión de terceros** con `<all_urls>` que puede leer el DOM de la dApp y suplantar el provider; (c) migración o sincronización accidental a **`chrome.storage.sync`**; (d) **suplantación de `window.truekeate`** por la propia página (asignación simple, sin definición no configurable). | `documento_tecnico.md:524-529`, `:1150-1155`; 0 coincidencias de `storage.sync`/`telemetr` en `RepoTecnico/` | Añadir las cuatro filas con su control mínimo: definición del provider con `Object.defineProperty` no configurable, prohibición explícita de `storage.sync` verificada por test, aviso in-product de que el revelado es vulnerable a capturas, y declaración de terceros como fuera de control. | S S-10 |
| **ADT-27** | 🟡 | Verificabilidad de riesgos | La tabla §8.1 no tiene columna de **verificación** y 6 de los 15 riesgos carecen de criterio o prueba observable (R3, R9, R10, R12, R13, R15). | `documento_tecnico.md:1500`, `:1506-1512`, `:1522`, `:1488` | Añadir la columna «Criterio de verificación / evidencia» y cerrar R13 con una medición incluida en §7.4. | S S-11 |

### 🔵 Bajas

| ID | Sev. | Área | Hallazgo | Evidencia | Recomendación | Origen |
|---|---|---|---|---|---|---|
| **ADT-28** | 🔵 | Constantes | `entornos_globales.md` sitúa las constantes del plazo en `src/background/approvals.ts`; el árbol propuesto las reparte en `approvals/timeout.ts` y `shared/constants.ts`, sin declarar la divergencia. | `entornos_globales.md:161-162` vs `documento_tecnico.md:276`, `:327`, `:541` | Alinear la ruta en `entornos_globales.md` o anotar que el árbol propuesto la sustituye. | C COH-08 |
| **ADT-29** | 🔵 | Protocolo | El enum de mensajes del técnico tiene **8 tipos**; la tabla de nomenclatura de `entornos_globales.md` §10 enumera **6**. | `documento_tecnico.md:363-381` vs `entornos_globales.md:366` | Actualizar §10 con los 8 tipos. | C COH-09 |
| **ADT-30** | 🔵 | Permisos del MVP | El permiso `notifications` se declara en el manifest del MVP y su justificación es RF-39, que el documento sitúa en el **ciclo posterior**. | `documento_tecnico.md:50`, `:1380`, `:1447`, `:1472` | Moverlo a `optional_permissions` y reincorporarlo con RF-39; verificarlo en `manifest.spec.ts`. | S S-12 |
| **ADT-31** | 🔵 | Contrato RPC | `wallet_revokePermissions` figura como aprobación «desde el popup», lo que contradice §3.2 del mismo documento y el diccionario, que admiten también el camino desde la dApp. | `documento_tecnico.md:1176` vs `:680` · `diccionario_datos.md:533` | Completar la celda con ambos caminos. | C COH-10 |
| **ADT-32** | 🔵 | Identidad visual | El botón fantasma se define con texto `--tk-navy-800`, mientras `identidad_visual.md` §5 lo describe con `--tk-gold-600`. | `documento_tecnico.md:1215` vs `identidad_visual.md:195`, `:105` | Fijar `--tk-navy-800` (variante accesible) en la identidad visual. | C COH-11 |
| **ADT-33** | 🔵 | Citas y pendientes evitables | Citas imprecisas (ADR-08 justifica mínimos privilegios con DEC-36, que solo regula el permiso de host en runtime; un ID de hallazgo `H-33` aparece en la columna de RNF/RT/RE) y pendientes que en realidad ya están resueltos (P-3.8 recarga de la ventana y P-3.9 patrón de nombres de test). | `documento_tecnico.md:546`, `:1318`, `:1527-1528`, `:1541-1545` | Reasignar las citas y promover P-3.8 a invariante de §2.3 y P-3.9 a convención de §7.4. | C COH-12, V H-12 |

---

## 3. Decisiones adoptadas al consolidar (no requieren consulta)

| # | Decisión | Cierra |
|---|---|---|
| **D-H** | Corregir la sintaxis de los 3 diagramas y añadir una comprobación de CI que valide todos los bloques Mermaid. | ADT-02 |
| **D-I** | La fuente de verdad de la tabla de errores es **`diccionario_datos.md` §4.3**; las referencias cruzadas se prefijan siempre con el documento. | ADT-03 |
| **D-J** | El origen de una petición se deriva **solo** de `sender.origin`; con `frameId !== 0` se prohíbe el respaldo a `sender.tab.url`. | ADT-07 |
| **D-K** | Decodificación del calldata con una **tabla local cerrada** de selectores; fuera de ella, `functionName = null` y aviso bloqueante. Prohibido consultar servicios externos de firmas. `verifyingContractMismatch` pasa a significar «dirección cero o contrato no desplegado». | ADT-08 |
| **D-L** | Tamaño máximo de payload **64 KiB** (por encima, `-32602`); previews largas redactadas en reposo. | ADT-21 |
| **D-M** | Cuota objetivo de `chrome.storage.local`: **10 MB** (mínimo exigido Chrome 114); **sin** `unlimitedStorage`; el rechazo por cuota se hace observable con 1 reintento, `code: -32603` y aviso en el panel. | ADT-14 |
| **D-N** | UUID del provider **literal y congelado** y **`key` fija** en el manifest para estabilizar el ID de la extensión (necesario para la allowlist CORS y para la reproducibilidad de los E2E). | ADT-19 |
| **D-O** | `exclude_matches` para `chrome-extension://*` y orígenes de otras wallets + `use_dynamic_url: true`, con su fila de justificación en §7.3 y corrección de la redacción de RNF-10. | ADT-20 |
| **D-P** | `notifications` sale del MVP y pasa a `optional_permissions`. | ADT-30 |
| **D-Q** | *Token bucket* por origen extendido a **todo** el catálogo RPC, no solo a los métodos aprobables. | ADT-24 |
| **D-R** | Persistir la marca de «transacción en vuelo» por cuenta y la ventana de tasa por origen. | ADT-23 |
| **D-S** | Provider definido con `Object.defineProperty` **no configurable**; prohibición explícita de `chrome.storage.sync` verificada por test; cuatro filas nuevas en el modelo de amenaza. | ADT-26 |
| **D-T** | Redacción de logs unificada en **los primeros 10 bytes** de `data`. | ADT-12 |
| **D-U** | Se añade la columna «Criterio de verificación / evidencia» a la tabla de riesgos y se especifican íntegramente el build (§7.5), el arnés E2E (§7.4), los contratos internos (§2.5.2) y el contrato Forge (§5.4). | ADT-01, ADT-05, ADT-16, ADT-17, ADT-27 |

## 4. Decisiones pendientes de consulta

| ID | Pregunta | Hallazgo |
|---|---|---|
| **P-20** | ¿Qué política aplicamos al **portapapeles** al revelar la frase semilla? | ADT-09 |
| **P-21** | ¿**Una sola** ventana de confirmación global o una por origen? | ADT-22 |
| **P-22** | ¿`wallet_addEthereumChain` **activa** la red nueva o exige un cambio aparte? | ADT-25 |

---

## 5. Anexo — Comprobaciones que salieron correctas

- **40/40 RF Must asignados a módulo** en §6.2 y **40/40 presentes en §6.1**; matriz RF→CU **idéntica** a `casos_uso.md` §5.1 (0 discrepancias).
- **346 identificadores cruzados verificados** (36 CU, 50 RF, 25 RNF, 13 RT, 4 RE, 20 ADR, 36 DEC, 42 H, 30 ACU, 65 módulos, 25 referencias de sección) con **0 inexistentes o fuera de rango**.
- **0 secciones vacías** ni marcadores de posición en las 10 secciones; **65/65 módulos** con responsabilidad definida.
- **0 defectos de `H-01..H-42` y `ACU-01..ACU-30` reintroducidos** (verificado: `eth_sign` fuera del catálogo, cola persistida sin `Map` de promesas, `chrome.alarms` en lugar de `setTimeout`, alias `window.codecrypto`, redacción, `setAccessLevel`, allowlist, retención 500/200, TTL 24 h y ausencia de `codecrypto_`).
- **25 defensas de seguridad concretas** especificadas (11 en la tabla de §3.7 y 14 transversales) y **9 de los 15 riesgos con verificación observable**.
- **Decisiones respetadas sin contradicción:** DEC-21, DEC-22, DEC-23, DEC-24, DEC-26, DEC-27, DEC-29, DEC-30, DEC-31, DEC-35, DEC-36 y ADR-01..ADR-20.
- **RE-03 sin caso de uso: legítimo** — es una restricción de proceso no observable en runtime, verificada por inspección del historial. 0 RE huérfanos.
