# 📌 Estado del Proyecto — TrueKeate Wallet

> Memoria de trabajo del proyecto. Se lee al inicio de cada turno y se actualiza de forma incremental.

**Última actualización:** 2026-09-13 — **Fase 4 — Pruebas ✅ COMPLETADA**: los **tres frentes** en verde (**Vitest 1185 passed / 71 ficheros · 0 failed**, **Playwright 84 passed / 0 failed / 0 flaky · 33 specs**, **Forge 30 passed · 2 suites**), cobertura de ramas de RNF-17 **cumplida** (global **86,11 %**, `crypto/` **86,98 %**, `approvals/` **95,60 %**, `shared/validation/` **93,13 %**) y `tsc -b`/`build`/`lint:prohibited`/`coverage` **exit 0**; **15 defectos corregidos** más **4** de concurrencia y retención, y **4 flecos cerrados** sin decisión de producto. Informe consolidado en **`RepoTecnico/INFORME_PRUEBAS_FASE4.md`** y batería real en **§3.7**. **Siguiente: Fase 5 — Manuales.** Salvedades que **siguen abiertas**: **RNF-15 verificado solo en Windows** (sin WSL2 ni CI en el equipo) y **ensayo del Perfil B no verificado por sujeto externo**. *Histórico:* **Fase 3 — Desarrollo ✅ COMPLETADA (H1..H6)**: **H1 ✅ COMPLETADO** (build MV3 de las 6 entradas, arnés de pruebas e instrumento Forge; ID de extensión **`oiahebaliobknoeeonhgaacapjcpgblo`**), **H2 ✅ COMPLETADO** (cartera, cuentas HD, importación por clave privada, recepción con QR, revelado con higiene, reset y persistencia: **Vitest 290 passed / 18 ficheros**, **Playwright 23 passed / 0 failed**), **H3 ✅ COMPLETADO** (provider `window.truekeate` + alias `window.codecrypto`, EIP-6963, conexión con `connect.html`, sesión por origen de **24 h renovables**, revocación, eventos y polling de saldos contra Anvil: **Vitest 403 passed / 28 ficheros**, **Playwright 37 passed / 0 failed**), **H4 ✅ COMPLETADO (2026-09-12)** (firma, aprobación con vista previa decodificada y transacciones EIP-1559/EIP-155: **Vitest 587 passed / 41 ficheros**, **Playwright 44 passed / 0 failed / 0 skipped**, `tsc -b`/`build`/`lint:prohibited` exit 0, `forge:test` **10/10** y una transacción real con hash `0x21f9…33a6` y recibo `status 0x1`) y **H5 ✅ COMPLETADO (2026-09-12)** (redes con aprobación y permiso de host en runtime, `chainChanged` a todas las pestañas, panel de actividad con exportación JSON y cuota observable: **Vitest 636 passed / 47 ficheros**, **Playwright 58 passed / 0 failed / 0 skipped**, `forge:test` **10/10**, **Anvil 31337** + **secundario 31338**), **H6 ✅ COMPLETADO (2026-09-12)** (identidad visual en las **cuatro** superficies, accesibilidad AA con `axe-core` y teclado, documentación de la rúbrica —`README.md` en 4 pasos, `INSTRUCCIONES.md` con los 15 flujos, `LICENSE` y `NOTICE`—, **JSDoc de contrato por módulo** con gate mecánico `src/docs.spec.ts` (**145 ficheros / 148 comprobaciones**), **umbrales de cobertura de RNF-17 aplicados por configuración** y **cumplidos** —ramas: global **86,13 %**, `crypto/` **86,38 %**, `approvals/` **95,21 %**, `shared/validation/` **91,09 %**—, **Vitest 1076 passed / 63 ficheros**, **Playwright 74 passed / 0 failed / 0 skipped** en ventana limpia, `tsc -b`/`build`/`lint:prohibited`/`forge`/`check:mermaid` **exit 0** y evidencia en `RepoTecnico/evidencia/H6/` con `ACTA_H6.md`), con las **5 desviaciones aceptadas** de H1 (**DEC-47..DEC-51**), las **5 correcciones aceptadas** de H2 (**DEC-52..DEC-56**), los **5 puntos de cierre** de H3 (**DEC-57..DEC-61**), los **7 puntos de cierre** de H4 (**DEC-62..DEC-68**), los **10 puntos de cierre** de H5 (**DEC-69..DEC-78**) y los **9 puntos de cierre** de H6 (**DEC-79..DEC-87**). La Fase 2 quedó **CERRADA** (`VEREDICTO_FASE2_V1.md` v1.3: **0 residuales**, 18/18 contradicciones, 10/10 `VR-01..VR-10`). **✅ FASE 3 — DESARROLLO COMPLETADA (H1..H6). Siguiente: Fase 4 — Pruebas.** Salvedades declaradas: **RNF-15 verificado solo en Windows** (sin WSL2 en el equipo) y **ensayo del Perfil B no verificado por sujeto externo**; **D-H6-B quedó cerrada** en el mismo turno (los specs de contraste leen la paleta de `tokens.css`).
**Versión de este documento:** ✅ **v2.7**
**Fase actual:** **5 de 5 — Manuales ⏭️ OMITIDA POR DECISIÓN DEL USUARIO (2026-09-13)** (Fases 1–4 ✅ completadas; el usuario ordenó omitir la creación de manuales, imágenes, PDF y sección de Ayuda; se conservan los 42 documentos de manuales ya generados). Cierre anterior: **4 de 5 — Pruebas ✅ COMPLETADA (2026-09-13)** — los **tres frentes** cerraron en verde: **Vitest 1185 passed / 71 ficheros · 0 failed**, **Playwright 84 passed / 0 failed / 0 flaky / 0 skipped · 33 specs** y **Forge 30 passed · 2 suites**, con `npx tsc -b`, `npm run build`, `npm run lint:prohibited` y `npm run coverage` en **exit 0** y los **umbrales de ramas de RNF-17 cumplidos** (global **86,11 %**, `crypto/` **86,98 %**, `approvals/` **95,60 %**, `shared/validation/` **93,13 %**). Se corrigieron **15 defectos** (el más grave: el hueco no-cadena en `truekeate_accounts`, que hacía que `wallet_revealSecret('idx:2')` entregara la clave privada de **otra** cuenta) **más 4** de concurrencia y retención, y se cerraron **4 flecos** sin decisión de producto (ventana de tasa 6/60 s del plan, error tipado `invalidAmount`, rechazo del nodo `-32003` sin confundirlo con «sin conexión», y RMW serializado + purga de `truekeate_connect_request`). Informe consolidado en **`RepoTecnico/INFORME_PRUEBAS_FASE4.md`** y batería real en **§3.7**. **Siguiente paso: Fase 5 — Manuales** (parte ya entregada en H6: `README.md`, `INSTRUCCIONES.md`, `LICENSE`, `NOTICE`). **Siguen abiertas, declaradas y sin maquillar** las dos salvedades heredadas de la Fase 3: **RNF-15 verificado solo en Windows** (no hay WSL2 ni CI en el equipo) y **ensayo del Perfil B no verificado por sujeto externo**; los **10 RF Should (C1..C10)** siguen **fuera del MVP** con su estado real medido. Fases 1, 2 y 3 ✅ cerradas.
**Comando para continuar:** `/estado` · **Fase 5 — Manuales** · `/push` (solo con orden explícita)
**Rama de trabajo activa:** `chrome-wallet-DSH`
**Historial de cambios:** v1.4 decisiones DEC-21..DEC-26 y remediación de `INFORME_OPTIMIZACION_V1.md` · v1.5 decisiones **DEC-27..DEC-36** (P-17, P-18, P-19 y D-A..D-G), `requerimientos.md` v1.5 con **50 RF (40 Must / 10 Should)** y desviaciones **D-01..D-13**, y alta de los artefactos de casos de uso (**36 CU**) y de su auditoría (**30 hallazgos**) · **v1.6** decisiones **DEC-37..DEC-44** (P-20, P-21, P-22 y D-J..D-Q), `requerimientos.md` **v1.6** (RF-50 con 30 s, ocultado por pérdida de foco y política de portapapeles; RNF-09 y RNF-10 corregidos; RF-23/RF-35 y RT-04/RT-13 ajustados) y `estado_proyecto.md` **v1.6** con el **cierre de la Fase 2** y los artefactos `documento_tecnico.md` v1.0 (+`AUDITORIA_DOCUMENTO_TECNICO_V1.md`, 33 hallazgos) y `casos_uso/diagramas.md` v1.0 (12 diagramas).
 · **v1.7** sincronización de la memoria con el **veredicto de reevaluación** (`VEREDICTO_FASE2_V1.md` v1.0, registrado en §2): la Fase 2 queda **NO cerrada** con **12 residuales `R-01..R-12`** y **18 contradicciones** en cierre documental; se alinean las versiones reales de todos los artefactos (`requerimientos.md` v1.7, `diccionario_datos.md` v1.6, `entornos_globales.md` v1.8, `identidad_visual.md` v1.4, `casos_uso.md` v1.3, `diagramas.md` v1.1, `documento_tecnico.md` v1.2) y se corrigen los conteos (66 módulos M1..M66, 9 `sequenceDiagram`), los próximos pasos y los criterios de aceptación de la Fase 2.
 · **v1.8** cierre de **`R-09`** con las decisiones **DEC-45** (bloqueo del revelado/exportación y del borrado de una cuenta importada cuando la cuenta está en uso por una dApp) y **DEC-46** (bloqueo del reset con la cola `truekeate_pending_requests` no vacía o una transacción en vuelo en `truekeate_inflight_tx`): se sincronizan la tabla §2, los próximos pasos y los criterios de la Fase 2 con el **`VEREDICTO_FASE2_V1.md` v1.2** (**0 residuales abiertos**, **18/18 contradicciones resueltas**) y queda **pendiente solo la última pasada de consistencia** antes de la Fase 3.
 · **v1.9** cierre de la Fase 2: la última pasada de consistencia cerró los **10 defectos residuales `VR-01..VR-10`** —permisos del manifest unificados en los cuatro documentos (`favicon`, `clipboardRead` y `clipboardWrite`), catálogo de eventos a **24**, tabla de errores del técnico convertida en **índice código → causa** contra `diccionario_datos.md` §4.3, retirada de `windowsByApprovalId`/`windowId` sustituidos por `truekeate_approval_window`, afirmación no falsable del puerto reescrita, `settings.networks` → `truekeate_networks`, evidencias normalizadas, **15** diagramas Mermaid en el técnico, fuentes sincronizadas y **0 mojibake**— y `VEREDICTO_FASE2_V1.md` **v1.3** declara el corpus ✅ **consistente y apto para autorizar la Fase 3**; se sincroniza la tabla §2 con las versiones finales y se marcan cumplidos los criterios de la Fase 2.
 · **v2.0** apertura de la **Fase 3 — Desarrollo** y **cierre de H1** (2026-09-11): fase actual **3 de 5 🚧 EN CURSO**, hito completado **H1** (andamiaje, build MV3 de las 6 entradas e instrumento Forge; ID de extensión **`oiahebaliobknoeeonhgaacapjcpgblo`**) e **hito actual H2**; **§2** incorpora los artefactos de código creados (`src/`, `vite.config.ts`, `vitest.config.ts`, `playwright.config.ts`, `tsconfig*.json`, `eslint.config.js`, `scripts/`, `contracts/`, `test/`, `e2e/`, `test.html`, `package.json`/`package-lock.json`, `public/fonts/`) y la evidencia de H1; **§4** añade las decisiones **DEC-47..DEC-51** (las 5 desviaciones aceptadas de H1, en `plan_desarrollo.md` §3.1.10); versiones del corpus sincronizadas (`plan_desarrollo.md` v1.1, `documento_tecnico.md` v1.5, `entornos_globales.md` v2.0, `identidad_visual.md` v1.5).
 · **v2.1** **cierre de H2** (2026-09-11): fase **3 de 5 EN CURSO** con **H1 y H2 ✅ completados** e **hito actual H3**; **§2** incorpora los artefactos de H2 (los **12 specs de Vitest** del hito, los **7 specs de Playwright** —`01`, `02`, `03`, `05`, `06`, `16`, `25`—, las claves nuevas de `src/` y la evidencia de `RepoTecnico/evidencia/H2/` con `ACTA_H2.md`); **§3.2** recoge los **7 comandos verificados con su resultado real** (`tsc -b`, `build`, `lint:prohibited`, Vitest **290**, `coverage` **95,13 %/97,27 %**, Playwright **23 passed / 0 failed**, y los `grep` de `chrome.storage`/`ethers` en `src/popup` → **0**); **§4** añade las decisiones **DEC-52..DEC-56** (las **5 correcciones aceptadas** de H2, documentadas en `plan_desarrollo.md` §3.2.10); **§7** marca **H2 cerrado** y **H3 como hito actual**; **§8.3** declara el hito **H2 ✅ COMPLETADO**; versiones del corpus sincronizadas (`plan_desarrollo.md` v1.2) · **v2.2 (esta versión)** **cierre de H3** (2026-09-11): fase **3 de 5 EN CURSO** con **H1, H2 y H3 ✅ completados** e **hito actual H4**; **§2** incorpora los artefactos de H3 (`src/connect/**`, `src/popup/views/SitesView.tsx`, `src/popup/hooks/useBalancePolling.ts`, `src/background/{sessions,events,connections}.ts`, `src/background/rpc/{client,pageMethods,rateLimit,internalMethods}.ts`, `src/background/networks/catalog.ts`, `test.html`, `e2e/07..28`, la evidencia de `RepoTecnico/evidencia/H3/` con `ACTA_H3.md` y `documento_tecnico.md` **v1.7**), con las versiones de `plan_desarrollo.md` **v1.3** y `entornos_globales.md` **v2.1**; **§3.3** recoge los **comandos verificados de H3** con su resultado real (`tsc -b`, `build` y `lint:prohibited` exit 0, Vitest **403 passed / 28 ficheros**, Playwright **37 passed / 0 failed**, `rg` de `chrome.storage`/`ethers` en `src/popup`/`src/connect` → **0** y los tres `cast` de Anvil: `31337`, bloque **≥ 1** y **10 000 ETH**); **§4** añade las decisiones **DEC-57..DEC-61** (los **5 puntos de cierre** de H3, documentados en `plan_desarrollo.md` §3.3.10); **§7** marca **H3 cerrado** y **H4 como hito actual**; **§8.3** declara el hito **H3 ✅ COMPLETADO**.
 · **v2.3** **puesta en verde del cableado de H4 — hito EN CURSO, no completado** (2026-09-12): **§1/§7/§8.3** reflejan que el **hito actual sigue siendo H4** y que `CA-RF-35`, `CA-RF-41` (parte E2E) y RNF-08 (parte E2E) quedan **PARCIALES**; **§3.4** nueva con la **batería real** del hito (`tsc -b`, `build` y `lint:prohibited` exit 0, `forge:test` **10/10**, Vitest **587 passed / 41 ficheros**, Playwright **42 passed / 2 failed** y la **prueba real de extremo a extremo** con hash `0x21f97614e3117d6e9083ba46b8b2be20c428008e5f6801adcc19fc00741833a6`, recibo `status 0x1`, cola vacía y marca en vuelo liberada); **§2** sincroniza las versiones (`plan_desarrollo.md` **v1.4**, `entornos_globales.md` **v2.2**, esta memoria **v2.3**); **§4** añade las decisiones **DEC-62..DEC-65**: el cableado de los 6 aprobables era un problema de **metadatos del catálogo**, no del router (**D-H4-E1**), el comando de Anvil del arnés usa **`--allow-origin`** y no `--http.corsdomain` (**D-H4-E3**), la regresión de H3 **D-H4-E2** queda **verificada en verde sin cambio de producción** (plazo de conexión inyectado por el arnés) y los **dos bloqueos abiertos** **D-H4-E9**/**D-H4-E10** (servidor de la dApp solo en IPv6 y transporte del relay al suspender el SW) quedan documentados con su causa raíz y su corrección exacta, **pendientes de autorización** por vivir en `vite.config.ts` y `src/content-script.ts`, fuera del terreno autorizado.

 · **v2.5 (esta versión)** **cierre de H5** (2026-09-12): **§1/§7/§8.3** declaran **H5 ✅ COMPLETADO** y **hito actual H6**; **§2** incorpora los artefactos de H5 (`src/background/networks/**` con M23/M24/M25 y su `networks.spec.ts`, `src/background/logging/**` con M30/M31/M32, `src/popup/views/{NetworksView,LogsView}.tsx`, las pestañas «Redes» y «Actividad» de `App.tsx`, las clases nuevas de `base.css`, `test.html` con los 7 flujos, los **4 specs de Playwright** del hito y la evidencia de `RepoTecnico/evidencia/H5/` con `ACTA_H5.md`); **§3.5** recoge la **batería real de cierre** (`tsc -b`, `build` y `lint:prohibited` exit 0, Vitest **636 passed / 47 ficheros**, Playwright **58 passed / 0 failed / 0 skipped**, `forge:test` **10/10** y los **dos nodos Anvil**: 31337 y el secundario 31338); **§4** añade las decisiones **DEC-69..DEC-78**: el permiso de host se pide en el popup dentro del clic (**D-H5-A**), el ciclo aprobable de red no exige sesión de dApp (**D-H5-B**), el saldo de la vista previa se **reporta** por reintentar 7 s (**D-H5-C**, `approvals/**` es de solo lectura), el router escribe la traza de cada llamada (**D-H5-D**), el descarte por cuota no re-persiste el lote (**D-H5-E**), el contador de descartes solo se escribe al cambiar y la medición de RNF-08 usa intervalo fijo (**DEC-74/DEC-77**), `D-H5-N` queda **reportado** (alta desde una dApp: gesto de la ventana de confirmación) y `D-H5-O` **NO VERIFICADO** (alta de red nueva en `E2E_HEADLESS`), más las **cinco correcciones del arnés E2E** de H5 (**DEC-76**); versiones sincronizadas (`plan_desarrollo.md` **v1.6**).
 · **v2.4** **cierre de H4** (2026-09-12): **§1/§7/§8.3** declaran **H4 ✅ COMPLETADO** y **hito actual H5**; **§2** incorpora los artefactos de H4 (`src/background/approvals/**` con M14..M19, `src/background/crypto/sign.ts`, `src/background/rpc/txContract.ts`, `src/notification/**`, `contracts/`, los **14 specs** de Vitest del hito y los **6 specs** de Playwright, además de la evidencia de `RepoTecnico/evidencia/H4/` con `ACTA_H4.md`); **§3.4** recoge la **batería real de cierre** (`tsc -b`, `build` y `lint:prohibited` exit 0, Vitest **587 passed / 41 ficheros**, Playwright **44 passed / 0 failed / 0 skipped**, `forge:test` **10/10** y la prueba real de extremo a extremo con hash y recibo `status 0x1`); **§4** cierra **DEC-65** (los dos bloqueos quedan **cerrados**) y añade **DEC-66..DEC-68**: la corrección de `host` en `vite.config.ts` (**D-H4-E9**), la correlación `requestId` + transporte del content script (**D-H4-E10**) y los **dos defectos medidos nuevos** del re-render de la ventana única —canal del empuje (**D-H4-E11**) y carrera de orden con `SIGN_RESPONSE` (**D-H4-E12**)—; versiones sincronizadas (`plan_desarrollo.md` **v1.5**).

---

## 1. Resumen del proyecto

Extensión de navegador Chrome/Edge (Manifest V3) que funciona como **wallet Ethereum estilo MetaMask**, con provider EIP-1193 inyectado (`window.truekeate`), gestión de cuentas HD e importadas por clave privada, envío y recepción de transferencias (con QR), firma de transacciones y de datos EIP-712, firma de mensajes (`personal_sign`), y una **dApp de pruebas** que consume la wallet contra una **red local de Foundry (Anvil)**.

**Stack:** React 19 · TypeScript 5.9 · Vite 7 · ethers.js v6 · Chrome Extension APIs (MV3).
**Pruebas:** Vitest + Playwright + Forge (contrato verificador EIP-712).
**Alcance de despliegue:** 100 % local (sin GCP).

---

## 2. Artefactos de `RepoTecnico/`

| Archivo | Estado | Contenido |
|---|---|---|
| `requisitos.md` | ✅ Fuente (no se modifica) | Enunciado original de la tarea (36 especificaciones). |
| `TAREA_PARA_ESTUDIANTE.md` | ✅ Fuente (no se modifica) | Enunciado extendido con arquitectura y ejemplos de código. |
| `GUIA_RAPIDA_TESTING.md` | ⏳ Línea base previa (**no vinculante**) | Guía del prototipo anterior (nomenclatura `codecrypto_*`); P-10/DEC-09 decidió **no reutilizarla** (H-24). El proyecto tendrá su propia guía de troubleshooting. |
| `requerimientos.md` | ✅ **v1.9** (ver cabecera) | **50 RF (40 Must / 10 Should)**, 25 RNF, 13 RT y 4 RE, actores, desviaciones **D-01..D-13**, riesgos, entrevista cerrada y **criterio de aceptación + evidencia por RF/RT** (H-01/DEC-25). Incorpora **RF-50** (revelar/exportar semilla y claves privadas con confirmación explícita, D-13), la **caducidad de sesión de dApp** en RF-25 (D-B) y la **aprobación del cambio de red** en RF-22 (P-19). **v1.6 (ADT-04/06/09/12/14/19/20/22/25/30; P-20/P-21/P-22):** RF-50 con **30 s**, ocultado por pérdida de foco y **política de portapapeles**; **RNF-09** («nunca hacia la página ni por `postMessage`») y **RNF-10** (`<all_urls>` con `use_dynamic_url` y `exclude_matches`) reescritos; RF-23 **añade sin activar**; RF-35 → **una sola ventana global**; RT-04 con `notifications` **opcional**; RT-13 con **UUID literal y `key` fija**; redacción de logs a **10 bytes** de `data`. **v1.8 (R-09/DEC-45/DEC-46):** `CA-RF-05`, `CA-RF-06`, `CA-RF-11` y `CA-RF-50` recogen las **dos guardas de estado** (cuenta en uso por una dApp y reset con cola no vacía o transacción en vuelo, ambas `-32000`). **v1.9 (`VR-01`/`VR-07`/`VR-08`):** RT-04 y `CA-RT-04` fijan el **conjunto cerrado de permisos** (`storage`, `alarms`, `favicon`, `clipboardRead`, `clipboardWrite`) con la justificación de cada uno, RT-06 cita la clave canónica `truekeate_networks` y cuatro evidencias pasan a las formas admitidas. |
| `diccionario_datos.md` | ✅ **v1.8** (ver cabecera) | Claves de storage, entidades, protocolo de mensajes y catálogo RPC/errores. Red única Anvil. Decisiones P-03..P-08 aplicadas. **§4.3 es la fuente única de los literales de mensaje** (ACU-05/D-E) y el catálogo de eventos tiene **24** valores. **v1.7 (R-09/DEC-45/DEC-46):** dos filas nuevas en §4.3 (causas «Cuenta en uso por una dApp conectada» y «Reset bloqueado», ambas con `-32000`), guarda de sesión activa en §3.10 y **§3.11 nueva** con las guardas y el orden de comprobación del reset. **v1.8 (`VR-01`/`VR-03`/`VR-06`):** permisos del manifest unificados con el técnico y `entornos_globales.md` §4, referencias a las constantes de §3 de ese documento ya existentes y formulación falsable del puerto de larga vida en §3.4. |
| `entornos_globales.md` | ✅ **v2.2** (ver cabecera) | Entorno verificado, comandos, constantes, permisos de **mínimos privilegios**, política de versiones, 3 remotos, herramientas de prueba, identidad visual y GCP cerrado. **v1.9 (`VR-01`/`VR-03`):** `permissions` = `storage`, `alarms`, `favicon`, `clipboardRead` y `clipboardWrite`, con fila de justificación por permiso, y **siete constantes nuevas** en la tabla de §3. **v2.0 (H1 / DEC-47, DEC-49 y DEC-51):** **prohibido** el esquema `chrome-extension://` en `matches`/`exclude_matches` (§4, patrón eliminado), **ficheros de fuente reales** con Poppins no variable y licencias OFL (§1/§2) y literal normativo de Forge `forge test --root contracts --match-contract EIP712VerifierTest` (§2/§8). **v2.1 (H3 / DEC-61):** comando de Anvil que **sí** funciona con el arnés, **sin `--silent`**, con el aviso de que **`anvil --silent` no arranca** cuando se lanza con `Start-Process` y redirección de salida (exit 1 → `ERR_CONNECTION_REFUSED`), y la recomendación de que el `global-setup` **aborte con un mensaje claro si Anvil no responde** (§2.1/§2.4). **v2.2 (H4 / DEC-63):** **corrige la bandera de CORS** —`--http.corsdomain` **no existe** en Anvil 1.7.2-dev (`error: unexpected argument '--http.corsdomain' found`, medido)— a **`anvil --host 127.0.0.1 --port 8545 --chain-id 31337 --allow-origin "*"`**, **sin `--silent`**; la allowlist normativa usa la misma bandera con **lista separada por comas** (no repetible) y la nota documenta **los dos errores**. |
| `identidad_visual.md` | ✅ **v1.5** (ver cabecera; aprobado, P-14) | **Anexo vinculante de diseño**: marca TrueKeate, paleta medida, degradados, tipografía, componentes, tokens CSS, **matriz de contraste cerrada** y criterios de accesibilidad (H-17). **v1.5 (H1 / DEC-49 y DEC-50):** **tamaño explícito del `body`** de las tres ventanas con los tokens `--tk-*-width/height` (§5.1) y **ficheros de fuente reales** —Poppins en tres pesos discretos, Inter y JetBrains Mono variables, licencias OFL— (§4). |
| `INFORME_OPTIMIZACION_V1.md` | ✅ v1 (Fase 2) | **Informe de auditoría de la Fase 2**: **42 hallazgos** (2 CRITICA · 14 ALTA · 21 MEDIA · 5 BAJA) y **27 descartados**, con plan de acción (QW-1..QW-16, M-1..M-18, RM-1..RM-7) y criterios de aceptación. |
| `estado_proyecto.md` | ✅ **v2.5** (este archivo) | Memoria de trabajo. Decisiones **DEC-01..DEC-78**. **v2.4:** **H4 ✅ COMPLETADO** (2026-09-12), **hito actual H5**, **§2** con los artefactos de H4 (código, specs y evidencia), **§3.4** con la batería de cierre (Playwright **44 passed / 0 failed / 0 skipped**) y **§4** con **DEC-65 cerrada** y **DEC-66..DEC-68**. **v2.5:** **H5 ✅ COMPLETADO** (2026-09-12), **hito actual H6**, **§2** con los artefactos de H5 (`networks/**`, `logging/**`, `NetworksView`, `LogsView`, pestañas nuevas, `test.html` de 7 flujos y los 4 specs E2E del hito), **§3.5** con la batería real (Vitest **636**, Playwright **58 passed / 0 failed / 0 skipped**, Forge **10/10** y los **dos nodos Anvil**) y **§4** con **DEC-69..DEC-78** (permiso de host en el popup, ciclo de red sin sesión, traza por llamada del router, cuota observable, `D-H5-N` reportado y `D-H5-O` no verificado). |
| `casos_uso/casos_uso.md` | ✅ **v1.5** (Fase 2, ver cabecera) | **36 casos de uso** (Gherkin/EARS) con fichas completas, flujos alternativos y de excepción, criterios con evidencia y matriz de trazabilidad `E-xx → RF-xx → CU → test`. Sus gráficos viven en `casos_uso/diagramas.md`. **v1.4 (R-09):** convención 13 (guardas de estado) y flujos/criterios nuevos en CU-06, CU-07 y CU-30; §2, §5 y §7 siguen siendo idénticas entre sí. **v1.5 (`VR-04`/`VR-05`/`VR-08`/`VR-10`):** literal de vencimiento de CU-15 con «(120 s)», retirada de `windowsByApprovalId` y del `windowId` por solicitud (CU-08 y CU-15) a favor de `truekeate_approval_window`, evidencias canónicas y mojibake corregido. |
| `casos_uso/AUDITORIA_CASOS_USO_V1.md` | ✅ v1.0 (Fase 2) | **Auditoría de los 36 CU**: **30 hallazgos** (ACU-01..ACU-30: 2 CRITICA · 8 ALTA · 18 MEDIA · 2 BAJA), decisiones **D-A..D-G** y **P-17/P-18/P-19** resueltas; su remediación se aplicó en `requerimientos.md` v1.5 y en esta memoria (**DEC-27..DEC-36**). |
| `casos_uso/diagramas.md` | ✅ **v1.2** (Fase 2) | **12 diagramas** UML de los 36 CU: 9 `sequenceDiagram` (Figuras 2–10), 2 `flowchart` (Figura 1 de casos de uso y Figura 12 de arquitectura) y 1 `stateDiagram-v2` (Figura 11, ciclo de vida del `PendingRequest`), con índice de trazabilidad y notas de renderizado. **v1.2 (`VR-09`):** bloque «Fuentes» sincronizado con las versiones vigentes (`casos_uso.md` v1.5, `requerimientos.md` v1.9, `diccionario_datos.md` v1.8 y `entornos_globales.md` v1.9) e historial reparado. |
| `documento_tecnico.md` | ✅ **v1.7** (ver cabecera) | Arquitectura y especificación técnica: **66 módulos (M1..M66)**, **20 ADR**, **15** diagramas Mermaid, contratos internos, contrato Forge y trazabilidad. Auditado en `AUDITORIA_DOCUMENTO_TECNICO_V1.md`, con la remediación ya aplicada. **v1.4:** permisos del manifest idénticos a `entornos_globales.md` §4, catálogo de eventos a **24**, tabla de errores convertida en índice código → causa, retirada de `windowsByApprovalId` y del `windowId` por solicitud, y conteo de **15** diagramas Mermaid. **v1.5 (H1 / DEC-47..DEC-51):** **prohibido** `chrome-extension://` en `matches`/`exclude_matches` (§7.3), **3 builds encadenados** con la API `build()` desde `closeBundle` (§7.5.2/§7.5.3), **tamaño explícito del `body`** en las tres ventanas (§5.2) y literal normativo `forge test --root contracts --match-contract EIP712VerifierTest` con la nota del **falso verde** (§7.2/§7.4). §7.4.1.f pasa a `H1..H6` (cierre de la brecha **G-01**). **v1.6 (H2):** §5.1.1 amplía el contrato interno a **15** métodos `wallet_*` (el popup no toca el almacén, RNF-14). **v1.7 (H3):** cierra los dos huecos de contrato del hito —`wallet_getState.connectedSites` y el nuevo `wallet_getConnectRequest` (§5.1.1 pasa a **16** métodos)—, sin claves ni causas de error nuevas. |
| `AUDITORIA_DOCUMENTO_TECNICO_V1.md` | ✅ **v1.0** (Fase 2) | **Auditoría del documento técnico**: **33 hallazgos** (ADT-01..ADT-33), decisiones **D-H..D-U** y **P-20/P-21/P-22** resueltas; su remediación se aplica al `documento_tecnico.md`, a `requerimientos.md` v1.6 y a esta memoria (**DEC-37..DEC-44**). |
| `VEREDICTO_FASE2_V1.md` | ✅ **v1.3** (Fase 2, reevaluación y cierre) | **Veredicto de reevaluación y cierre de la Fase 2**: ✅ **corpus consistente y apto para autorizar la Fase 3** (**0 residuales abiertos** —12/12 `R-01..R-12` cerrados, `R-09` con DEC-45/DEC-46—, **18/18 contradicciones resueltas** y **0 defectos residuales** de consistencia —10/10 `VR-01..VR-10` cerrados en la **v1.3**, §7—). Incluye la tabla de cierre CRITICA/ALTA de las tres auditorías, la evidencia `ruta:línea` de ambos lados de cada contradicción y el plan de cierre por bloques (§5). |
| `plan_desarrollo.md` | ✅ **v1.5** (Fase 3, H1, H2, H3 y H4 cerrados) | Plan de desarrollo vertical por hitos y **estimación por hitos** (sustituye al «~40 h», H-34). **v1.1:** §3.1.9 marca **H1 ✅ COMPLETADO (2026-09-11)**. **v1.2:** §3.2.9 marca **H2 ✅ COMPLETADO (2026-09-11)**; **§3.2.10** con las **5 correcciones aceptadas** de H2 (**D-H2-G..D-H2-K**). **v1.3:** §3.3.9 marca **H3 ✅ COMPLETADO (2026-09-11)**; **§3.3.10** con los 5 puntos de cierre (**D-H3-A..D-H3-D** y la nota operativa de Anvil). **v1.4:** **§3.4.10** nueva con el cableado de H4 y los bloqueos **D-H4-E9**/**D-H4-E10**; H4 **no** se marca completado. **v1.5:** §3.4.9 marca **H4 ✅ COMPLETADO (2026-09-12)** con la batería de cierre (Vitest **587 passed / 41 ficheros**, Playwright **44 passed / 0 failed**, `forge:test` **10/10**); **§3.4.10** cierra **D-H4-E9** (`host` en `vite.config.ts`), **D-H4-E10** (transporte del relay + `requestId`) y añade **D-H4-E11**/**D-H4-E12** (canal del empuje a la ventana única y carrera de orden con `SIGN_RESPONSE`); §5 y §6 marcan H4 completado. **v1.6:** cierre de **H5** (matriz §6 con la fila del hito y §3.5.10 con las 10 desviaciones). **v1.7 (esta versión):** §3.6.9 marca **H6 ✅ COMPLETADO (2026-09-12)** con la batería real; **§3.6.10 nueva** con las **9 desviaciones aceptadas** (**D-H6-A**..**D-H6-I**: cobertura de `approvals/` cubierta desde `test/oracle/**`, literales de color de los specs de contraste, dos citas de evidencia desactualizadas, RNF-15 solo Windows, ensayo del Perfil B no independiente, «Acerca de» por RNF-23, `grep http dist/`, 35 cabeceras JSDoc completadas y la contaminación por concurrencia de la primera batería E2E); **§2, §5 y §6** marcan **H2, H5 y H6 ✅ COMPLETADOS** y §5 declara el **esfuerzo real** (sin medición horaria por sesión; sin activar la regla de desviación >1,3×). **Fase 3 completada.** |
| **Código de la extensión (creado en H1)** | ✅ existe y verificado (`npm run build` exit 0) | `src/` con **31 ficheros**: `manifest.ts` (M1) · `background.ts` (M2) y `background/rpc/{router,catalog,errors}.ts` (M3/M4/M6) · `background/security/{senderGuard,accessLevel}.ts` (M20/M21) · `background/state/schema.ts` (M33) · `inject/{index,provider,eip6963,icon-data}.ts` (M35..M37) · `content-script.ts` (M38) · `popup/{main.tsx,App.tsx}` (M39) · `connect/`, `notification/` · `shared/{types,protocol,constants}.ts` (M55..M57) · `styles/{tokens,base}.css` (M64/M65) · los 4 `.spec.ts` de H1 (`manifest`, `naming`, `windowContract`, `errors`). |
| `package.json` / `package-lock.json` | ✅ versionados (H1) | **9 scripts** (`dev`, `build`, `test`, `test:watch`, `coverage`, `test:e2e`, `lint:prohibited`, `check:mermaid`, `typecheck`, `forge:test`). Dependencias: `ethers` `~6.15.0`, `react`/`react-dom` `^19.0.0`; herramienta: `typescript` `~5.9.0`, `vite` `^7.0.0`, `vitest` `^3.0.0`, `@vitest/coverage-v8` `^3.0.0`, `jsdom` `^26.0.0`, `@playwright/test` `^1.50.0`, `eslint` `^9.0.0`, `typescript-eslint` `^8.0.0`, `mermaid` `^11.0.0`, `@types/chrome` `^0.1.0`, `@types/node` `^24.0.0`. |
| Configuración de build y pruebas (H1) | ✅ existe y verificado | `vite.config.ts` (3 builds encadenados: ES + IIFE × 2 y **generación del manifest** desde `src/manifest.ts`), `vitest.config.ts` (jsdom + `test/setup/chrome-stub.ts`), `playwright.config.ts` (proyecto `chromium-extension`, `globalSetup`), `tsconfig.json`/`tsconfig.node.json` (strict), `eslint.config.js`. |
| `scripts/` (H1) | ✅ existe y verificado | `lint-prohibited.mjs` (RT-03, nomenclatura, colores, `chrome.storage.sync`, `ethers` en el popup y 0 URLs de CDN en `dist/`), `check-mermaid.mjs` (**27/27** bloques), `generate-icons.ps1` y `generate-icon-data.mjs`. |
| `contracts/` (instrumento Forge, H1) | ✅ existe y verificado (**7 passed**) | Proyecto Foundry: `foundry.toml` (`solc` **0.8.24** fijado, `evm_version = "cancun"`, `optimizer = false`), `src/EIP712Verifier.sol`, `test/EIP712Verifier.t.sol` y `test/fixtures/eip712-signature.json` (fixture versionado), con `lib/forge-std` vendorizado (solo referencia de pruebas). |
| `test/` y `e2e/` (H1) | ✅ existe y verificado (**4 passed**) | `test/setup/chrome-stub.ts` (stub de `chrome.*` con reloj inyectable), `e2e/fixtures/extension.ts` (`launchPersistentContext` con perfil nuevo y descubrimiento del ID), `e2e/global-setup.ts` (plazos inyectados + evidencia) y `e2e/01-onboarding.spec.ts`. |
| `test.html` (H1) | ✅ existe | dApp de pruebas en la raíz, servida en `http://localhost:5174/test.html` (`strictPort`); la detección del provider en página real se cierra en **H3** (`E2E: 07-provider.spec.ts`). |
| `public/fonts/` (H1) | ✅ existe y versionado | **Poppins** en tres pesos discretos (`poppins-latin-400.woff2`, `-600`, `-700`), **Inter** variable (`inter-latin.woff2`), **JetBrains Mono** variable (`jetbrains-mono-latin.woff2`) y licencias **OFL-1.1** (`LICENSE-poppins.txt`, `LICENSE-inter.txt`, `LICENSE-jetbrains-mono.txt`). |
| `RepoTecnico/evidencia/H1/` | ✅ archivado (2026-09-11) | `build-*.log`, `typecheck-*.log`, `vitest-*.log`, `playwright-*.log`, `e2e-*.json`, `lint-prohibited-*.log`, `forge-eip712verifier-*.log`, `check-mermaid-*.log`, `manifest-*.json`, `01-onboarding-*.png` y **`ACTA_H1.md`**. |
| **Código de H2 (cartera y cuentas)** | ✅ existe y verificado (**Vitest 290 passed / 18 ficheros**) | `src/background/crypto/{mnemonic,hd,importAccount,secrets,integrity}.ts` (M8..M13) · `src/background/accounts.ts` (M28) · `src/background/settings.ts` (M29) · `src/background/state/{schema,migrations}.ts` (M33/M34) · `src/background/rpc/{catalog,router}.ts` (M3/M4, **15** métodos internos) · `src/popup/{App,walletState,walletRpc,popupErrors,validation,runtimeChannel}.tsx?` y `src/popup/{views,components}/` (M39..M46) · `src/shared/validation/{address,mnemonic,amount,privateKey}.ts` (M58..M61) · `src/shared/{format,qr}.ts` (M62/M63) · `src/shared/constants.ts` (M57). |
| **Specs de H2** | ✅ en verde | **Vitest (12 specs, 290 tests en total con los 4 de H1..H2)**: `crypto/{mnemonic,derivation,importPrivateKey,integrity,secretsExport,revealHygiene,revealClipboard}.spec.ts`, `accounts.spec.ts`, `state/{state,reset}.spec.ts`, `shared/validation/validation.spec.ts`, `shared/qr.spec.ts`. **Playwright (7 specs, 23 tests)**: `e2e/01-onboarding`, `02-cuentas`, `03-recibir`, `05-persistencia`, `06-reset`, `16-validacion`, `25-recuperacion` — **23 passed / 0 failed / 0 skipped**. |
| `RepoTecnico/evidencia/H2/` | ✅ archivado (2026-09-11) | `typecheck-2026-09-11.log`, `build-2026-09-11.log`, `lint-prohibited-2026-09-11.log`, `vitest-2026-09-11.log`, `vitest-coverage-2026-09-11.log`, `coverage-2026-09-11.json`, `playwright-2026-09-11.log`, `e2e-2026-09-11.{json,log}`, `e2e-global-setup-2026-09-11.{log,json}`, `01-onboarding-2026-09-11.{json,png}`, `25-recuperacion-2026-09-11.png`, `reset-2026-09-11.json`, `_verificacion-resumen.log` y **`ACTA_H2.md`**. |
| **Código de H3 (provider, conexión y lectura)** | ✅ existe y verificado (**Vitest 403 passed / 28 ficheros**) | `src/inject/{index,provider,eip6963,icon-data}.ts` (M35..M37, `window.truekeate` + alias `window.codecrypto`, EIP-6963 con `uuid` literal) · `src/content-script.ts` (M38, relay de los dos saltos con `targetOrigin` cerrado) · `src/background/{sessions,events,connections}.ts` (M26/M27: `truekeate_connected_sites` con TTL de 24 h renovables y propagación de `accountsChanged`/`connect`/`disconnect`/`message`) · `src/background/rpc/{client,pageMethods,rateLimit,internalMethods}.ts` (M5: `JsonRpcProvider` único con política cerrada de **4 llamadas** —1+3, backoff 1/2/4 s, timeout 5 s→ `4900`—; catálogo de lectura; *token bucket* 6/60 s por origen persistido; `internalMethods.ts`, módulo hoja de **D-H3-C**) · `src/background/networks/catalog.ts` (M23) · `src/background/security/senderGuard.ts` (M20) · `src/connect/{App,ConnectRow}.tsx` y `main.tsx` (M48/M49, 420×650) · `src/popup/views/SitesView.tsx` (M44) y `src/popup/hooks/useBalancePolling.ts` (M47, 1 RPC por cuenta visible cada 5 s) · `src/shared/{constants,types,protocol}.ts` (M55..M57) · `test.html` (**4 flujos** operativos). |
| **Specs de H3** | ✅ en verde | **Vitest (113 tests nuevos, 403 en total)**: `src/inject/inject.spec.ts`, `src/shared/naming.spec.ts` (ampliado), `src/background/rpc/{eip1193,setCurrentAccount,rpcRetry}.spec.ts`, `src/background/messaging.spec.ts`, `src/background/sessions.spec.ts`, `src/background/polling.spec.ts`, `src/background/rateLimit.spec.ts`, `src/background/originFrame.spec.ts` y `src/background/security/importOrder.spec.ts` (**D-H3-C**). **Playwright (14 tests nuevos, 37 en total)**: `e2e/07-provider`, `08-eventos` (incluye el cambio de cuenta activa de **D-H3-A**), `09-conectar`, `13-revocar`, `14-polling`, `27-rpc-caido` y `28-iframe-hostil` — **37 passed / 0 failed / 0 skipped / 0 flaky**. |
| `RepoTecnico/evidencia/H3/` | ✅ archivado (2026-09-11) | `vitest-2026-09-11.log`, `coverage-2026-09-11.log`, `e2e-2026-09-11.{log,json}`, `e2e-global-setup-2026-09-11.{log,json}`, `lint-prohibited-2026-09-11.log`, `cast-2026-09-11.log`, `07-provider-2026-09-11.json`, `08-eventos-2026-09-11.json`, `08-cambio-cuenta-2026-09-11.json`, `09-conectar-2026-09-11.{json,png}`, `13-revocar-2026-09-11.json`, `14-polling-2026-09-11.json`, `28-iframe-hostil-2026-09-11.json`, `rpc-caido-2026-09-11.log`, `diagnostico-ca-rf-15-2026-09-11.log`, `diagnostico-import-order-2026-09-11.log`, `25-recuperacion-2026-09-11.png` y **`ACTA_H3.md`**. |
| **Código de H4 (firma, aprobación y transacciones)** | ✅ existe y verificado (**Vitest 587 passed / 41 ficheros**) | `src/background/approvals/{queue,decisions,responses,timeout,reconcile,ports,focus,dispatch,preview,calldata}.ts` (M14..M19: cola `Record<approvalId, PendingRequest>` con `rmwLock` y cardinalidad, espera de decisión, `SIGN_RESPONSE`, plazo con `chrome.alarms`, reconciliación al arrancar, puerto `truekeate_approval` con `RESUME`, ventana global única, orquestación de los 6 aprobables y previews) · `src/background/crypto/sign.ts` (M11: EIP-1559 tipo 2, EIP-155, EIP-712 y `personal_sign`) · `src/background/rpc/{txContract,errors,router,catalog}.ts` (M7/M6/M3/M4: contrato observable de la transacción, `estimateGas` bloqueante, despacho de los aprobables) · `src/notification/{App.tsx,Notification.tsx,TxPreviewPanel.tsx,TypedDataPanel.tsx,PersonalSignPanel.tsx,RiskWarnings.tsx}` (M50..M54: ventana de decisión; **decide, no firma**) · `src/content-script.ts` (M38: relay con **correlación del salto 1** por `requestId` y transporte con espera acotada) · `src/shared/{constants,types,protocol}.ts` (M57/M55/M56: `TX_TYPE_EIP1559`, `SIGN_TIMEOUT_MS`, `PendingRequest.requestId`) · `contracts/src/EIP712Verifier.sol` + `contracts/test/EIP712Verifier.t.sol` + fixture. |
| **Specs de H4** | ✅ en verde | **Vitest (14 specs nuevos, 587 en total)**: `approvals/{queue,windowQueue,windowRediscovery,approvalTimeout,approvalReconcile,payloadLimit,dispatch}.spec.ts`, `crypto/{sign,eip1559,eip155,typedData,personalSign}.spec.ts`, `rpc/{txContract,errors}.spec.ts`. **Playwright (6 specs, 44 en total)**: `e2e/04-enviar`, `10-aprobar-tx`, `11-firmar-eip712`, `11-firmar-mensaje`, `18-concurrencia` y `29-sw-suspendido` — **44 passed / 0 failed / 0 skipped**. |
| `RepoTecnico/evidencia/H4/` | ✅ archivado (2026-09-12) | `typecheck-2026-09-12.log`, `build-2026-09-12.log`, `lint-prohibited-2026-09-12.log`, `vitest-2026-09-12.log`, `forge-2026-09-12.log`, `e2e-2026-09-12.{log,json}`, `e2e-global-setup-2026-09-12.{log,json}`, `04-enviar-2026-09-12.json`, `10-aprobar-tx-2026-09-12.{json,png}`, `11-firmar-eip712-2026-09-12.json`, `11-firmar-mensaje-2026-09-12.json`, `18-concurrencia-2026-09-12.json`, `29-sw-suspendido-2026-09-12.{json,log}`, `extremo-a-extremo-2026-09-12.{json,png}` y **`ACTA_H4.md`**. |
| **Documentación de la rúbrica (H6)** | ✅ creada y versionada (2026-09-12) | **`README.md`** (puesta en marcha en **4 pasos** autosuficiente: requisitos, `npm ci`, `npm run build`, carga de `dist/` en `chrome://extensions`; Anvil sin `--silent`; dApp en 5174; las 4 suites y la cobertura), **`INSTRUCCIONES.md`** (los **15 flujos y las 4 superficies** en español, con lo que debe verse en cada paso, los avisos de RNF-23 y el glosario de códigos de error), **`LICENSE`** (MIT, titular **ANLU corporations**, con el alcance que excluye marca y tipografías) y **`NOTICE`** (dependencias de ejecución y de desarrollo con versión/licencia/titular, contrato Forge y las **3 tipografías OFL-1.1**). Los tres `LICENSE-*.txt` de `public/fonts/` siguen versionados (RNF-23, `CA-RT-12`). |
| **Puerta documental (H6)** | ✅ existe y en verde | **`src/docs.spec.ts`** (M1..M66, RNF-01/RNF-13/RNF-14): audita mecánicamente que **cada fichero `.ts`/`.tsx` de `src/`** abra con bloque **JSDoc** con su identificador de módulo (`M1..M66`) y una referencia `RF/RNF/RT`. Cubre **145 ficheros** con **148 comprobaciones** (una por fichero + 3 de conjunto) y **completó 35 cabeceras** (34 con línea de referencia y `src/manifest.ts` con su bloque `//` convertido a JSDoc); **solo comentarios**. |
| **Cobertura de `approvals/` (H6, D-H6-A)** | ✅ existe y en verde | **`test/oracle/**` con 11 specs y 245 pruebas** (`calldata`, `preview`, `ports`, `responses`, `decisions`, `timeout`, `queue-coverage`, `reconcile-coverage`, `focus-coverage`, `dispatch-coverage`, `crypto-numeric`), añadidos **sin tocar `src/background/**`**: elevan `src/background/approvals/` de **64,62 %** a **95,21 %** de ramas. `vite.config.ts` amplía el universo de Vitest a `test/oracle/**/*.spec.ts` y declara los **umbrales de RNF-17**. |
| **Código de H6 (identidad y accesibilidad)** | ✅ existe y verificado (carga paralela) | `src/styles/{tokens,base}.css` y `src/styles/{contrast,goldUsage}.spec.ts` + `src/styles/cssTokens.ts` (matriz de contraste recalculada y oro solo decorativo), `src/shared/{i18n,format}.ts` con sus specs (`i18n.spec.ts`, `format.spec.ts`), `src/popup/components/AboutDialog.tsx` (RNF-23: aviso en «Acerca de», logotipo y tagline), `src/notification/{FirstSignatureNotice.tsx,firstSignature.ts}` (aviso antes de la primera firma) y los **4 specs E2E del hito**: `e2e/{17-i18n,23-marca,24-accesibilidad,26-avisos}.spec.ts`. |
| `RepoTecnico/evidencia/H6/` | ✅ archivado (2026-09-12) | `npm-ci-2026-09-12.log`, `tiempos-instalacion-2026-09-12.txt`, `build-windows-2026-09-12.log`, `typecheck-2026-09-12.log`, `vitest-2026-09-12.log`, `coverage-2026-09-12.log`, `docs-spec-2026-09-12.log`, `forge-2026-09-12.log`, `check-mermaid-2026-09-12.log`, `lint-prohibited-2026-09-12.log`, `grep-http-dist-2026-09-12.log`, `git-ls-files-2026-09-12.log`, `wsl2-no-disponible-2026-09-12.log`, `auditoria-rf-evidencia-2026-09-12.log`, `e2e-2026-09-12.{log,json}`, `e2e-intento1-contaminado-2026-09-12.{log,json}`, `e2e-cierre-2026-09-12.{log,json}`, los JSON de los flujos del hito (`17-i18n`, `23-marca`, `24-accesibilidad`, `26-avisos` y los de H1–H5 regenerados), `instalacion-2026-09-12.md` y **`ACTA_H6.md`**. |

---

## 3. Entorno verificado (resumen)

- Windows · Node `v24.16.0` · npm `11.13.0`.
- Foundry `anvil`/`forge`/`cast` **1.7.2-dev** instalados (`C:\Users\lucci\.cargo\bin`); rango soportado `>=1.0.0 <2.0.0` (`entornos_globales.md` §8).
- Repositorio git **inicializado**: `main` (commit raíz con la documentación) y rama de trabajo `chrome-wallet-DSH`.
- Remotos configurados: `origin` (GitHub, **no existe**), `gitlab` (GitLab.com, **no existe**), `codecrypto` (GitLab ANLU, **existe con el código previo, solo como referencia**).
- **El proyecto se reconstruye desde cero (DEC-09):** el código del remoto `codecrypto` **no se adopta, no se reutiliza y no se versiona**; `src/` **se creó en H1** (2026-09-11) y el remoto solo se consulta. No hay ninguna vía de reutilización de ese código en este plan.
- RPC local en `127.0.0.1:8545` **detenido** (se levanta con `anvil` al probar, con allowlist de CORS).
- Sin `gh`/`glab` ni tokens; GCP fuera de alcance.
- Identidad visual **TrueKeate** incorporada: 6 activos originales en `TrueKeate/`, 4 iconos generados en `public/icons/` (versionados, no se regeneran en Linux) y 6 activos de marca en `public/brand/`.
- **Tipografías auto-hospedadas (H1/DEC-50):** `Poppins` en **tres** pesos discretos (`poppins-latin-400/600/700.woff2`), `Inter` y `JetBrains Mono` **variables** (`inter-latin.woff2`, `jetbrains-mono-latin.woff2`) y las licencias **OFL-1.1** en `public/fonts/LICENSE-{poppins,inter,jetbrains-mono}.txt`.
- **ID de extensión estable (H1):** **`oiahebaliobknoeeonhgaacapjcpgblo`**, derivado de la `key` fija del manifest (DEC-43) y verificado en los E2E; es el `<ID>` que consume la allowlist CORS de Anvil (RE-04).
- **`dist/` cargable (H1):** 6 entradas (`index.html`, `connect.html`, `notification.html`, `background.js`, `content-script.js`, `inject.js`) + `dist/manifest.json`, generado por un solo `npm run build`.

### 3.1 Comandos verificados en H1 (2026-09-11)

| Comando | Resultado real | Evidencia |
|---|---|---|
| `npm run build` | **exit 0** — 6 entradas en `dist/` + `dist/manifest.json` | `evidencia/H1/build-2026-09-11.log`, `manifest-2026-09-11.json` |
| `npx tsc -b` | **exit 0** (strict, 0 errores) | `evidencia/H1/typecheck-2026-09-11.log` |
| `npm run test` (Vitest) | **47 passed / 4 ficheros** | `evidencia/H1/vitest-2026-09-11.log` |
| `npm run test:e2e` (Playwright) | **4 passed** (`01-onboarding.spec.ts`) | `evidencia/H1/playwright-2026-09-11.log`, `e2e-2026-09-11.json` |
| `npm run lint:prohibited` | **exit 0** (0 hallazgos; 0 URLs de CDN en `dist/`) | `evidencia/H1/lint-prohibited-2026-09-11.log` |
| `forge test --root contracts --match-contract EIP712VerifierTest` | **7 passed, 0 failed, 0 skipped** | `evidencia/H1/forge-eip712verifier-contracts-2026-09-11.log` |
| `npm run check:mermaid` | **27/27 bloques válidos** | `evidencia/H1/check-mermaid-2026-09-11.log` |

> **Regla vinculante (DEC-51):** el comando normativo de Foundry es **`forge test --root contracts --match-contract EIP712VerifierTest`** (`npm run forge:test`). El literal **sin `--root contracts`** responde «Nothing to compile», ejecuta **0 pruebas** y sale con **exit 0**: es un **falso verde** y queda prohibido en el corpus.

### 3.2 Comandos verificados en H2 (2026-09-11)

Ejecutados con **Anvil en `127.0.0.1:8545` (chainId 31337)** y `dist/` reconstruido por el `global-setup` del arnés. Evidencia completa en `RepoTecnico/evidencia/H2/` y acta `ACTA_H2.md`:

| Comando | Resultado real | Evidencia |
|---|---|---|
| `npx tsc -b` | **exit 0** — strict, 0 errores (incluye `src/`, `test/` y `e2e/`) | `evidencia/H2/typecheck-2026-09-11.log` |
| `npm run build` | **exit 0** — 6 entradas en `dist/` + `dist/manifest.json` | `evidencia/H2/build-2026-09-11.log` |
| `npm run lint:prohibited` | **exit 0** — 0 hallazgos (73 ficheros de `src/` y 13 de `dist/`) | `evidencia/H2/lint-prohibited-2026-09-11.log` |
| `npm run test` (Vitest) | **exit 0** — **290 passed / 18 ficheros**, 0 failed | `evidencia/H2/vitest-2026-09-11.log` |
| `npm run coverage` | **exit 0** — `crypto/` **95,13 %** de líneas (488/513) y `shared/validation/` **97,27 %** (214/220); umbral ≥ 80 % **cumplido** | `evidencia/H2/coverage-2026-09-11.json`, `vitest-coverage-2026-09-11.log` |
| `npm run test:e2e` (Playwright) | **exit 0** — **23 passed / 0 failed / 0 skipped / 0 flaky** | `evidencia/H2/playwright-2026-09-11.log`, `e2e-2026-09-11.json` |
| `rg -n "chrome\.storage" src/popup` | **0 coincidencias** (RNF-14) | `evidencia/H2/_verificacion-resumen.log` |
| `rg -n "from 'ethers'" src/popup` | **0 coincidencias** (`CA-RT-02`) | `evidencia/H2/_verificacion-resumen.log` |

> **Nota de host:** en este equipo Windows no hay `grep`; se usa **ripgrep** (`rg`), el mismo motor de búsqueda, que devuelve **exit 1 = 0 coincidencias**.

### 3.3 Comandos verificados en H3 (2026-09-11)

Ejecutados con **Anvil en `127.0.0.1:8545` (chainId 31337)** y la dApp **autoservida** por el `webServer` de `playwright.config.ts` (D-H3-D). Evidencia completa en `RepoTecnico/evidencia/H3/` y acta `ACTA_H3.md`:

| Comando | Resultado real | Evidencia |
|---|---|---|
| `npx tsc -b` | **exit 0** — strict, 0 errores (incluye `src/`, `test/` y `e2e/`) | `evidencia/H3/` (batería de cierre) |
| `npm run build` | **exit 0** — 6 entradas en `dist/` + `dist/manifest.json` | `evidencia/H3/` (batería de cierre) |
| `npm run lint:prohibited` | **exit 0** — 0 hallazgos | `evidencia/H3/lint-prohibited-2026-09-11.log` |
| `npm run test` (Vitest) | **exit 0** — **403 passed / 28 ficheros**, 0 failed | `evidencia/H3/vitest-2026-09-11.log` |
| `npm run test:e2e` (Playwright) | **exit 0** — **37 passed / 0 failed / 0 skipped / 0 flaky** (los 23 de H1/H2 siguen verdes) | `evidencia/H3/e2e-2026-09-11.{log,json}`, `e2e-global-setup-2026-09-11.{log,json}` |
| `rg -n "chrome\.storage" src/popup src/connect` | **0 coincidencias** (RNF-14: ni el popup ni `connect.html` leen el almacén) | `evidencia/H3/` (verificación de cierre) |
| `rg -n "from 'ethers'" src/popup src/connect` | **0 coincidencias** (`CA-RT-02`) | `evidencia/H3/` (verificación de cierre) |
| `cast chain-id --rpc-url http://127.0.0.1:8545` | **31337** | `evidencia/H3/cast-2026-09-11.log` |
| `cast block-number --rpc-url …` | **≥ 1** (el spec `07` mina 2 bloques para la cota `≥ 1` de `CA-RF-18`) | `evidencia/H3/cast-2026-09-11.log` |
| `cast balance 0xf39F…2266 --rpc-url …` | **10 000 ETH** (`10000000000000000000000` wei) | `evidencia/H3/cast-2026-09-11.log` |

> **Lección del hito (D-H3-D):** la suite **no** depende de que alguien arranque la dApp a mano; `playwright.config.ts` la arranca y la apaga (`webServer`). **Anvil, en cambio, sigue siendo una dependencia externa**: se levanta antes de la suite con `anvil --host 127.0.0.1 --port 8545 --chain-id 31337 --allow-origin "*"` **sin `--silent`** (`entornos_globales.md` **v2.2** §2.1; la bandera `--http.corsdomain` de la v2.1 **no existe** en Anvil 1.7.2-dev).

### 3.4 Comandos verificados en H4 (2026-09-12) — **hito COMPLETADO**

Ejecutados con **Anvil 1.7.2-dev en `127.0.0.1:8545` (chainId 31337)** y la dApp **autoservida** por el `webServer`. Evidencia completa en `RepoTecnico/evidencia/H4/` y acta `ACTA_H4.md`:

| Comando | Resultado real | Evidencia |
|---|---|---|
| `npx tsc -b` | **exit 0** — strict, 0 errores (incluye `src/`, `test/` y `e2e/`) | `evidencia/H4/typecheck-2026-09-12.log` |
| `npm run build` | **exit 0** — 6 entradas en `dist/` + `dist/manifest.json` | `evidencia/H4/build-2026-09-12.log` |
| `npm run lint:prohibited` | **exit 0** — 127 ficheros de `src/`, 0 hallazgos | `evidencia/H4/lint-prohibited-2026-09-12.log` |
| `npm run test` (Vitest) | **exit 0** — **587 passed / 41 ficheros**, 0 failed (los 403 de H1–H3 intactos) | `evidencia/H4/vitest-2026-09-12.log` |
| `npm run test:e2e` (Playwright) | **exit 0** — **44 passed / 0 failed / 0 skipped** (los 37 de H1–H3 siguen verdes; `18-concurrencia` y `29-sw-suspendido` pasan tras cerrar **D-H4-E9**/**D-H4-E10** y los dos defectos del re-render) | `evidencia/H4/e2e-2026-09-12.{log,json}`, `e2e-global-setup-2026-09-12.{log,json}` |
| `npm run forge:test` | **exit 0** — **10 passed / 0 failed** | `evidencia/H4/forge-2026-09-12.log` |
| Prueba real de extremo a extremo (`node RepoTecnico/evidencia/H4/prueba-extremo-a-extremo.mjs`) | **OK** — `test.html` envía 1 ETH → **UNA** ventana con preview decodificada y contador `1 solicitud en espera` → aprobar → la dApp recibe el hash `0x21f97614e3117d6e9083ba46b8b2be20c428008e5f6801adcc19fc00741833a6` → recibo **`status 0x1`** → cola vacía y `truekeate_inflight_tx` en `phase: 'broadcast'` (cuenta liberada) | `evidencia/H4/extremo-a-extremo-2026-09-12.{json,png}` |
| Suspensión REAL del SW por CDP (`e2e/29-sw-suspendido.spec.ts`) | **OK** — la alarma despierta al SW, la cola se purga sin huérfanas, la dApp recibe **`4001`** («plazo establecido»), `nonce` sin avanzar (0 doble difusión) y reconstrucción **< 1 s** (RNF-08) | `evidencia/H4/29-sw-suspendido-2026-09-12.{json,log}` |
| Concurrencia de 2 orígenes (`e2e/18-concurrencia.spec.ts`) | **OK** — 2 `pending` coexistentes en UNA sola ventana con contador «2 solicitudes en espera» y las dos firmas obtenidas de esa MISMA ventana | `evidencia/H4/18-concurrencia-2026-09-12.json` |

> **Comando del nodo (vinculante):** `anvil --host 127.0.0.1 --port 8545 --chain-id 31337 --allow-origin "*"` **sin `--silent`** (`entornos_globales.md` **v2.2** §2.1). **Servidor de la dApp:** `vite.config.ts` declara `server: { host: '127.0.0.1', port: 5174, strictPort: true }` (**D-H4-E9**), de modo que `http://127.0.0.1:5174/test.html` responde y `18-concurrencia` puede usar sus dos orígenes.

---

### 3.5 Comandos verificados en H5 (2026-09-12) — **hito COMPLETADO**

Ejecutados con **Anvil principal 1.7.2-dev en `127.0.0.1:8545` (chainId 31337)** y el **Anvil secundario en `127.0.0.1:8546` (chainId 31338)** —ambos comprobados por el `globalSetup`, que arranca el segundo si falta— y la dApp autoservida por el `webServer`. Evidencia completa en `RepoTecnico/evidencia/H5/` y acta `ACTA_H5.md`:

| Comando | Resultado real | Evidencia |
|---|---|---|
| `npx tsc -b` | **exit 0** — strict, 0 errores (incluye `src/`, `test/` y `e2e/`); los **5** `TS18048` de los specs de `logging/` corregidos | `evidencia/H5/typecheck-2026-09-12.log` (0 bytes) |
| `npm run build` | **exit 0** — 6 entradas en `dist/` + `dist/manifest.json` | `evidencia/H5/build-2026-09-12.log` |
| `npm run lint:prohibited` | **exit 0** — 140 ficheros de `src/`, 14 de `dist/`, 0 hallazgos | `evidencia/H5/lint-prohibited-2026-09-12.log` |
| `npm run test` (Vitest) | **exit 0** — **636 passed / 47 ficheros**, 0 failed (los 587 de H1–H4 intactos; +49 del hito) | `evidencia/H5/vitest-2026-09-12.log` |
| `npm run test:e2e` (Playwright) | **exit 0** — **58 passed / 0 failed / 0 skipped** (los 44 de H1–H4 siguen verdes; +14: `12-redes` 5, `15-logs` 3, `21-eip6963` 1, `22-dapp` 5) | `evidencia/H5/e2e-2026-09-12.{log,json}`, `e2e-global-setup-2026-09-12.{log,json}` |
| `npm run forge:test` | **exit 0** — **10 passed / 0 failed** | `evidencia/H5/forge-2026-09-12.log` |
| Nodo principal / **nodo secundario** | **OK** — `cast chain-id` = **31337** y **31338** | `evidencia/H5/e2e-global-setup-2026-09-12.{log,json}` |
| Cambio de red aprobado (`e2e/12-redes.spec.ts`) | **OK** — alta sin activación, cambio posterior aprobado con `truekeate_chain_id` = `0x7a6a` y `chainChanged` en **2** pestañas; denegación del permiso de host ⇒ **`4001`** sin persistir | `evidencia/H5/12-redes-2026-09-12.json` |
| Registro y exportación (`e2e/15-logs.spec.ts`) | **OK** — 1 entrada por llamada con `method`/`origin`/`ts` y el popup **cerrado**; errores en rojo con su `code`; exportación JSON por `Blob` **sin ninguna petición de red** | `evidencia/H5/15-logs-2026-09-12.json`, `15-logs-sw-2026-09-12.json`, `logs-export-2026-09-12.json` |
| dApp de pruebas (`e2e/22-dapp.spec.ts`, `e2e/21-eip6963.spec.ts`) | **OK** — los **7 flujos** con resultado en pantalla medido en **< 5000 ms**, errores `4001`/`4900`/`4901` con su acción sugerida y anuncio EIP-6963 con identidad literal | `evidencia/H5/22-dapp-flujos-1-4-2026-09-12.json`, `22-dapp-flujos-5-7-2026-09-12.json`, `22-dapp-4900-2026-09-12.json`, `21-eip6963-2026-09-12.json` |

> **Comando del nodo secundario (verificado):** `anvil --host 127.0.0.1 --port 8546 --chain-id 31338 --allow-origin "*"` **sin `--silent`**; el `globalSetup` lo arranca si no responde y lo registra en la evidencia. Los **defectos de producto** corregidos en H5 (`D-H5-A`..`D-H5-E`) y los **del arnés** (`D-H5-H`..`D-H5-M`) están en `plan_desarrollo.md` §3.5.10 y en `ACTA_H5.md` §3/§3.b, con `D-H5-N` (alta nacida en una dApp) **reportado** y `D-H5-O` (alta de red nueva) declarado **NO VERIFICADO** en `E2E_HEADLESS`.

---

### 3.6 Comandos verificados en H6 (2026-09-12) — **hito COMPLETADO y Fase 3 cerrada**

Ejecutados **desde una instalación limpia** (`npm ci`, 33 s) sobre **Windows**, Node `v24.16.0`, npm `11.13.0`, Foundry `1.7.2-dev`, con **Anvil principal en `127.0.0.1:8545` (chainId 31337)**, el **secundario en 8546 (31338)** y la dApp autoservida por el `webServer`. Evidencia completa en `RepoTecnico/evidencia/H6/` y acta `ACTA_H6.md`:

| Comando | Resultado real | Evidencia |
|---|---|---|
| `npm ci` | **exit 0** — instalación reproducible desde `package-lock.json`, **33 s** | `evidencia/H6/npm-ci-2026-09-12.log`, `tiempos-instalacion-2026-09-12.txt` |
| `npm run build` | **exit 0** — `✓ built in 7,93s`, **6 entradas** + `dist/manifest.json` (v1.0.0), **22 s** | `evidencia/H6/build-windows-2026-09-12.log` |
| `npx tsc -b` | **exit 0** — `strict: true`, 0 errores (`src/`, `test/`, `test/oracle/` y `e2e/`) | `evidencia/H6/typecheck-2026-09-12.log` (0 bytes) |
| `npm run test` (Vitest) | **exit 0** — **63 ficheros / 1076 pruebas, todas en verde** (los 636 de H1–H5 intactos; **+245** de `test/oracle/**` y **+148** del gate documental) | `evidencia/H6/vitest-2026-09-12.log` |
| `npm run coverage` | **exit 0** — **umbrales de RNF-17 declarados en configuración y CUMPLIDOS**: ramas globales **86,13 %** (3192/3706); `crypto/` **86,38 %** (279/323); `approvals/` **95,21 %** (1233/1295); `shared/validation/` **91,09 %** (92/101) | `evidencia/H6/coverage-2026-09-12.log`, `coverage/coverage-summary.json` |
| `npx vitest run src/docs.spec.ts` | **exit 0** — **148 comprobaciones sobre 145 ficheros** de `src/` (JSDoc de contrato con `M1..M66` y referencia `RF/RNF/RT`) | `evidencia/H6/docs-spec-2026-09-12.log` |
| `npm run forge:test` | **exit 0** — **10 passed / 0 failed / 0 skipped** | `evidencia/H6/forge-2026-09-12.log` |
| `npm run check:mermaid` | **exit 0** — **27/27 bloques válidos** | `evidencia/H6/check-mermaid-2026-09-12.log` |
| `npm run lint:prohibited` | **exit 0** — **0 hallazgos** (150 ficheros de `src/` y 14 de `dist/`); la comprobación de `dist/` da **0 hosts de CDN** y 0 recursos remotos. Los literales de color de los specs de contraste que salían en la primera pasada se eliminaron en el mismo turno (**D-H6-B cerrada**) | `evidencia/H6/lint-prohibited-2026-09-12.log` |
| `grep -rn "http" dist/` | **34 coincidencias** en 41 ficheros, **ninguna de CDN** (`host_permissions` locales, *namespaces* XML de los SVG, URL de la OFL en los `LICENSE-*.txt` y subcadenas minificadas) → **0 hosts de CDN** (`D-H6-G`) | `evidencia/H6/grep-http-dist-2026-09-12.log` |
| `git ls-files` | `LICENSE`, `NOTICE`, `README.md`, `INSTRUCCIONES.md`, `src/docs.spec.ts` y los 3 `public/fonts/LICENSE-*.txt` **versionados** (RNF-23, `CA-RT-12`) | `evidencia/H6/git-ls-files-2026-09-12.log` |
| `wsl -l -v` (RNF-15) | **NO EJECUTABLE** — «Subsistema de Windows para Linux no tiene distribuciones instaladas» → **RNF-15 verificado solo en Windows; pendiente en Linux** (`D-H6-D`) | `evidencia/H6/wsl2-no-disponible-2026-09-12.log` |
| Auditoría `RF Must → evidencia` | **40/40 Must con evidencia**; 2 citas con nombre de fichero desactualizado en `requerimientos.md` §9.2 (`D-H6-C`) | `evidencia/H6/auditoria-rf-evidencia-2026-09-12.log` |
| `npm run test:e2e` (Playwright) | **exit 0** — **74 passed / 0 failed / 0 skipped** en **ventana limpia** (269,4 s). La primera pasada quedó contaminada por **concurrencia** con la carga paralela (dos suites sobre el mismo `dist/` y `test-results/`: **68 passed / 8 failed**, con dos ficheros sonda borrados a mitad de ejecución; `D-H6-I`) y se conserva como evidencia | `evidencia/H6/e2e-cierre-2026-09-12.{log,json}`, `e2e-intento1-contaminado-2026-09-12.{log,json}` |

> **Ensayo de instalación (tarea 6.10):** dry-run cronometrado del camino crítico **55 s** (`npm ci` 33 s + `build` 22 s), **0 consultas al autor** y **0 ambigüedades bloqueantes**; el ensayo **independiente** del Perfil B queda **NO VERIFICADO por sujeto externo** (`D-H6-E`). Detalle en `evidencia/H6/instalacion-2026-09-12.md`.

---

### 3.7 Fase 4 — Pruebas ✅ **COMPLETADA (2026-09-13)**

Los **tres frentes** se ejecutaron en **ventana limpia** (una suite pesada a la vez, **DEC-87**/`D-H6-I`) sobre **Windows**, con **Anvil principal en `127.0.0.1:8545` (chainId 31337)** —`--allow-origin "*"`, **sin `--silent`**—, el **secundario en 8546 (31338)** arrancado por el `globalSetup` del arnés y la dApp autoservida por el `webServer`. Evidencia completa en `RepoTecnico/evidencia/Fase4/` e **informe consolidado en `RepoTecnico/INFORME_PRUEBAS_FASE4.md`**:

| Comando | Resultado real | Evidencia |
|---|---|---|
| `npm run test` (Vitest) | **exit 0** — **71 ficheros / 1185 pruebas, todas en verde**, **0 failed** (los 1154 de la pasada de auditoría siguen verdes; **+31** de esta pasada, sin `skip`/`fixme` ni aserciones relajadas) | `evidencia/Fase4/coverage-fase4-cierre.log` |
| `npm run coverage` | **exit 0** — umbrales de RNF-17 **CUMPLIDOS**: ramas globales **86,11 %**; `crypto/` **86,98 %**; `approvals/` **95,60 %**; `shared/validation/` **93,13 %** (bajada de la global declarada y explicada: código defensivo nuevo) | `evidencia/Fase4/coverage-fase4-cierre.log`, `coverage/coverage-summary.json` |
| `npm run test:e2e` (Playwright) | **exit 0** — **84 passed / 0 failed / 0 flaky / 0 skipped**, **33 specs**, ~6,7 min, **ventana limpia** (los **74** E2E de H1..H6 siguen verdes; **+10** specs nuevos de la fase) | `evidencia/Fase4/e2e-cierre-fase4.log`, `e2e-2026-09-13.json` |
| `forge test --root contracts` | **exit 0** — **30 passed / 0 failed / 0 skipped** en **2 suites**: `EIP712VerifierTest` (**10**, los 5 de `CA-RT-11` + H4) y `EIP712VerifierFase4Test` (**20**, batería adversaria) | `evidencia/Fase4/forge-2026-09-12.log` |
| `npx tsc -b` | **exit 0** — `strict: true`, 0 errores | salida de la puerta de cierre |
| `npm run build` | **exit 0** — 6 entradas + `dist/manifest.json` | salida de la puerta de cierre |
| `npm run lint:prohibited` | **exit 0** — **0 hallazgos** | salida de la puerta de cierre |

**Defectos corregidos:** **15** (12 de unitarias, 1 de E2E más 1 del arnés, y 2 documentales del contrato) **más 4** de concurrencia y retención medidos al cerrar los flecos, todos con **síntoma, causa raíz con `archivo:línea`, solución y prueba** en `INFORME_PRUEBAS_FASE4.md` §4. El más grave fue el **hueco no-cadena en `truekeate_accounts`**: al compactar la lista, `wallet_revealSecret('idx:2')` entregaba la **clave privada de OTRA cuenta** (fallo **cerrado** sin compactar, con el informe de integridad marcando la cartera como dañada).

**Flecos cerrados en la misma pasada (sin decisión de producto):** (1) **ventana de tasa alineada con `plan_desarrollo.md` §3.13** —6 solicitudes/60 s—, eliminando las constantes muertas `rateLimitBurst`/`rateLimitRefillPerSecond` y **sin debilitar** ninguna defensa; (2) `validateAmount` devuelve el **error tipado** `-32602 invalidAmount` de §4.3.1; (3) el **rechazo del nodo** en `eth_estimateGas` (`-32003`) ya **no** se confunde con «sin conexión»: se registra en `diccionario_datos.md` **§4.3.4**, no se reintenta y la UI **no** pasa a «desconectado»; (4) **RMW serializado** en `sessions.ts` y `connections.ts` (cerrojo reutilizable `src/background/state/serialLock.ts`) y **purga** de `truekeate_connect_request` por vencimiento y al resolver.

**Declarado y NO cumplido (heredado de la Fase 3, sin cambio):** **RNF-15** sigue **verificado solo en Windows; pendiente en Linux** (`D-H6-D`) y el **ensayo del Perfil B** sigue **no verificado por sujeto externo** (`D-H6-E`).

---

## 4. Decisiones tomadas

| # | Decisión | Motivo |
|---|---|---|
| DEC-01 | Renumerar los requerimientos como `RF-01..RF-50` usando `requerimientos.md` como **fuente única**. | El enunciado repite los números 20, 26 y 29 (D-01); RF-48/RF-49 son aportaciones de identidad visual (**más RF-50 en DEC-28**) y quedan dentro del rango oficial (H-03). |
| DEC-02 | Adoptar **Foundry Anvil** como red por defecto y **única**. | Petición explícita del usuario (D-02). |
| DEC-03 | Añadir la **importación por clave privada** (RF-05/RF-06) y la **recepción de fondos con QR/copiar dirección** (RF-07). | Petición explícita del usuario (D-03/D-04). |
| DEC-04 | Mantener la arquitectura del enunciado: **React = solo UI**, **Service Worker = criptografía y RPC**. | Ya está validada y refuerza RNF-09/RNF-14. |
| DEC-05 | Incluir desde el diseño la **cola de aprobaciones persistida** y los **timeouts** (RF-37/RF-40/RNF-08). | El Service Worker MV3 se duerme y pierde el estado en memoria. **Rediseñada en DEC-24** (H-02/H-07/H-08). |
| DEC-06 | Confirmar `personal_sign` (RF-21) y `wallet_revokePermissions` (RF-26) dentro del alcance. | Respuestas **P-05** y **P-06** del usuario. |
| DEC-07 | **Retirar Sepolia** del alcance y de `host_permissions`. | Decisión **P-02**. |
| DEC-08 | Mantener la **carga sin contraseña** del mnemonic, documentándolo como modo desarrollo. | Decisión **P-03**. |
| DEC-09 | **Reconstruir el proyecto desde cero**: el código del remoto `codecrypto` es solo **referencia** y no se reutiliza ni se versiona en el nuevo proyecto. | Decisión **P-10** (bloqueante, resuelta). |
| DEC-10 | Registrar **3 remotos** (`origin`, `gitlab`, `codecrypto`) pero **no hacer push** hasta orden explícita (`/push`, RE-03). | Regla del proceso + remotos de GitHub/GitLab.com aún inexistentes. |
| DEC-11 | **5 cuentas HD por defecto + botón "Añadir cuenta"** para derivar la siguiente. | Decisión **P-04**. |
| DEC-12 | **Vitest + Playwright + contrato verificador EIP-712 con Forge** como stack de pruebas. | Decisión **P-07**. |
| DEC-13 | **100 % local, sin GCP.** | Decisión **P-08**. |
| DEC-14 | **UI y documentación en español; identificadores de código en inglés.** | Decisión **P-09**. |
| DEC-15 | Adoptar la marca **TrueKeate** entregada en `TrueKeate/` como identidad visual del producto, con el documento vinculante `identidad_visual.md`. | Petición explícita del usuario en Fase 1. |
| DEC-16 | La paleta se obtuvo por **medición de píxeles** de los activos originales (no por estimación visual) y se congeló en tokens CSS. | RNF-18 exige que no haya colores fuera de los tokens. |
| DEC-17 | Los iconos de 16/32 px usan una **variante simplificada** (zoom a las flechas + saturación) y los de 48/128 el isologo completo. | El isologo completo se emborrona por debajo de 48 px. |
| DEC-18 | **Renombrar todo el producto a TrueKeate**, incluido el provider inyectado (`window.truekeate`), el prefijo de storage (`truekeate_`) y los tipos de mensaje. | Decisión **P-13** (desviación consciente del literal E-03 del enunciado). El alias de compatibilidad se cierra en **DEC-21**. |
| DEC-19 | Aprobar el sistema de diseño de `identidad_visual.md` tal cual: paleta medida de los activos, Poppins + Inter + JetBrains Mono auto-hospedadas, ventanas de 380×600 / 420×650 / 420×640. | Decisión **P-14**. |
| DEC-20 | Iconos de 16/32 px con **variante simplificada** (zoom a las flechas + saturación); 48/128 con el isologo completo. | Decisión **P-15**. |
| DEC-21 | El provider inyectado expone **`window.truekeate`** **y además el alias `window.codecrypto = window.truekeate`** (el **mismo objeto**), con un **test que verifica ambos nombres**. | Cierra **H-15** y protege los **5 puntos de la rúbrica** ligados al nombre literal del enunciado. Decisión firme del usuario (sustituye a «si el evaluador lo exige»). |
| DEC-22 | **`eth_sign` retirado del catálogo RPC**: se responde `4200` (Unsupported method) y solo se admite **`personal_sign`**. | Cierra **H-11**: `eth_sign` firma un digest de 32 bytes sin interpretación, el enunciado no lo exige y P-05 confirma solo `personal_sign`. |
| DEC-23 | **Vista previa con decodificación completa del calldata y avisos de riesgo**: selector y nombre de función, parámetros legibles, contrato destino etiquetado y aviso en `approve`/`setApprovalForAll`/valor ilimitado; en EIP-712 se muestran `verifyingContract` y `name` (con aviso si no coinciden con lo declarado); en `personal_sign` se previsualiza el **texto UTF-8** (aviso si el payload es hexadecimal ilegible). | Cierra **H-11**: el usuario debe ver qué autoriza realmente antes de firmar. |
| DEC-24 | **Rediseño del ciclo de aprobación MV3**: puerto de larga vida (`chrome.runtime.connect` con reconexión y backoff), **`chrome.alarms`** en los permisos, **cola persistida `Record<approvalId, PendingRequest>`** con *read-modify-write* serializado, **reconciliación al arrancar** el SW (marca `expired` y responde `4001` a los huérfanos) y **SW dueño único del plazo** (`SIGN_TIMEOUT_MS` 120 s / `CONNECT_TIMEOUT_MS` 60 s anclados a `createdAt`); al expirar cierra la ventana, marca `expired` y purga el badge. | Cierra **H-02, H-07 y H-08**; completa DEC-05. |
| DEC-25 | Los **50 RF y los 13 RT** llevan columna de **criterio de aceptación (verificable)** y **evidencia**; los criterios Gherkin viven en **`requerimientos.md` §9 (anexo)** y en los casos de uso. | Cierra **H-01**: sin oráculo por requisito no se puede derivar el test. La redacción dentro de `requerimientos.md` corresponde al analista. |
| DEC-26 | **MVP = los 40 RF Must** (incluye **RF-50**, DEC-28); los **10 RF Should** se planifican en un **ciclo posterior**. La estimación «~40 h» se sustituye por **estimación por hitos** en `plan_desarrollo.md`. | Cierra **H-34** y hace explícito qué cae si se agota el presupuesto (§9). |
| DEC-27 | **El MVP no depende del badge (P-17):** **RF-38 (badge)** y **RF-39 (notificaciones)** permanecen en el **ciclo posterior** y **no se promociona ninguno**; el oráculo del caso central de la cola de aprobaciones (CU-16) pasa a ser «**2 entradas `pending` en `truekeate_pending_requests` + 1 transacción en vuelo por cuenta**». | Cierra **ACU-07**; el alcance comprometido (§9) deja de depender de dos RF Should. |
| DEC-28 | Se añade **RF-50 (Must)**: **revelar y exportar** la frase semilla (BIP-39) y las claves privadas de las cuentas **bajo confirmación explícita** del usuario (advertencia de riesgo, valores ocultos por defecto, revelado temporal de **30 s** y prohibición de exponerlos por `window.postMessage`), con **`CA-RF-50`** en Gherkin y EARS y desviación **D-13**. | Cierra **ACU-14** (**P-18**): sin revelado/exportación, las cuentas importadas por clave privada son irrecuperables (H-25/RNF-22). |
| DEC-29 | **`wallet_switchEthereumChain` exige aprobación del usuario cuando la red destino no es la activa**: crea una solicitud en `truekeate_pending_requests` que se resuelve en `notification.html`; si **ya es la red activa**, responde **sin cambios ni ventana**. | Cierra **ACU-16** (**P-19**); se alinea en RF-22, `CA-RF-22` y el catálogo RPC del diccionario de datos. |
| DEC-30 | **Fuente de verdad de EIP-6963 = RT-13**: `name: "TrueKeate"` y `rdns: "academy.codecrypto.truekeate"`; el diccionario de datos y los casos de uso se alinean a esos literales. | Cierra **ACU-03** (**D-A**); elimina la divergencia con el diccionario §4.1.1. |
| DEC-31 | La **caducidad de la sesión de dApp** (`expiresAt = lastUsedAt + 86400000`, **24 h renovables en cada uso**) forma parte de **RF-25**, con criterio de aceptación y evidencia propios. | Cierra **ACU-17** (**D-B**); `lastUsedAt`/`expiresAt` y `sessionTtlMs` dejan de ser entidades sin requisito. |
| DEC-32 | El literal de «build limpio» es **`npm ci && npm run build`** en todo el corpus (RNF-15, RNF-24 y §2.6), en sustitución de `npm install`. | Cierra **ACU-26** (**D-C**). |
| DEC-33 | `truekeate_logs` incorpora el campo **`event`** (enum de los **24 eventos** del catálogo de §2.2) **separado de `category`** (5 valores); se corrigen los CU que usaban categorías como eventos. | Cierra **ACU-04** (**D-D**). |
| DEC-34 | La **tabla cerrada de errores EIP-1193 (§2.1)** admite **varios mensajes por código** (uno por causa) y **todo error mostrado al usuario lleva `code`**. | Cierra **ACU-05** (**D-E**). |
| DEC-35 | Se define **`accountLabels: Record<indice, string>`** en `truekeate_settings` para etiquetar las **cuentas derivadas** (además del `label` de las importadas). | Cierra **ACU-06** (**D-F**). |
| DEC-36 | El **alta de red siempre solicita el permiso de host en runtime** (`chrome.permissions.request`), **también desde el popup**: no hay excepción por el origen de la UI. | Cierra **ACU-27** (**D-G**); coherente con RT-04 y `entornos_globales.md` §4. |
| DEC-37 | **Política de portapapeles de la semilla y plazo de revelado (P-20):** copiar la frase semilla **está permitido** mientras el valor está revelado; el revelado dura **30 s** y se oculta también **por pérdida de foco**; al ocultarse, la extensión **borra el portapapeles** si aún contiene la semilla, con **test E2E que lo comprueba**. | Cierra **ADT-09**, **ADT-06** y **P-20**; se aplica en RF-50, `CA-RF-50` y RNF-09 (higiene del revelado). Retira cualquier mención a 60 s en el revelado. |
| DEC-38 | **Una sola ventana global de confirmación (P-21):** existe **como máximo una** `notification.html`; el resto de solicitudes **esperan en la cola** y la ventana muestra el **contador de pendientes** (nunca dos solicitudes a la vez). Sustituye la invariante «una ventana por origen». | Cierra **ADT-22** y **ADT-14** (**P-21**); se aplica en RF-35, `CA-RF-35` y RNF-05. |
| DEC-39 | **El alta de red no activa la red (P-22):** `wallet_addEthereumChain` **solo añade** la red y la deja inactiva; usar la red nueva exige su propio `wallet_switchEthereumChain` con aprobación del usuario. | Cierra **ADT-25** (**P-22**); coherente con RF-22/`CA-RF-22`/P-19 y DEC-29. |
| DEC-40 | **El origen de una petición se deriva solo de `sender.origin`**; con **`frameId !== 0`** queda prohibido el respaldo a `sender.tab.url` (un iframe hostil no hereda la sesión del sitio anfitrión) y la respuesta se envía solo a ese frame. | Cierra **ADT-07** (**D-J**); se aplica a RNF-10/RNF-11 y al ciclo de aprobación. |
| DEC-41 | **Decodificación del calldata con tabla local cerrada de selectores**, sin servicios externos de firmas (RT-03); fuera de la tabla, `functionName = null` con aviso bloqueante. **`verifyingContractMismatch`** pasa a significar **dirección cero o contrato no desplegado**. | Cierra **ADT-08** (**D-K**); se aplica a RF-19/RF-20 y a DEC-23. |
| DEC-42 | **Cota de payload de 64 KiB** (por encima, `-32602`) con previews largas redactadas en reposo, y **cuota objetivo de `chrome.storage.local` de 10 MB** (mínimo exigido Chrome 114) **sin `unlimitedStorage`**: el rechazo por cuota es **observable** con 1 reintento, `code: -32603` y aviso en el panel. | Cierra **ADT-21** y **ADT-14** (**D-L/D-M**); se aplica a RF-37, RF-28..RF-32 y RNF-16. |
| DEC-43 | **UUID literal del provider y `key` fija del manifest:** el UUID v4 del provider EIP-6963 es una **constante literal congelada** y el manifest generado incluye una **`key` fija** que estabiliza el **ID de la extensión**. | Cierra **ADT-19** (**D-N**); requisito para la allowlist CORS de Anvil (RE-04) y para la reproducibilidad de los E2E (RT-13/`CA-RT-13`). |
| DEC-44 | **`notifications` pasa a permiso opcional** (`optional_permissions`, solicitado en runtime al activar RF-39, ciclo posterior) y el ***token bucket* por origen cubre todo el catálogo RPC**, no solo los métodos aprobables. | Cierra **ADT-30** y **ADT-24** (**D-P/D-Q**); se aplica a RT-04/`CA-RT-04`, a la tabla de excepciones de §2.5 y a RF-28. |
| DEC-45 | **Bloqueo por sesión de dApp activa (R-09a).** Si una cuenta tiene una **entrada vigente en `truekeate_connected_sites`**, la extensión **bloquea** el **revelado/exportación** de su clave privada —y del mnemonic que la deriva si cualquiera de sus cuentas está en uso— y la **eliminación** de la cuenta importada, con error tipado **`-32000`** y el literal de la causa «Cuenta en uso por una dApp conectada» de `diccionario_datos.md` §4.3, que nombra el `origen` y la acción (revocar ese permiso). **No se permite continuar.** | Cierra **R-09a** del veredicto de reevaluación; se aplica a **RF-05, RF-06 y RF-50** (`CA-RF-05`, `CA-RF-06`, `CA-RF-50`), a `diccionario_datos.md` §3.10, a CU-06/CU-07 y a `documento_tecnico.md` §3.8 (regla 9). |
| DEC-46 | **Bloqueo del reset por cola o transacción en vuelo (R-09b).** Pulsar **Reset Wallet** con solicitudes `pending` en `truekeate_pending_requests` o con una transacción en vuelo vigente en `truekeate_inflight_tx` **bloquea** el reset con error tipado **`-32000`** (error de validación de UI: ningún método RPC lo devuelve) y el literal de la causa «Reset bloqueado» de `diccionario_datos.md` §4.3; la UI indica **cuántas** solicitudes quedan y ofrece **resolverlas** (aprobar o rechazar) o **esperar** a que expiren. Orden estricto: **cola vacía → sin transacción en vuelo → confirmación destructiva → limpieza**; solo entonces procede y se conserva `truekeate_logs` (RF-32). | Cierra **R-09b** del veredicto de reevaluación; se aplica a **RF-11** (`CA-RF-11`), a `diccionario_datos.md` §3.11, a CU-30 y a `documento_tecnico.md` §3.9. |
| DEC-47 | **Prohibido el esquema `chrome-extension://` en `matches`/`exclude_matches` (desviación D-H1-1).** El patrón `chrome-extension://*/*` **no es válido** en `content_scripts`: Chrome **rechaza el manifest completo** («Invalid value for 'content_scripts[0].exclude_matches[0]'») y la extensión **no carga** (el Service Worker nunca aparece). Se elimina; las exclusiones quedan en `https://metamask.io/*` y `https://*.metamask.io/*`. Además es **innecesario**: `<all_urls>` **no** incluye el esquema `chrome-extension://`. | Evidencia directa de H1: el manifest con ese patrón no cargaba en Chromium; con el patrón eliminado, `dist/` carga con 0 errores (`E2E: 01-onboarding.spec.ts — 4 passed`). Se aplica a `documento_tecnico.md` §7.3 (fragmento del manifest, tabla de justificación y nota de superficie amplia), `entornos_globales.md` §2/§4 y `requerimientos.md` (RNF-10). |
| DEC-48 | **El build MV3 se implementa como 3 builds encadenados con la API programática `build()` (desviación D-H1-2).** **Vite 7 no admite** que el fichero de configuración **exporte un array de builds**; y `format: 'iife'` + `inlineDynamicImports: true` **exigen una entrada por build**. Cadena: **build 1 ES** (3 páginas + Service Worker), **build 2 IIFE** (`content-script`), **build 3 IIFE** (`inject`, que además genera y valida `dist/manifest.json`), lanzados desde el hook `closeBundle`. | El efecto observable del corpus se mantiene: **un solo `npm run build`** produce las **6 entradas** y `dist/manifest.json` (`build-2026-09-11.log`). Se documenta en `documento_tecnico.md` §7.5.2/§7.5.3. **No** cambia ningún criterio de aceptación (`CA-RT-05` sigue cumplido). |
| DEC-49 | **Las tres ventanas declaran `width`/`height` explícitos en el `body` (desviación D-H1-3).** `body.tk-popup` / `body.tk-connect` / `body.tk-notification` usan los tokens `--tk-popup-width/height`, `--tk-connect-width/height` y `--tk-notification-width/height`, y el contenedor interno (`.tk-window`) hereda `height: 100%`. Con `min-height: 100vh` el `body` medía **720 px** de alto en lugar de 600 y Chrome abría el popup con la **altura por defecto del navegador**. `test.html` **no** lleva esas clases (layout fluido). | El popup se abre a **380 × 600** y las ventanas a 420 × 650 / 420 × 640, verificado en el E2E de H1 (`01-onboarding.spec.ts`). Se documenta en `identidad_visual.md` §5.1 y `documento_tecnico.md` §5.2. |
| DEC-50 | **Poppins se auto-hospeda en tres ficheros y las licencias OFL se versionan (desviación D-H1-4).** **Poppins no es una fuente variable**: tiene pesos **discretos**, de modo que se descargan `poppins-latin-400.woff2`, `poppins-latin-600.woff2` y `poppins-latin-700.woff2`, con **un `@font-face` por peso**. **Inter** y **JetBrains Mono** sí son variables (`inter-latin.woff2`, `jetbrains-mono-latin.woff2`). Se añaden las licencias **OFL-1.1** `public/fonts/LICENSE-{poppins,inter,jetbrains-mono}.txt`. | Cierra la suposición errónea del corpus (un único `.woff2` con `font-weight: 400 700`); la tipografía se sirve sin CDN y con licencia registrada (RT-12, RNF-20, RNF-23). Se documenta en `identidad_visual.md` §4 y `entornos_globales.md` §1/§2/§9. |
| DEC-52 | **El revelado se autoriza por ORIGEN y ruta del popup, no por `sender.tab` (desviación D-H2-G).** `canRevealInContext` (`src/background/crypto/secrets.ts`) exigía `tabId === null`, pero el popup del `action` y la página `index.html` abierta en una pestaña llegan **con `sender.tab`** —la misma observación que ya documentó **D-H2-B** para el router—. Medido en el E2E: `wallet_revealSecret` respondía **`4200 methodNotAllowedInContext`** SIEMPRE, de modo que `CA-RF-50` era inalcanzable por la UI. Se elimina esa condición y se conservan **`isExtensionContext`** y **`REVEAL_ALLOWED_ROUTES = ['index.html']`**: una página web sigue respondiendo `4200`. | Cierra el defecto que bloqueaba `CA-RF-50` (CU-07). Con sesión de dApp vigente el SW responde ahora **`-32000 accountInUseByDapp`** con el `origen` (DEC-45/R-09a). Se alinean **2 aserciones** de `secretsExport.spec.ts` (el spec codificaba el defecto) y se añade el caso `notification.html` con pestaña. `plan_desarrollo.md` §3.2.10. |
| DEC-53 | **Orden de guardas del reset en el SW y consulta previa del popup (desviación D-H2-H).** (a) `handleResetWallet` (`src/background/rpc/catalog.ts`) comprobaba `confirm` **antes** que las guardas, invirtiendo el orden de **CU-30** (cola `pending` vacía → sin transacción en vuelo → confirmación destructiva → limpieza) y dejando el `-32000 resetBlocked` inalcanzable cuando la llamada no aporta `confirm`. (b) El popup abría el diálogo destructivo **sin** consultar las guardas, cuando CU-30 paso 3 y §3.9 exigen abrirlo **solo** con ambas en verde. Ahora M33 aplica el orden documentado y el popup consulta con **`wallet_resetWallet { confirm: false }`** (`probeResetGuards`, `src/popup/walletState.ts`): guardas activas → `-32000` con el número exacto de pendientes y **sin** diálogo; guardas en verde → `4001` cancelado (señal de que el diálogo puede abrirse) **sin tocar el almacén** (§5.1.1). | Cierra `CA-RF-11` (parte 2, la guarda `-32000` de DEC-46/R-09b) en H2, sin añadir métodos al contrato cerrado de 15 internos (§5.1.1) ni leer el almacén desde el popup (RNF-14). Evidencia E2E: `06-reset.spec.ts` — bloqueo con `pending` y con transacción en vuelo. `plan_desarrollo.md` §3.2.10. |
| DEC-54 | **El aviso de éxito del reset sobrevive al desmontaje de la vista (desviación D-H2-I).** El reset deja la cartera vacía: `App` vuelve al formulario inicial (CU-30 paso 6) y `SecurityView` se desmonta, así que el aviso «Cartera reseteada» —estado local de la vista— desaparecía y el usuario no recibía ninguna confirmación. El aviso se eleva a `App` (`flash`, `tone="success"`) mediante el nuevo `onResetDone` de `SecurityView` y se pinta sobre el estado inicial. | Cumple CU-30 paso 6 («el popup vuelve a la pantalla inicial») **informando** al usuario en lugar de en silencio. Evidencia E2E: `06-reset.spec.ts:81`. `plan_desarrollo.md` §3.2.10. |
| DEC-55 | **Borrado del portapapeles APLAZADO cuando el ocultado ocurre sin foco (desviación D-H2-J).** Medido con el navegador real desde una página de la extensión con `clipboardRead`/`clipboardWrite`: con el documento sin foco, `navigator.clipboard.writeText('')` responde **`NotAllowedError: Document is not focused`** y `document.execCommand('copy')` devuelve `true` **sin** modificar el portapapeles. Es decir, con el disparador de **pérdida de foco** el borrado inmediato es **físicamente imposible** y la semilla quedaría copiada, incumpliendo `CA-RF-50` («al ocultarse … borra el portapapeles si aún contiene la semilla»). Solución: al ocultar sin foco se guarda **solo la huella SHA-256** del valor —el mecanismo `clipboardHash` que ya prescribe `diccionario_datos.md` §3.10—, se descarta el valor (§3.8 regla 4) y el borrado se ejecuta **en cuanto la ventana recupera el foco**, comparando por huella: **nunca** se borra a ciegas lo que el usuario haya copiado entretanto (§3.8 regla 5 / P-20). Si el borrado diferido falla, se registra y se avisa con `-32603 clipboardFailure`. | Mantiene el efecto observable de `CA-RF-50` (evidencia E2E: `25-recuperacion.spec.ts:110` — el portapapeles queda vacío tras una pérdida de foco **real**) sin conservar el valor en memoria ni destruir contenido ajeno. **Desviación declarada** de §3.8 regla 5 («no se reintenta si la API falla»): el reintento es **uno solo**, al recuperar el foco y solo si el contenido sigue coincidiendo. `plan_desarrollo.md` §3.2.10. |
| DEC-56 | **Correcciones del arnés E2E (desviación D-H2-K).** (1) `seedWallet` sembraba `truekeate_current_account: 'idx:0'` con `accounts: []` y `truekeate_accounts: []`: M13 lo clasifica como **cartera dañada** (`current-account-missing`, RNF-22, ya probado en `integrity.spec.ts`) y el popup pintaba «Wallet dañada» en vez del estado vacío; ahora solo escribe claves **coherentes** con lo sembrado. (2) `getByRole('button', { name: 'Importar frase' })` resolvía a **2 elementos** (el que abre el formulario y el `submit`): se acota al `form`. (3) «Una segunda pestaña pasa a primer plano» **no produce pérdida de foco** en Chromium headless (Playwright mantiene la emulación de foco: `hasFocus` sigue en `true` y no hay `blur`): nuevo `loseFocusToOtherPage` (`e2e/fixtures/extension.ts`) que la desactiva por CDP (`Emulation.setFocusEmulationEnabled`) y provoca un `blur` **real** del renderer. (4) El reset elimina `truekeate_settings` (§3.9), de modo que el aviso no descartable de **RNF-23** vuelve a mostrarse: `06-reset.spec.ts` lo acepta antes de comprobar el estado vacío, igual que `01-onboarding.spec.ts` (que prohíbe que el estado vacío esté en el DOM mientras el aviso bloquea). (5) `popupError` busca la **acción** de §4.3 por la **plantilla del mensaje**: un `-32000 resetBlocked` (mensaje con `<n>` ya sustituido) mostraba la acción de «cuenta en uso por una dApp». | El arnés deja de exigir estados que el producto no produce y de depender de una condición que el navegador no emite en headless; ninguna aserción se relaja (los 23 E2E siguen comprobando el mismo efecto observable). Evidencia: **Playwright 23 passed / 0 failed** (`ACTA_H2.md`). `plan_desarrollo.md` §3.2.10. |
| DEC-57 | **El cambio de cuenta activa emite `accountsChanged` (defecto D-H3-A).** `handleSetCurrentAccount` (`src/background/rpc/catalog.ts`) solo persistía la referencia `truekeate_current_account`: `emitAccountsChanged` (`src/background/events.ts`) **no tenía ningún emisor de producción** ligado al cambio de cuenta, de modo que la dApp no se enteraba del cambio (su caché `selectedAddress` quedaba obsoleta) y `CA-RF-15` quedaba a medias. Ahora persiste la referencia y **emite `accountsChanged` con la cuenta nueva** a las pestañas afectadas. | Cierra **D-H3-A** y deja **`CA-RF-15` CUMPLIDO** (evidencia: `08-cambio-cuenta-2026-09-11.json`, `setCurrentAccount.spec.ts`). `plan_desarrollo.md` §3.3.10. |
| DEC-58 | **Semántica de la sesión por origen: opción (a) (ambigüedad D-H3-B).** Mientras la sesión de un origen siga vigente, la dApp comparte la **cuenta activa**: el cambio de cuenta desde el popup **actualiza la cuenta compartida de las sesiones vigentes** (`account` en `truekeate_connected_sites`) y emite `accountsChanged`, **sin borrar la sesión** (equivalente a MetaMask). La **alternativa (b)** —sesión fijada a la cuenta elegida en la conexión— queda **descartada** y no debe reintroducirse. | Cierra la ambigüedad medida en H3 (`eth_accounts` seguía autorizando `idx:0` tras `wallet_setCurrentAccount { ref: 'idx:1' }`) y es la semántica que hace decidible `CA-RF-15`; `setCurrentAccount.spec.ts` y `sessions.spec.ts` la fijan. `plan_desarrollo.md` §3.3.10. |
| DEC-59 | **`INTERNAL_METHODS` en un módulo hoja (defecto D-H3-C).** `senderGuard.ts` capturaba la lista al evaluar el módulo (`INTERNAL_METHOD_NAMES = INTERNAL_METHODS`) dentro de la cadena circular `catalog → connections → sessions → senderGuard → catalog`: según el orden de importación la referencia podía ser `undefined` y la guarda lanzaba `TypeError: Cannot read properties of undefined (reading 'includes')`, degradando el router a **`-32603` en cualquier petición**. La lista se extrae a **`src/background/rpc/internalMethods.ts`** (sin dependencias) y se consume **sin depender del orden de importación**. | Cierra **D-H3-C** (fragilidad de inicialización, no fallo activo) y lo fija `src/background/security/importOrder.spec.ts`. `plan_desarrollo.md` §3.3.10. |
| DEC-60 | **`webServer` obligatorio en el arnés E2E (defecto del arnés D-H3-D).** Los E2E que abren la dApp fallaban con `page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:5174/test.html` porque `npm run dev` se arrancaba **a mano** (**26 passed / 11 failed**). `playwright.config.ts` declara **`webServer`**: `command: 'npm run dev'`, `url: 'http://localhost:5174/test.html'`, `reuseExistingServer: true`, `timeout: 120 s`. La suite **se autosirve la dApp** y la apaga al terminar (**37 passed / 0 failed**). | **Regla vinculante:** la suite **no** debe depender de que alguien arranque la dApp a mano. Es un defecto del arnés y **no cambia ningún criterio de aceptación**. `plan_desarrollo.md` §3.3.10. |
| DEC-61 | **Nota operativa de Anvil y aborto explícito del `global-setup`.** `anvil --silent` **no arranca** cuando se lanza mediante `Start-Process` con redirección de salida: el proceso muere con **exit 1** y toda la suite E2E falla con `ERR_CONNECTION_REFUSED` / `error sending request`, un síntoma que se confunde con un defecto de la extensión. Se registra en `entornos_globales.md` §2.1 y se recomienda que el **`global-setup` aborte con un mensaje claro si Anvil no responde** (hoy solo avisa y las pruebas con red se saltan o fallan más tarde). | Evita diagnosticar como fallo de producto lo que es un arranque fallido del nodo. La **allowlist de CORS de RE-04/H-41 sigue siendo la norma del producto**; el comodín queda acotado a la máquina de desarrollo del arnés. `entornos_globales.md` §2.1/§2.4, `plan_desarrollo.md` §3.3.10. **La bandera `--http.corsdomain` de esta decisión no existe en Anvil 1.7.2-dev: queda corregida por DEC-63.** |
| DEC-62 | **Cableado de los 6 métodos aprobables: el catálogo, no el router (defecto D-H4-E1).** La ruta de M19.b **ya existía** en `src/background/rpc/router.ts` (paso 6 → `defaultRouterDeps.approve` → `approvals/dispatch.ts`: preview M19 → cola M14 → plazo M15 → ventana única M18 → decisión → firma M11 → difusión M7 → `deliverApprovalResolution` M17), pero era **inalcanzable**: `getCatalogEntry` filtra por `isSupportedMethod` y los 5 aprobables restantes seguían con `implemented: false` y **fuera** de `supportedMethods`, de modo que el router respondía **`4200`** en el paso 3 antes de llegar al despacho (síntoma medido: `waitForEvent("page") Timeout 20000 ms` en `04-enviar`, `10-aprobar-tx` y `11-firmar-mensaje`; la ventana de confirmación **nunca se abría**). Se corrigen los **metadatos** de `rpc/catalog.ts` (los 6 aprobables `implemented: true` y `supportedMethods` = lecturas + aprobables + internos) y la guarda de contexto de `rpc/router.ts` deja pasar `wallet_revokePermissions` desde una **página** al despacho de aprobaciones, conservando el manejador interno para el popup (tarea 3.11). No se duplicó ninguna lógica: `enqueueApprovalRequest`, `buildApprovalPreview` y `showOldestPending` pasan a tener llamador de producción. | Cierra **D-H4-E1**; `CA-RF-08`, `CA-RF-19`, `CA-RF-20` y `CA-RF-21` pasan a **CUMPLIDOS** con la prueba real de extremo a extremo archivada. Se ajustan **2 aserciones de `messaging.spec.ts`** que codificaban la afirmación de H3 «los 6 aprobables responden `4200`» y que H4 **debe** sustituir (con la línea de motivo en cada una); el conflicto `4001`/`-32000` de `queue.spec.ts` **no existía**: ese spec ya exigía `-32000 inflightTxInProgress`. `plan_desarrollo.md` §3.4.10. |
| DEC-63 | **El comando de Anvil del arnés usa `--allow-origin`, no `--http.corsdomain` (corrección de D-H4-E3).** Medido con **Anvil 1.7.2-dev**: `anvil … --http.corsdomain "*"` muere al instante con `error: unexpected argument '--http.corsdomain' found`, el nodo no arranca y **toda la suite posterior** falla con `ERR_CONNECTION_REFUSED` / `error sending request` (síntoma que se confunde con un defecto del producto). El comando verificado es **`anvil --host 127.0.0.1 --port 8545 --chain-id 31337 --allow-origin "*"`**, **sin `--silent`**; `--allow-origin` admite **una lista separada por comas** y **no** puede repetirse (`cannot be used multiple times`). | Cierra el defecto del arnés y evita el diagnóstico erróneo. Se documentan **los dos errores** en `entornos_globales.md` **v2.2** §2.1 (con la allowlist normativa del producto reescrita a la misma bandera) y se corrigen las referencias de `plan_desarrollo.md` §3.3.10/§6. La **allowlist de CORS de RE-04/H-41 sigue siendo la norma del producto**; el comodín queda acotado a la máquina del arnés. |
| DEC-64 | **`D-H4-E2` (regresión de H3) verificada EN VERDE: sin cambio de producción.** Las tres pruebas que la batería anterior daba por rojas (`08-eventos.spec.ts:73`, `08-eventos.spec.ts:162`, `09-conectar.spec.ts:77`) están **verdes** en la batería de H4 del 2026-09-12, tanto aisladas como en la suite completa (3 ejecuciones), con las 3 suites de H3 (`08`, `09`) y el E2E `13`/`14`/`28` verdes. El síntoma registrado (conexión de la dApp → `4001`) corresponde al plazo de conexión **inyectado por el arnés** (`VITE_CONNECT_TIMEOUT_MS=2000`, `e2e/global-setup.ts:47`): con el equipo cargado, `connect.html` puede tardar más de 2 s en pintar sus 5 filas con saldo real antes del clic. | **No se toca producción** (no hay defecto reproducible) y **no** se desactiva ni se salta ninguna prueba, tal y como exige la orden de trabajo. Si reaparece, la palanca es el plazo inyectado del arnés, **no** el orden de guardas ni el cálculo de `origin`/`sender`. `plan_desarrollo.md` §3.4.10. |
| DEC-65 | **Los dos bloqueos de H4 quedan CERRADOS (2026-09-12) con causa raíz medida.** (1) **D-H4-E9** — `18-concurrencia` rojo con `net::ERR_CONNECTION_REFUSED at http://127.0.0.1:5174/test.html`: la prueba necesita **dos orígenes del mismo servidor** (`localhost` y `127.0.0.1`), pero el servidor de la dApp escuchaba **solo en IPv6** (`vite.config.ts` declaraba `server: { port: 5174, strictPort: true }` **sin `host`**, y el defecto de Vite `localhost` resuelve a `::1`). (2) **D-H4-E10** — `29-sw-suspendido` rojo porque la dApp recibía **`-32603`** en lugar de `4001`: al suspender el SW por CDP se cerraba el canal del relay y `src/content-script.ts` respondía `internalTransportError()` ~130 ms después de la suspensión, **antes** de que la alarma despertara al SW, y el `4001` de la reconciliación se empujaba con `id = approvalId`, que **no correlacionaba** con el `id` de la capa inject. | **CERRADA en el cierre de H4** por **DEC-66** (D-H4-E9) y **DEC-67** (D-H4-E10): los dos E2E están **verdes** y `CA-RF-35`, `CA-RF-41` (parte E2E) y RNF-08 (parte E2E) pasan a **CUMPLIDOS**. **H4 queda ✅ COMPLETADO (2026-09-12)** y el hito actual pasa a **H5**. `plan_desarrollo.md` §3.4.9/§3.4.10, `ACTA_H4.md` §3/§4. |
| DEC-66 | **`host: '127.0.0.1'` en Vite: la dApp responde también en IPv4 (cierre de D-H4-E9).** `vite.config.ts` declaraba `server: { port: 5174, strictPort: true }` sin `host`, y el valor por defecto (`localhost`) resolvía a **`::1`** en este equipo: `http://127.0.0.1:5174/test.html` daba `ERR_CONNECTION_REFUSED` y `18-concurrencia` (que necesita **dos orígenes del mismo servidor**) no podía ejecutarse. Se declara **`server: { host: '127.0.0.1', port: 5174, strictPort: true }`** (y el mismo `host` en `preview`). | **Regla vinculante del arnés:** el servidor de la dApp debe escuchar en la interfaz que la prueba usa por nombre (`127.0.0.1`), no solo en `::1`. **No cambia ningún criterio de aceptación**; `18-concurrencia` pasa a verde. `plan_desarrollo.md` §3.4.10. |
| DEC-67 | **Correlación del salto 1 y transporte del relay al suspender el SW (cierre de D-H4-E10).** Dos correcciones en `src/content-script.ts` con su cadena hasta el SW: (a) el relay **ya no** convierte el cierre del canal en un `-32603` definitivo —reintenta **solo** cuando el fallo garantiza que el mensaje no llegó a nadie (`Could not establish connection. Receiving end does not exist.`, que es lo que despierta a un SW dormido) y, en cualquier otro fallo de transporte, **espera** la resolución que el SW empuja hasta agotar `SIGN_TIMEOUT_MS + TIMEOUT_SAFETY_MARGIN_MS` (H-07: la capa content es solo red de seguridad con margen superior)—; (b) el `id` de la página viaja como **`requestId`** en el sobre `TRUEKEATE_RPC` (`shared/protocol.ts`), el router lo propaga (`rpc/router.ts` → `approvals/dispatch.ts` → `approvals/queue.ts`), se **persiste** con la entrada (`PendingRequest.requestId`, `shared/types.ts`) y la entrega empujada responde con **ese** `id` (`approvals/ports.ts` → `responseCorrelationId`), de modo que sobrevive a la suspensión del SW; `lastApprovalId` se asigna al **observar** el `approvalId` que acompaña a la resolución empujada (sin transformar el sobre, nota H-39). | Cierra **D-H4-E10** y con él **CA-RF-41** y **RNF-08** en su parte E2E: la dApp recibe **`4001`** («plazo establecido») en lugar de `-32603`, la cola se purga sin huérfanas, no hay doble difusión (`nonce` intacto) y la reconstrucción es **< 1 s**. `plan_desarrollo.md` §3.4.10. |
| DEC-68 | **Re-render de la ventana única: canal del empuje y orden respecto de la respuesta (cierre de D-H4-E11/D-H4-E12).** Al poner `18-concurrencia` en verde se midieron **dos** defectos del re-render: (1) **D-H4-E11** — el cuerpo de la siguiente solicitud se empujaba con `chrome.tabs.sendMessage(tabId, …)`, que **no** alcanza a `notification.html` (es una **página de la extensión**, no un content script): medido, responde `Could not establish connection. Receiving end does not exist.`, y el `tabId` ni siquiera era resoluble sin el permiso `tabs` (retirado en H-36: `windows.getAll({ populate: true })` devuelve `url: null`). (2) **D-H4-E12** — carrera de orden: la pasada de M18 empujaba la siguiente solicitud **dentro** del ciclo que respondía `SIGN_RESPONSE`, así que el empuje llegaba antes de que la ventana procesara su propia decisión y el `setOutcome`/`decided` posterior dejaba el botón «Aprobar» **deshabilitado** con la solicitud correcta en pantalla. | **CERRADA**: (1) el empuje usa **`chrome.runtime.sendMessage`** —el único canal que alcanza a las páginas de la extensión— conservando `tabs.sendMessage` como respaldo; **medido además que no llega a los content scripts** (la dApp no recibe nada), con lo que **RNF-09 se mantiene** y el cuerpo no sale de los contextos confiables; (2) `background.ts` responde la decisión **primero** y hace la pasada de M18 **después** (`showOldestPending({ repush: true })`, con `responses.ts` aceptando `refresh: false` y `focus.ts` re-entregando el cuerpo de forma idempotente). **Verificado** por `18-concurrencia` en verde (contador «1 solicitud en espera» y segunda firma obtenida de la MISMA ventana). `plan_desarrollo.md` §3.4.10. |

| DEC-69 | **El permiso de host se pide en el POPUP, dentro del clic (defecto D-H5-A).** `chrome.permissions.request` **exige un gesto de usuario en el contexto que llama**: medido con sonda propia, desde el **Service Worker** Chrome lo rechaza **siempre** con `This function must be called during a user gesture` —incluso con el origen **ya concedido**—, desde el popup sin gesto la promesa **nunca se resuelve** y con gesto aparece el aviso nativo (no accionable sin interfaz). El corpus (DEC-36) suponía que la aprobación de la ventana única era gesto válido para una llamada hecha en el SW, y **no lo es**: `wallet_addEthereumChain` terminaba **siempre** en `4001` y RF-23 era inalcanzable. Corrección: (a) `networks/addChain.ts` consulta **`contains`** y **no** llama a `request` si la concesión ya está vigente; (b) `popup/views/NetworksView.tsx` (M43) pide el permiso del `rpcUrl` **en el propio clic** antes de invocar el alta. | Desviación de **orden** aceptada en `plan_desarrollo.md` §3.5.10: desde el popup el permiso se pide **antes** de la ventana de confirmación, pero el **efecto** (persistir la red) sigue ocurriendo **después** de la aprobación y la denegación sigue siendo `4001` **sin persistir** (DEC-36 intacta). Verificado por `networks.spec.ts` y por el E2E `12-redes` (T2 alta con origen ya concedido; T4 denegación real desde la dApp). |
| DEC-70 | **El ciclo aprobable de los métodos de RED no exige sesión de dApp (defecto D-H5-B).** `rpc/router.ts` construía el `runner` de `wallet_switchEthereumChain`/`wallet_addEthereumChain` sobre `dispatchApproval`, que empieza por `resolveApprovalOrigin` → **exige sesión**: desde el **popup** (origen `extension`) respondía **`4100`** («no-session-for-approval») **sin abrir la ventana única**, y desde una dApp M24/M25 no recibían la cuenta de la sesión. El runner pasa a usar **`runApprovalCycle`** (cola M14 → plazo M15 → ventana única M18 → decisión) **sin exigir sesión** —la cuenta y la red ya las resuelve `resolveApprovalAccount`, M24/M25— y el router aporta `sessionAccount` (`authorizedAccountFor`) cuando el emisor es una página (RNF-11). Nueva costura **opcional** `RouterDeps.runNetworkCycle` para no romper los dobles existentes. | Cierra el defecto que impedía **cambiar y dar de alta redes desde el popup** (RF-22/RF-23). No se duplica ni el encolado ni el plazo (se reutilizan M14/M15/M18) y el contrato de `PendingRequest` (§2.8) se conserva. Verificado por `e2e/12-redes` (T2/T4/T5) y por los 40 ficheros de Vitest de `src/background`. `plan_desarrollo.md` §3.5.10. |
| DEC-71 | **La vista previa reintenta el saldo 7 s con el nodo caído (defecto D-H5-C) — REPORTADO, no corregido en H5.** `safeBalance` (`src/background/approvals/dispatch.ts`) usa la política completa del cliente RPC (4 intentos + backoff 1/2/4 s) para un dato **informativo**, de modo que la ventana única se abre ~7 s tarde cuando el nodo no responde y los **5 casos de `dispatch.spec.ts`** agotan el plazo de 5 s de Vitest **en ese escenario** (medido en la línea base con Anvil detenido: `ECONNREFUSED` × 4 intentos). | **No se toca**: `src/background/approvals/**` es **de solo lectura** en el terreno de H5 y la regla del hito manda **reportar** los defectos de ahí. **Corrección propuesta (1 línea, para H6):** `getBalanceWei(address, { attempts: 1 })`, el mismo criterio *fail-fast* que la estimación de gas de RNF-25. Con **Anvil en marcha** (el entorno de la batería de cierre) los 5 casos pasan en ~200 ms: la suite queda **verde** y lo que queda abierto es el retraso de la confirmación con el nodo caído. `plan_desarrollo.md` §3.5.10, `ACTA_H5.md` §3. |
| DEC-72 | **El router escribe la traza de CADA llamada del catálogo (defecto D-H5-D).** `CA-RF-28` («log de llamadas con método, params redactados, origen y `ts`») **no era verificable**: `logging/**` estaba implementado pero **sin cablear** —no existía ninguna llamada a `logEvent` en `rpc/router.ts`—. `handleRPCRequest` pasa a ser un envoltorio transparente que delega en `dispatchRPCRequest` y escribe **exactamente 1** entrada por llamada: `rpc_call` (con `method`, `origin`, `ts` y los `params` **ya redactados** por M22) o `rpc_error` con el `code` de §4.3. | La traza se escribe **después** de resolver y **nunca** cambia la respuesta; el descarte por cuota sigue siendo visible (`wallet_getLogs.dropped`). Verificado por `e2e/15-logs` (con el popup **cerrado**, una llamada ⇒ **una** entrada nueva) y por los 47 ficheros de Vitest. |
| DEC-73 | **El descarte por cuota no re-persiste el lote descartado (defecto D-H5-E).** `persistQuotaDiagnostic` recibía `plan.retained` —que **incluye** las entradas nuevas que la cuota acaba de rechazar—, de modo que la entrada descartada se colaba en el almacén por la puerta de atrás de la escritura de diagnóstico. Ahora se persiste **el histórico + la entrada de diagnóstico** y `logEvent` devuelve la entrada **del evento pedido** (o `null`), nunca la de diagnóstico. | Cumple §2.15/RNF-16: lo descartado **no** se guarda y lo observable es la entrada `storage_quota_exceeded` con su `code: -32603` y el contador de descartes. Verificado por `storageQuota.spec.ts`. |
| DEC-74 | **Contador de descartes: se escribe solo cuando cambia (optimización de RNF-08).** El diagnóstico de cuota se reescribía en `truekeate_logs_dropped` en **cada** traza (dos operaciones de almacén por entrada). Medido con el E2E `29-sw-suspendido` (RNF-08: reconstrucción < 1 s): la escritura redundante llevaba la reconstrucción a ~1,3-1,9 s en la suite completa. Ahora se persiste **solo si `dropped`/`retries` cambian** (y lo hidratado cuenta como persistido). | Con la observabilidad de H5 activa, la reconstrucción vuelve a **< 1 s** (`29-sw-suspendido-2026-09-12.json`) conservando el modo de fallo observable de §2.15: el valor que sobrevive a la suspensión del SW es el último cambio. |
| DEC-75 | **El alta nacida en una dApp queda REPORTADA (defecto D-H5-N), no corregida en H5.** Para un alta originada en una página, el gesto que exige `chrome.permissions.request` tendría que aportarlo la **ventana de confirmación** (`src/notification/**`), que está **fuera del terreno autorizado de H5** (solo lectura). El modo de fallo es **observable y está verificado**: aprobación ⇒ permiso sin gesto ⇒ **`4001`** con «No se concedió el permiso de acceso…» y **red no persistida**. | Corrección propuesta para **H6**: que el botón «Aprobar» de `notification.html` pida el permiso de host de `networkPreview.rpcUrl` **antes** de responder `SIGN_RESPONSE` (un clic real en esa ventana **sí** es gesto válido). Alternativa declarada y no verificable en el arnés: el alta de una red **nueva** desde el popup exige responder al aviso nativo de Chrome (**D-H5-O**, NO VERIFICADO en `E2E_HEADLESS`), cubierto por `networks.spec.ts`. `ACTA_H5.md` §3.c/§5. |
| DEC-76 | **Correcciones del arnés E2E de H5 (defectos D-H5-H..D-H5-L).** (1) La **ventana única** se perdía: el plazo inyectado es de **3 s** y registrar la espera **después** del clic pierde una ventana que nace y muere dentro de ese margen; además la ventana es **única** (P-21) y el SW puede **reutilizarla** sin emitir ningún evento `page`. Nuevo `conVentanaDeDecision` (espera **antes** de la acción y admite la reutilizada). (2) La política de tasa **real** es de **6 solicitudes por 60 s y origen** (`pendingRequestsPerMinute`): los 7 flujos encadenados en una sola prueba se pasaban del cupo y el 6.º/7.º método respondía `4001 rateLimitExceeded` **sin abrir ventana**; se reparten en **dos pruebas** con perfil nuevo (ninguna aserción se relaja). (3) El *polling* de saldos del popup mantiene el SW despierto e impedía `ServiceWorker.stopWorker` (flake intermitente de `09-conectar` en la suite completa): la prueba **sale de la pestaña «Cuentas»** antes de suspender (su sujeto —la sesión sobrevive— no cambia). (4) Con el nodo caído el SW escribe trazas **por diseño** (`CA-RF-28`): `27-rpc-caido` excluye las claves de **observabilidad** y conserva intacta la aserción de RNF-07 sobre el estado de la cartera. (5) Los ficheros de `e2e/**` corren en **Node** y no pueden importar `constants.ts` (`import.meta.env`): el literal de RNF-23 se transcribe en el spec. | Los 44 E2E de H1–H4 siguen verdes (**58 passed / 0 failed / 0 skipped** con los 14 del hito). **Regla de proceso vinculante:** nunca ejecutar Vitest **a la vez** que `test:e2e` —Vitest escribe temporales en `src/` y el *watcher* de Vite mata el servidor de la dApp con `EBUSY` (**D-H5-J**, 30 pruebas caídas en un intento)—. `plan_desarrollo.md` §3.5.10, `ACTA_H5.md` §3.b. |
| DEC-77 | **La medición de RNF-08 usa intervalo FIJO de 50 ms.** En `29-sw-suspendido` la reconstrucción se medía con el intervalo **adaptativo** de `expect.poll` (50 → 100 → 250 → 500 → 1000 ms), que añadía hasta ~1,5 s de sobrecoste **del propio arnés** a una comparación con el umbral de **< 1 s** de RNF-08: la prueba medía su propia espera. Se fija **`intervals: [50]`**. | El **umbral no cambia** (RNF-08 sigue exigiéndose) y la medición pasa a reflejar la reconstrucción del SW: **< 1 s** con la observabilidad de H5 activa. `plan_desarrollo.md` §3.5.10. |
| DEC-78 | **Concurrencia del catálogo de redes: cerrojo de escritura (defecto de producto D-H5-P).** `truekeate_networks` se escribe con **lectura-modificación-escritura**: `seedDefaultNetwork` (siembra de arranque, que corre en **cada** despertar del SW) y `upsertNetwork` (cada alta) leían el mapa y lo reescribían **sin serialización**. Si la siembra leía antes de un alta y escribía después, el mapa resultante era `{ ...catálogo_leído, Anvil }` y **la red recién dada de alta desaparecía** (síntoma medido: la guarda de diagnóstico de `12-redes` detectó que el catálogo sembrado había perdido la segunda red). Se añade un **cerrojo por encadenamiento de promesas** (`runSerializedNetworkWrite`, el mismo patrón que M29/M30 para la cola y el log) que envuelve **lectura y escritura** de las dos funciones. | Elimina la pérdida silenciosa de un alta (CA-RF-23) dentro del Service Worker. La escritura **externa** (el arnés, por CDP) no participa del cerrojo: su siembra se **reintenta comprobándola** dentro del test. Verificado por `networks.spec.ts` y por `12-redes` en verde. Junto con **D-H5-Q** (espera observable de `truekeate_networks` en `07-provider`, que también era intermitente) la suite queda **58 passed / 0 failed / 0 skipped**. `plan_desarrollo.md` §3.5.10, `ACTA_H5.md` §3.b. |
| DEC-79 | **La puerta de cobertura de RNF-17 se abre en H6 y destapa `approvals/` por debajo del umbral (desviación D-H6-A).** Medido con la suite completa antes del cierre: `src/background/approvals/` daba **473/732 = 64,62 %** de ramas (`ports.ts` 12/39, `preview.ts` 35/108, `responses.ts` al 0 % de sentencias) frente al **≥ 80 %** que exige RNF-17 («el hito no se cierra por debajo del umbral»). Se añaden **245 pruebas en 11 specs de `test/oracle/**`** y se amplía el universo de Vitest (`include: ['src/**/*.spec.ts', 'test/oracle/**/*.spec.ts']`), en lugar de crear specs junto a los módulos, porque el terreno de este cierre **no incluía `src/background/**`**: **ninguna línea de producto cambió**. Los umbrales quedan **declarados en configuración** (`coverage.thresholds` del bloque `test` de `vite.config.ts`): 70 % global y 80 % en `crypto/`, `approvals/` y `shared/validation/`. | `npm run coverage` pasa a ser una **puerta que falla sola** (RNF-17): **exit 0** con ramas globales **86,13 %**, `crypto/` **86,38 %**, `approvals/` **95,21 %** y `shared/validation/` **91,09 %**. **Nota de medición (v8):** el denominador de ramas crece con el código *ejecutado*, de modo que el 64,62 % inicial y el 95,21 % final **no** se comparan sobre el mismo total (732 → 1298); ambos son reales. `plan_desarrollo.md` §3.6.10, `ACTA_H6.md` §1/§7. |
| DEC-80 | **El gate de RNF-18 marcó los literales de color de los specs que verifican el contraste (desviación D-H6-B) — CERRADA en el mismo turno.** Primera pasada de `npm run lint:prohibited` en H6: **exit 1** con **33 hallazgos**, **todos** literales de color en `src/styles/contrast.spec.ts` (27) y `src/styles/goldUsage.spec.ts` (6), que contenían la paleta esperada congelada para hacer falsable la tarea 6.2. El **código de producto** nunca tuvo literales y la comprobación de `dist/` (0 hosts de CDN, 0 recursos remotos) siempre pasó. | **CERRADA** por la carga de identidad+accesibilidad, que reescribió ambos specs para que **lean la paleta de `tokens.css`** (opción (a) de las dos declaradas): hoy tienen **0 literales hexadecimales** y `npm run lint:prohibited` da **exit 0 con 0 hallazgos** (150 ficheros de `src/`, 14 de `dist/`). **No** hizo falta tocar `scripts/lint-prohibited.mjs` ni RNF-18/`CA-RF-49`, así que **no hubo cambio de requisito**. `plan_desarrollo.md` §3.6.10, `ACTA_H6.md` §1/§3. |
| DEC-81 | **Dos citas de evidencia con nombre de fichero desactualizado (desviación D-H6-C).** La auditoría mecánica de la matriz `RF Must → evidencia` (40/40 con evidencia localizada) detectó que `requerimientos.md` §9.2 cita **`tx.spec.ts`** en RF-08 (el fichero real es `src/background/rpc/txContract.spec.ts`) y **`approvalQueue.spec.ts`** en RF-37 (el real es `src/background/approvals/queue.spec.ts`). | **Es deriva documental, no falta de evidencia:** ambos specs **existen y están en verde**, y **los 40 RF Must conservan su evidencia**. `requerimientos.md` queda fuera del terreno de H6 → se declara para la **pasada de consistencia de la Fase 4** (§7 del plan: actualizar el corpus en el mismo turno). Evidencia: `evidencia/H6/auditoria-rf-evidencia-2026-09-12.log`. |
| DEC-82 | **RNF-15 se marca «verificado solo en Windows; pendiente en Linux» (desviación D-H6-D).** `wsl -l -v` responde **«Subsistema de Windows para Linux no tiene distribuciones instaladas»** y no hay CI declarado; el comando normativo `wsl -d Ubuntu -- bash -lc "npm ci && npm run build"` **no es ejecutable** en este equipo. | Se aplica **H-20** y §3.6.8 del plan: **nunca** se marca «cumplido». Lo verificado es `npm ci && npm run build` **exit 0** en Windows (**33 s + 22 s**) con `package-lock.json` versionado y `npx tsc -b` exit 0 (strict). Evidencia: `evidencia/H6/wsl2-no-disponible-2026-09-12.log`, `build-windows-2026-09-12.log`. |
| DEC-83 | **El ensayo de instalación del Perfil B se declara NO VERIFICADO en su parte de sujeto (desviación D-H6-E).** La tarea 6.10 pide «un evaluador **sin conocimiento previo**» que instale en **≤ 15 min** con **0 consultas al autor**; quien lo ejecutó es el **autor del proyecto y de los dos documentos**. | Se entrega un **dry-run cronometrado** (camino crítico de máquina: **55 s**; **0 consultas**; ninguna ambigüedad bloqueante) y la equivalencia automatizada de «`dist/` carga con 0 errores» (`01-onboarding.spec.ts`), pero el ensayo **independiente** queda **NO VERIFICADO** con el motivo escrito. **Recomendación:** que una persona ajena ejecute los 4 pasos del `README.md` con cronómetro y registre tiempo, consultas e interpretaciones. `evidencia/H6/instalacion-2026-09-12.md`. |
| DEC-84 | **El diálogo «Acerca de» existe porque RNF-23 lo exige; RF-48 NO se promociona (desviación D-H6-F).** RNF-23 pide el aviso de entorno «en **Acerca de**»; la carga paralela implementó `AboutDialog` (M39/M50 de soporte), que además muestra el **logotipo horizontal** y la **tagline** `PRODUCTOS \| SERVICIOS \| CRIPTOACTIVOS TOKENIZADOS`, las licencias y la referencia a `NOTICE`, y lo monta en las **tres** ventanas. | **El alcance del MVP no cambia** (DEC-26/P-17): **RF-48 sigue siendo *Should*** en el ciclo posterior como **pantalla completa independiente**, y ni su `CA-RF-48` ni su CU se declaran cerrados en H6. Se evita así promocionar un requisito *Should* por la puerta de atrás de un RNF. `INSTRUCCIONES.md` §18.1 recoge el diálogo como superficie del aviso. |
| DEC-85 | **`grep -rn "http" dist/` no da 0 apariciones: el criterio 6.9 se lee como «0 URLs de CDN» (desviación D-H6-G).** El comando devuelve **34 coincidencias** en 41 ficheros y **ninguna es remota**: `host_permissions` **locales** del manifest (`http://127.0.0.1:8545/*`, `http://localhost:8545/*`, exigidos por RT-04), el *namespace* XML de los SVG de marca (`http://www.w3.org/...`), la URL de la licencia **OFL** dentro de los tres `LICENSE-*.txt` y subcadenas del código minificado y de los mapas de origen. | La comprobación **específica** de CDN (**lista cerrada de hosts + patrones de recurso remoto** sobre `dist/`) la ejecuta `lint:prohibited` y da **0**. Se declara para que el criterio no se interprete como «0 apariciones de la subcadena `http`», que sería falso y contradeciría a RT-04. Evidencia: `evidencia/H6/grep-http-dist-2026-09-12.log`. |
| DEC-86 | **35 módulos de `src/` completan su cabecera JSDoc de contrato (desviación D-H6-H).** El gate nuevo `src/docs.spec.ts` destapó 35 ficheros sin la referencia `RF/RNF/RT` o sin identificador de módulo (34 con una línea añadida y `src/manifest.ts`, cuyo bloque `//` inicial pasó a bloque JSDoc), más `src/styles/goldUsage.spec.ts` detectado a mitad de cierre. | **Solo comentarios**: ninguna línea de lógica cambió (`npx tsc -b` exit 0 y suite completa en verde). El gate cubre **145 ficheros** con **148 comprobaciones** y falla nombrando el fichero si alguien entra sin cabecera. **Regla nueva (criterio de la rúbrica de Documentación):** todo fichero `.ts`/`.tsx` de `src/` abre con bloque JSDoc con identificador `M-xx` del corpus y referencia de requisito. `plan_desarrollo.md` §3.6.5 tarea 6.6. |
| DEC-87 | **La primera batería E2E de cierre se contaminó por concurrencia (desviación D-H6-I) y se repite en ventana limpia.** Dos cargas de trabajo de H6 ejecutaron Playwright **a la vez** sobre el **mismo** `dist/`, el **mismo** `test-results/` y la **misma** carpeta `RepoTecnico/evidencia/H6/`: la primera pasada cerró en **68 passed / 8 failed**, con **dos ficheros sonda borrados a mitad de ejecución** (`ENOENT` al importar `91-sonda-axe.spec.ts` y `92-sonda-aviso.spec.ts`), `Timeout ... waiting for event "serviceworker"` en `01-onboarding`/`04-enviar`/`09-conectar`/`10-aprobar-tx`/`18-concurrencia` y `ENOENT` sobre artefactos de traza —la firma inequívoca de dos suites simultáneas—. | Se conserva el **intento contaminado** como evidencia (`e2e-intento1-contaminado-2026-09-12.{log,json}`) y se **re-ejecuta en una ventana sin ningún proceso de Playwright** (dos comprobaciones consecutivas), archivando el resultado limpio en `e2e-cierre-2026-09-12.{log,json}`. **Regla de proceso para la Fase 4:** las suites de Playwright de dos cargas de trabajo **no pueden solaparse**, extensión de la lección `D-H5-J`/DEC-76 (Vitest y E2E, tampoco a la vez). |

---

## 5. Entrevista de la Fase 1 ✅ CERRADA

### Bloque 1 — Entorno, repositorios y seguridad

| ID | Pregunta | Respuesta |
|---|---|---|
| P-01 | Repositorios remotos | Crear en GitHub `chrome-wallet` (`main` + `chrome-wallet-DSH`), crear en GitLab `chrome-wallet` (mismas ramas), agregar `gitlab.codecrypto.academy/anlucorporations/chrome-wallet.git`. |
| P-02 | Red por defecto | **Solo Anvil local** (`127.0.0.1:8545`, chainId 31337), **sin Sepolia**. |
| P-03 | Seguridad del mnemonic | **Sin contraseña** (modo desarrollo); riesgo aceptado. |

### Bloque 1-bis — Línea base existente

| ID | Pregunta | Respuesta |
|---|---|---|
| P-10 | ¿Qué se hace con la implementación previa del remoto `codecrypto`? | **Reconstruir desde cero** (DEC-09): el remoto queda solo como **referencia de consulta** y su código no se reutiliza ni se versiona. |

### Bloque 2 — Alcance funcional

| ID | Pregunta | Respuesta |
|---|---|---|
| P-04 | Nº de cuentas derivadas | **5 por defecto + botón "Añadir cuenta"**. |
| P-05 | `personal_sign` y QR de recepción | **Sí a ambos.** |
| P-06 | Revocar permisos y renombrar cuentas | **Sí a ambos.** |

### Bloque 3 — Calidad, pruebas y entrega

| ID | Pregunta | Respuesta |
|---|---|---|
| P-07 | Frameworks de prueba | **Vitest + Playwright + contrato verificador EIP-712 con Forge.** |
| P-08 | Despliegue | **100 % local, sin GCP.** |
| P-09 | Idioma | **UI y documentación en español; identificadores de código en inglés.** |

### Bloque 4 — Identidad visual ✅ RESUELTO

| ID | Pregunta | Estado |
|---|---|---|
| P-13 | Nomenclatura marca/enunciado | ✅ **Renombrar todo a TrueKeate**, incluido el provider (`window.truekeate`) y su alias `window.codecrypto` (DEC-21). |
| P-14 | Sistema de diseño | ✅ **Aprobado tal cual** (`identidad_visual.md`, hoy v1.4). |
| P-15 | Iconos pequeños | ✅ **Aprobada** la variante simplificada para 16/32 px. |

### Bloque 5 — Decisiones de la auditoría del documento técnico ✅ RESUELTO

| ID | Pregunta | Respuesta |
|---|---|---|
| P-20 | ¿Qué política se aplica al **portapapeles** al revelar la frase semilla? | ✅ **Copiar permitido** mientras el valor está revelado, con **borrado del portapapeles al ocultar** si aún contiene la semilla; revelado de **30 s** con **ocultado por pérdida de foco** y test E2E que lo comprueba (**DEC-37**). |
| P-21 | ¿**Una sola** ventana de confirmación global o una por origen? | ✅ **Una sola ventana global** de `notification.html`; el resto de solicitudes **esperan en la cola** y la ventana muestra el **contador de pendientes** (**DEC-38**). |
| P-22 | ¿`wallet_addEthereumChain` **activa** la red nueva? | ✅ **No**: el alta **solo añade** la red; usar la red nueva exige su propio `wallet_switchEthereumChain` con aprobación (**DEC-39**). |

### Pendiente administrativo (no bloqueante)

| ID | Acción | Responsable |
|---|---|---|
| P-11 | Crear los repositorios `chrome-wallet` en **GitHub** y **GitLab.com** (organización `anlucorporations`, ramas `main` y `chrome-wallet-DSH`) y avisar para ejecutar `/push`. | Usuario |
| P-12 | Definir la estrategia de publicación frente a la historia divergente del remoto `codecrypto` (su `main`/`chrome-wallet-DSH` apuntan al commit `632d890`, sin ancestro común con la nueva historia). | Usuario (al llegar `/push`) |

---

## 6. Riesgos principales

| Riesgo | Prob. | Impacto | Mitigación |
|---|---|---|---|
| Mnemonic en claro en `chrome.storage.local` (modo sin contraseña) | Alta | Alto | **Riesgo aceptado (P-03)**; documentado como modo desarrollo. |
| Service Worker dormido pierde la cola de aprobaciones | Alta | Alto | **DEC-24**: puerto de larga vida + `chrome.alarms` + cola persistida `Record<approvalId, PendingRequest>` con reconciliación al arrancar y SW dueño único del plazo (RNF-08); al expirar, cierra la ventana, marca `expired` y responde `4001`. |
| Firma ciega: el usuario aprueba sin decodificar el calldata | Media | Alto | **DEC-22/DEC-23**: `eth_sign` fuera del catálogo (`4200`), decodificación del calldata, `verifyingContract`/`name` visibles y avisos de riesgo antes de firmar. |
| Bloqueo o exposición del RPC local | Media | Medio | `host_permissions` + **allowlist de CORS** en Anvil (RE-04, H-41); nunca `--http.corsdomain "*"` ni escucha fuera de `127.0.0.1`. |
| Fuga de clave privada hacia la página | Baja | Crítico | Nunca se envían claves ni el mnemonic a la página ni por `postMessage` (RNF-09/RNF-10, **DEC-40/DEC-41**); `chrome.storage.local.setAccessLevel({ accessLevel: 'TRUSTED_CONTEXTS' })` (H-32). |
| **Semilla o clave privada retenida fuera de la UI tras el revelado** (portapapeles, memoria de la UI, capturas de pantalla) | Media | Alto | **DEC-37**: revelado de **30 s** con **ocultado por pérdida de foco**, **borrado del portapapeles** al ocultar si aún contiene la semilla, descarte del valor de la memoria de la UI, aviso in-product de que el revelado es vulnerable a capturas y test E2E que comprueba que el portapapeles no la conserva (P-20, ADT-09). |
| Numeración inconsistente del enunciado | Alta | Bajo | Renumeración `RF-01..RF-50` (DEC-01) y conteos sincronizados en la tabla §2 (H-03). |
| **Alcance y plazo:** el MVP (40 RF Must) no cabe en el presupuesto de la asignatura | **Alta** | **Alto** | **MVP por hitos (DEC-26/DEC-27)**: los 10 RF Should pasan a un ciclo posterior; estimación por hitos en `plan_desarrollo.md` (sustituye al «~40 h»); tabla «MVP vs ciclo posterior» (§9). |
| Combinar Playwright + Forge + Vitest en solitario | Media | Medio | Los tests se construyen en cada ciclo, no al final; el contrato EIP-712 es mínimo. |
| Historia local y remota sin ancestro común en `codecrypto` | Alta | Medio | Definir la estrategia de publicación (solo historia git) antes del primer `/push` (**P-12**), sin reutilizar código (DEC-09). |

---

## 7. Próximos pasos

1. ~~Confirmar el cierre de la Fase 1 y pasar a la Fase 2~~ ✅ **hecho**.
2. ~~Ejecutar `/auditar`~~ ✅ **hecho**: `INFORME_OPTIMIZACION_V1.md` (42 hallazgos, 27 descartados).
3. ~~Cerrar la remediación documental de la Fase 2 (`INFORME_OPTIMIZACION_V1.md`, 42 hallazgos)~~ ✅ **hecho**.
4. ~~Ejecutar `/casos_uso` (Gherkin/EARS + matriz `E-xx → RF-xx → CU → test`)~~ ✅ **hecho**: **36 CU** en `casos_uso/casos_uso.md`.
5. ~~Auditar los casos de uso, resolver `P-17`/`P-18`/`P-19` y aplicar la remediación~~ ✅ **hecho**: `casos_uso/AUDITORIA_CASOS_USO_V1.md` (**30 hallazgos**) con remediación aplicada (**DEC-27..DEC-36**, `requerimientos.md` v1.5).
6. ~~Ejecutar `/graficos` (Mermaid/SVG de los 36 CU auditados)~~ ✅ **hecho**: `casos_uso/diagramas.md` (**12 diagramas**).
7. ~~Ejecutar `/documento_tecnico` (arquitectura y especificación técnica)~~ ✅ **hecho**: `documento_tecnico.md` (66 módulos, 20 ADR, 15 diagramas).
8. ~~Ejecutar `/auditar_documento` (auditoría del documento técnico)~~ ✅ **hecho**: `AUDITORIA_DOCUMENTO_TECNICO_V1.md` (**33 hallazgos ADT-01..ADT-33**), con **P-20/P-21/P-22** resueltas y remediación aplicada (`requerimientos.md` v1.6, `documento_tecnico.md` y **DEC-37..DEC-44**).
9. ~~Responder el Bloque 4 de identidad visual~~ ✅ **hecho** (P-13/P-14/P-15).
10. ~~**Cerrar la Fase 2:** ejecutar el **veredicto de reevaluación** que confirme el cierre de los 42 hallazgos, los 30 ACU y los 33 ADT~~ ✅ **hecho**: `VEREDICTO_FASE2_V1.md` v1.0 emitido (❌ Fase 2 NO cerrada en ese momento: 0 abiertos · 27 cerrados · 8 parciales · **12 residuales `R-01..R-12`** y **18 contradicciones**) y **hoy en v1.3**: ✅ **corpus consistente y apto para autorizar la Fase 3**, con **0 residuales abiertos**, **18/18 contradicciones resueltas** y **`VR-01..VR-10` cerrados**.
11. ~~**Ejecutar el plan de cierre de los residuales (`VEREDICTO_FASE2_V1.md` §5)**: bloques 1 (consistencia de valores), 2 (verificabilidad de criterios) y 3 (memoria y metadatos), y repetir la verificación por `grep`~~ ✅ **hecho**: los **12 residuales `R-01..R-12` cerrados** (el último, **`R-09`**, con **DEC-45/DEC-46**) y **18/18 contradicciones resueltas**; `VEREDICTO_FASE2_V1.md` **v1.2 → v1.3**.
12. ~~**Última pasada de consistencia** solicitada por el usuario (barrido final del corpus con `grep`: citas, versiones, conteos y `\uFFFD`) antes de dar por cerrada la Fase 2~~ ✅ **hecha** (`VR-01..VR-10` cerrados; `VEREDICTO_FASE2_V1.md` v1.3).
13. Crear los repositorios de GitHub y GitLab.com (P-11).
14. ~~Pasar a la **Fase 3 – Desarrollo** con `/plan_desarrollo` (estimación por hitos y MVP de **40 Must**, DEC-26)~~ ✅ **hecho**: `plan_desarrollo.md` v1.0 con **H1..H6** + **C1..C10** y el MVP de **40 RF Must**.
15. ~~Ejecutar **H1 — Andamiaje, build MV3 y arnés de pruebas**~~ ✅ **hecho (2026-09-11)**: **H1 ✅ COMPLETADO** — 6 entradas + `dist/manifest.json` con un solo `npm run build`, ID de extensión **`oiahebaliobknoeeonhgaacapjcpgblo`**, batería en verde (tsc `-b` exit 0 strict · Vitest **47** · Playwright **4** · Forge **7** con `--root contracts` · `lint:prohibited` exit 0 · `check:mermaid` **27/27**) y evidencia en `RepoTecnico/evidencia/H1/` (`ACTA_H1.md`). **5 desviaciones aceptadas** registradas en **DEC-47..DEC-51** y en `plan_desarrollo.md` §3.1.10.
16. ~~**H2 «Cartera, cuentas y recuperación»** (`plan_desarrollo.md` §3.2)~~ ✅ **hecho (2026-09-11)**: **H2 ✅ COMPLETADO** — 11 RF Must cerrados (RF-01..RF-05, RF-07, RF-09..RF-11, RF-33, RF-50) con `CA-RF-01..05/07/09/10/11/33/50` demostrados **en el navegador**: **Vitest 290 passed / 18 ficheros** (cobertura `crypto/` **95,13 %** y `shared/validation/` **97,27 %**), **Playwright 23 passed / 0 failed / 0 skipped**, `tsc -b`/`build`/`lint:prohibited` exit 0 y `grep` de `chrome.storage`/`ethers` en `src/popup` → **0**; evidencia en `RepoTecnico/evidencia/H2/` (`ACTA_H2.md`) y **5 correcciones aceptadas** en **DEC-52..DEC-56** (`plan_desarrollo.md` §3.2.10).
17. ~~**H3 «Provider, conexión y lectura»** (`plan_desarrollo.md` §3.3)~~ ✅ **hecho (2026-09-11) — H3 ✅ COMPLETADO**: RF-13..RF-18, RF-25..RF-27, RF-36 y RF-45 con M5, M14, M18, M26/M27, M35..M38, M47..M49 y M56; inyección real del provider en `test.html`, `eth_requestAccounts`/`eth_accounts` con sesión por origen (TTL 24 h renovables), lecturas (`eth_chainId`, `eth_blockNumber`, `eth_getBalance`, `eth_estimateGas`, `eth_gasPrice`, `eth_feeHistory`), política de **4 llamadas RPC** con backoff (1 s/2 s/4 s y timeout de 5 s), eventos `accountsChanged`/`chainChanged`, `wallet_revokePermissions` y la guarda de `sender`/`origin` (D-J/ADT-07). **Batería real:** `npx tsc -b`, `npm run build` y `npm run lint:prohibited` **exit 0**; **Vitest 403 passed / 28 ficheros**; **Playwright 37 passed / 0 failed** con Anvil 31337 y la dApp autoservida por el `webServer`; `rg` de `chrome.storage`/`ethers` en `src/popup`/`src/connect` → **0**; `cast chain-id` = **31337**, `cast block-number` **≥ 1** y `cast balance 0xf39F…2266` = **10 000 ETH**. Evidencia en `RepoTecnico/evidencia/H3/` (`ACTA_H3.md`) y **5 puntos de cierre** en **DEC-57..DEC-61** (`plan_desarrollo.md` §3.3.10).
18. ~~**H4 «Firma, aprobación y transacciones»** (`plan_desarrollo.md` §3.4)~~ ✅ **hecho (2026-09-12) — H4 ✅ COMPLETADO**: los **9 RF Must + la guarda de RF-11** cierran con la ruta de producción de los **6 métodos aprobables** (preview M19 → cola M14 → plazo M15 `chrome.alarms` → ventana única M18 → decisión M14.b → firma M11 → difusión M7 → entrega M17) y la prueba real de extremo a extremo verde (hash `0x21f97614e3117d6e9083ba46b8b2be20c428008e5f6801adcc19fc00741833a6`, recibo `status 0x1`, cola vacía y marca en vuelo liberada). **Batería real:** `tsc -b`, `build` y `lint:prohibited` **exit 0**; **Vitest 587 passed / 41 ficheros**; **Playwright 44 passed / 0 failed / 0 skipped** (los 37 de H1–H3 siguen verdes); `forge:test` **10/10**. Evidencia en `RepoTecnico/evidencia/H4/` (`ACTA_H4.md`) y **7 puntos de cierre** en **DEC-62..DEC-68** (`plan_desarrollo.md` §3.4.10), con **D-H4-E9**/**D-H4-E10** **cerrados** (DEC-66/DEC-67) y los dos defectos del re-render (**D-H4-E11**/**D-H4-E12**, DEC-68).
19. ~~**H5 «Redes y observabilidad»** (`plan_desarrollo.md` §3.5)~~ ✅ **hecho (2026-09-12) — H5 ✅ COMPLETADO**: los **8 RF Must + la observabilidad** cierran con `wallet_switchEthereumChain` (red ya activa ⇒ `null` sin ventana; a otra red ⇒ **1** `pending`, aprobación y `chainChanged` a todas las pestañas), `wallet_addEthereumChain` (validación previa, coherencia contra el nodo, aprobación, **permiso de host en runtime** y persistencia con `isDefault: false` **sin activar** la red), el panel de actividad (24 eventos, errores en rojo con su `code`, hash/firma y **exportación JSON sin red**) y `test.html` con los **7 flujos**. **Batería real:** `tsc -b`, `build` y `lint:prohibited` **exit 0**; **Vitest 636 passed / 47 ficheros**; **Playwright 58 passed / 0 failed / 0 skipped** (los 44 de H1–H4 siguen verdes); `forge:test` **10/10**; **Anvil 31337** y **secundario 31338** (arrancado por el `globalSetup` si falta). Evidencia en `RepoTecnico/evidencia/H5/` (`ACTA_H5.md`) y **9 puntos de cierre** en **DEC-69..DEC-78** (`plan_desarrollo.md` §3.5.10), con **D-H5-N reportado** (alta desde una dApp: necesita el gesto de la ventana de confirmación, `src/notification/**`, fuera del terreno de H5) y **D-H5-O declarado NO VERIFICADO** (alta de red nueva desde el popup: el aviso nativo de Chrome no es accionable sin interfaz).
20. ~~**H6 «Identidad visual, accesibilidad y entrega»** (`plan_desarrollo.md` §3.6)~~ ✅ **hecho (2026-09-12) — H6 ✅ COMPLETADO y Fase 3 cerrada**: identidad aplicada en las **cuatro** superficies (`23-marca`), accesibilidad AA con teclado, foco, zoom 200 % y **`axe-core` 0 violaciones** (`24-accesibilidad`), contraste recalculado (`contrast.spec.ts`) y oro solo decorativo (`goldUsage.spec.ts`), los **tres avisos** de RNF-23 (`26-avisos`: primer arranque, «Acerca de» y antes de la primera firma), formato y textos (`17-i18n`, `format.spec.ts`), **documentación de la rúbrica** (`README.md` en 4 pasos, `INSTRUCCIONES.md` con los 15 flujos, `LICENSE`, `NOTICE`), **JSDoc de contrato por módulo** con gate mecánico (`src/docs.spec.ts`: **145 ficheros / 148 comprobaciones**, **35 cabeceras completadas**), **cobertura de RNF-17 dentro de umbral con la puerta declarada en configuración** (global **86,13 %**, `crypto/` **86,38 %**, `approvals/` **95,21 %** —desde **64,62 %**, con **245 pruebas nuevas** en `test/oracle/**`— y `shared/validation/` **91,09 %**), **build limpio en Windows** (`npm ci` **33 s** + `build` **22 s**, ambos exit 0), **Vitest 1076 passed / 63 ficheros** y `forge:test` **10/10**. **Batería real, desviaciones y salvedades** en `plan_desarrollo.md` **§3.6.9/§3.6.10** (**D-H6-A**..**D-H6-I**), `ACTA_H6.md` y `RepoTecnico/evidencia/H6/`. **Queda declarado:** **RNF-15 verificado solo en Windows** (sin WSL2: `D-H6-D`) y **ensayo del Perfil B no verificado por sujeto externo** (`D-H6-E`); **D-H6-B quedó cerrada** en el mismo turno (los specs de contraste leen la paleta de `tokens.css` y `lint:prohibited` da **0 hallazgos**).
21. ~~**Fase 4 (Pruebas):** revalidación completa en ventana limpia, ensayo del Perfil B, build en Linux/CI y cierre del informe~~ ✅ **hecho (2026-09-13) — Fase 4 ✅ COMPLETADA**: los **tres frentes** en verde (**Vitest 1185 passed / 71 ficheros**, **Playwright 84 passed / 0 failed / 0 flaky**, **Forge 30 passed**), cobertura de ramas de RNF-17 cumplida (global **86,11 %**, `crypto/` **86,98 %**, `approvals/` **95,60 %**, `shared/validation/` **93,13 %**) y `tsc -b`/`build`/`lint:prohibited`/`coverage` en **exit 0**; **15 defectos corregidos** (+4 de concurrencia y retención) y **4 flecos cerrados** sin decisión de producto; informe consolidado en **`RepoTecnico/INFORME_PRUEBAS_FASE4.md`** y batería real en **§3.7**. **Quedan sin cumplir, declaradas**, **RNF-15 en Linux/CI** (`D-H6-D`) y el **ensayo del Perfil B con sujeto externo** (`D-H6-E`), que pasan a la Fase 5.
22. **Siguiente paso — Fase 5 (Manuales):** manuales técnicos y literales con su árbol y su **PDF**, **sección de Ayuda** en la plataforma, **ZIP de entrega `apellido_nombre_wallet.zip`** (`requerimientos.md` §4.2), **`video_demo.mp4`** opcional y **guía de *troubleshooting*** (`requerimientos.md` §4.6) que recoja, al menos, la diferencia entre `4900` y el **rechazo del nodo** (§4.3.4 del diccionario) y la prohibición de **solapar dos suites pesadas**. **Los 10 RF Should (C1..C10) siguen fuera del MVP** con su estado real medido en `INFORME_PRUEBAS_FASE4.md` §7.5 y su orden de recorte ya decidido (**C3 → C4 → C5**).
23. **Riesgo vigente que no bloquea:** el **plazo de conexión inyectado** por el arnés (`VITE_CONNECT_TIMEOUT_MS=2000`) sigue siendo la causa del síntoma intermitente descrito en **DEC-64** (`D-H4-E2`): si reaparece, la palanca es el plazo inyectado, **no** el producto. Del mismo modo, **nunca** deben solaparse dos suites pesadas: **Vitest a la vez que `test:e2e`** (**DEC-76**/`D-H5-J`) y **dos ejecuciones de Playwright a la vez** (**DEC-87**/`D-H6-I`, medido en H6: comparten `dist/`, `test-results/` y la carpeta de evidencia). En la Fase 4 se midió **un** fallo intermitente de este tipo (`09-conectar`, `locator.click` agotando 15 s en la primera pasada de cierre con el árbol bajo carga); la prueba **pasa aislada** (3/3) y en la repetición limpia (84/84), y el fallo queda archivado como evidencia en vez de ocultarse.
24. Crear los repositorios de GitHub y GitLab.com (P-11) y resolver **P-12** (estrategia de publicación) antes del primer `/push`; `/push` sigue **prohibido** sin orden explícita (RE-03/DEC-10).

---

## 8. Criterios de aceptación por fase

### 8.1 Fase 1 — Concepto ✅ CUMPLIDOS

- [x] Extracción de RF / RNF / RT / RE del enunciado fuente (`requerimientos.md` **v1.9**: 50 RF (**40 Must / 10 Should**) / 25 RNF / 13 RT / 4 RE, desviaciones **D-01..D-13**).
- [x] `requerimientos.md`, `diccionario_datos.md` y `entornos_globales.md` creados y consolidados.
- [x] `estado_proyecto.md` con el resumen de la fase.
- [x] Bloque 1, Bloque 1-bis, Bloque 2 y Bloque 3 de la entrevista respondidos (P-01..P-10).
- [x] Repositorio local inicializado con `main` y `chrome-wallet-DSH` y los 3 remotos configurados.
- [x] Decisión de alcance cerrada: red única Anvil, sin GCP, reconstrucción desde cero (DEC-09).
- [ ] Repositorios de GitHub y GitLab.com creados por el usuario (P-11, no bloquea la Fase 2).

### 8.2 Fase 2 — Auditoría ✅ **CERRADA** (veredicto v1.3: 0 residuales abiertos, 0 contradicciones y 0 defectos de consistencia; corpus consistente y apto para autorizar la Fase 3)

- [x] Auditoría ejecutada en 3 fases (7 revisores + 7 verificadores adversariales + síntesis): **42 hallazgos** (2 CRITICA · 14 ALTA · 21 MEDIA · 5 BAJA) y **27 descartados**, en `INFORME_OPTIMIZACION_V1.md`.
- [x] Quick wins documentales aplicados en `entornos_globales.md` v1.6, `identidad_visual.md` v1.2 y esta memoria: H-03, H-04, H-05, H-07 (dueño del plazo), H-15, H-20, H-24, H-29, H-33, H-34, H-36, H-41 y H-02 (permisos).
- [x] Decisiones de remediación del usuario registradas: **DEC-21..DEC-26** (auditoría de Fase 1), **DEC-27..DEC-36** (auditoría de los casos de uso), **DEC-37..DEC-44** (auditoría del documento técnico: **P-20/P-21/P-22** y **D-J..D-Q**) y **DEC-45/DEC-46** (cierre de `R-09`).
- [x] **Criterios de aceptación por requisito** (H-01): los **50 RF** y los 13 RT con criterio y evidencia en `requerimientos.md` **v1.9** (§1, §3 y Anexo A §9), con **`CA-RF-50`** (30 s, pérdida de foco, portapapeles y guarda de sesión de dApp activa), **`CA-RF-23`** (alta sin activación), **`CA-RF-35`** (ventana global única), **`CA-RF-39`** (permiso opcional), **`CA-RF-05`/`CA-RF-06`/`CA-RF-11`** (guardas de estado de `R-09`) y **`CA-RT-04`**/**`CA-RT-13`** (UUID literal y `key` fija).
- [x] **Casos de uso generados** (**36 CU**, `casos_uso/casos_uso.md` v1.5) y **auditados** (`casos_uso/AUDITORIA_CASOS_USO_V1.md` v1.0, **30 hallazgos**) con su remediación aplicada.
- [x] **Matriz de trazabilidad** `E-xx → RF-xx → CU → test` con los **40 Must** cubiertos (H-01/H-05/H-06/H-14): declarada en `requerimientos.md` §1/§1.0/§9.4 y materializada en la matriz de `casos_uso/casos_uso.md` v1.5 (40/40 Must con CU y evidencia; §2, §5 y §7 idénticas entre sí).
- [x] **Gráficos de los casos de uso** generados: `casos_uso/diagramas.md` **v1.2** con **12 diagramas** (9 `sequenceDiagram`, 2 `flowchart` y 1 `stateDiagram-v2`).
- [x] **`documento_tecnico.md` v1.4** redactado (66 módulos, 20 ADR, 15 diagramas) y **auditado** con `/auditar_documento`: `AUDITORIA_DOCUMENTO_TECNICO_V1.md` **v1.0** con **33 hallazgos** (ADT-01..ADT-33), decisiones **D-H..D-U** y **P-20/P-21/P-22** resueltas.
- [x] **Remediación de la auditoría del documento técnico aplicada**: hallazgos **ADT-04, ADT-06, ADT-09, ADT-12, ADT-14, ADT-19, ADT-20, ADT-22, ADT-25 y ADT-30** cerrados en `requerimientos.md` **v1.7** (conteos verificados y **sin cambios en v1.9**: **50 RF = 40 Must + 10 Should · 25 RNF · 13 RT · 4 RE**).
- [x] **Veredicto de reevaluación cerrado:** el revisor independiente confirmó **0 hallazgos abiertos**, **27 cerrados** y **8 parciales**; ejecutado el plan de cierre, quedan **0 residuales abiertos** (los **12 `R-01..R-12` cerrados**, `R-09` con **DEC-45/DEC-46**) y **18/18 contradicciones resueltas**: `VEREDICTO_FASE2_V1.md` **v1.2 → v1.3**.
- [x] **Última pasada de consistencia** del corpus ✅ **hecha**: cerró los **10 defectos residuales `VR-01..VR-10`** (permisos, catálogo de eventos, constantes, tabla de errores, modelo de ventana retirado, afirmación no falsable, clave canónica, formas de evidencia, metadatos de diagramas y mojibake) y `VEREDICTO_FASE2_V1.md` **v1.3** (§7) declara el **corpus consistente y apto para autorizar la Fase 3**, con **0 `U+FFFD`** y **0 mojibake**; la **Fase 2 queda cerrada**.

### 8.3 Fase 3 — Desarrollo ✅ **COMPLETADA** (H1 ✅, H2 ✅ y H3 ✅ el 2026-09-11; **H4, H5 y H6 ✅ el 2026-09-12**) — **13/13 criterios de cierre de `plan_desarrollo.md` §8** verificados con dos salvedades declaradas (`D-H6-D` y `D-H6-E`)

**H1 — Andamiaje, build MV3 y arnés de pruebas: ✅ COMPLETADO (2026-09-11).** Definición de terminado de `plan_desarrollo.md` §3.1.9, verificada con la batería real (§3.1 de esta memoria):

- [x] **Código:** los módulos de H1 (`manifest.ts`, `background.ts` y `background/**`, `inject/**`, `content-script.ts`, `popup/`, `connect/`, `notification/`, `shared/`, `styles/`) existen con su cabecera `M-xx`; `npx tsc -b` **exit 0** con `strict: true` (RNF-13/`CA-RT-08`).
- [x] **Pruebas:** Vitest **47 passed / 4 ficheros**, Playwright **4 passed** (`01-onboarding.spec.ts`) y Forge **7 passed** con `--root contracts` (`CA-RT-07`). *(La cobertura de ramas de RNF-17 con umbrales se activa en H2..H5, cuando existen los módulos cubribles.)*
- [x] **Evidencia:** `RepoTecnico/evidencia/H1/` con los logs de todos los comandos, `manifest-2026-09-11.json`, `01-onboarding-2026-09-11.png` y `ACTA_H1.md`.
- [x] **Memoria:** esta memoria en **v2.0** (fase, decisiones **DEC-47..DEC-51**, tabla §2, comandos verificados y próximos pasos) y corpus corregido (`plan_desarrollo.md` v1.1, `documento_tecnico.md` v1.5, `entornos_globales.md` v2.0, `identidad_visual.md` v1.5, `requerimientos.md` v1.9).
- [x] **Puerta de comandos:** `npm run build` **exit 0** (6 entradas + manifest), `npx tsc -b` **exit 0**, `npm run test` **47**, `npm run test:e2e` **4**, `npm run lint:prohibited` **exit 0**, `npm run forge:test` **7** y `npm run check:mermaid` **27/27** (`CA-RT-07`).
- [x] **Artefacto cargable:** `dist/` con **6 entradas** + `manifest.json`, ID de extensión **`oiahebaliobknoeeonhgaacapjcpgblo`**, popup **380×600** en estado vacío y **0 errores** de consola y de badge (`CU-36`).
- [x] **Desviaciones aceptadas:** **DEC-47** (`chrome-extension://` prohibido en `matches`/`exclude_matches`), **DEC-48** (3 builds encadenados), **DEC-49** (tamaño explícito del `body`), **DEC-50** (Poppins en tres ficheros + licencias OFL), **DEC-51** (`forge test --root contracts`), documentadas en `plan_desarrollo.md` §3.1.10.
- [ ] **Pendiente por diseño (hitos posteriores), no bloquea H1:** `test.html` detectando el provider **en página real** → **H3** (`E2E: 07-provider.spec.ts`; en H1 se verifica sobre el stub en `windowContract.spec.ts`); build limpio **en Linux/WSL2** (RNF-15) y cobertura con umbrales (RNF-17) → **H6**; los **10 RF Should** → ciclo posterior **C1..C10**.

**H2 — Cartera, cuentas y recuperación: ✅ COMPLETADO (2026-09-11).** Definición de terminado de `plan_desarrollo.md` §3.2.9, verificada con la batería real (§3.2 de esta memoria):

- [x] **Código:** los módulos de H2 existen con su cabecera `M-xx` —M8..M13 (`crypto/`), M28 (`accounts.ts`), M29 (`settings.ts`), M33/M34 (`state/`), M3/M4/M6/M22 (`rpc/` y `security/`), M39..M46 (`popup/`) y M57..M63 (`shared/`)—; `npx tsc -b` **exit 0** con `strict: true`.
- [x] **Pruebas:** **Vitest 290 passed / 18 ficheros** (12 specs nuevos del hito) y **Playwright 23 passed / 0 failed / 0 skipped / 0 flaky** (`01`, `02`, `03`, `05`, `06`, `16`, `25`) contra **Anvil 31337**.
- [x] **Cobertura:** `src/background/crypto/` **95,13 %** de líneas (488/513) y `src/shared/validation/` **97,27 %** (214/220): umbral ≥ 80 % de §3.2.9 **cumplido**.
- [x] **Criterios:** `CA-RF-01`, `CA-RF-02`, `CA-RF-03`, `CA-RF-04`, `CA-RF-05` (con la guarda `-32000` de R-09a), `CA-RF-07`, `CA-RF-09`, `CA-RF-10`, `CA-RF-11` (parte 1 **y** la guarda de DEC-46), `CA-RF-33` y `CA-RF-50` (30 s, pérdida de foco, borrado del portapapeles, nunca `postMessage`, guarda `-32000`) **demostrados en el navegador**; RNF-09, RNF-14, RNF-22 y RNF-23 (primer arranque) verificados también en el popup real.
- [x] **Evidencia:** `RepoTecnico/evidencia/H2/` con los logs de los 7 comandos, `coverage-2026-09-11.json`, `e2e-2026-09-11.json`, `01-onboarding-2026-09-11.{json,png}`, `25-recuperacion-2026-09-11.png`, `reset-2026-09-11.json` y **`ACTA_H2.md`**.
- [x] **Memoria:** esta memoria en **v2.1** (fase, decisiones **DEC-52..DEC-56**, tabla §2, comandos verificados y próximos pasos) y `plan_desarrollo.md` **v1.2** (H2 marcado completado y §3.2.10 nueva).
- [x] **Correcciones aceptadas:** **DEC-52** (revelado por origen, no por `sender.tab`), **DEC-53** (orden de guardas del reset + consulta previa del popup), **DEC-54** (aviso de éxito del reset), **DEC-55** (borrado del portapapeles aplazado sin foco) y **DEC-56** (correcciones del arnés E2E), documentadas en `plan_desarrollo.md` §3.2.10.
- [ ] **Pendiente por diseño (hitos posteriores), no bloquea H2:** las salidas «Resolver ahora / Esperar» del bloqueo del reset llevan a la cola de `notification.html` → **H4**; el saldo por cuenta en la lista de cuentas → **H3**; los **10 RF Should** → ciclo posterior **C1..C10**.

**H3 — Provider, conexión y lectura: ✅ COMPLETADO (2026-09-11).** Definición de terminado de `plan_desarrollo.md` §3.3.9, verificada con la batería real (§3.3 de esta memoria):

- [x] **Código:** los módulos de H3 existen con su cabecera `M-xx` —M35..M37 (`inject/`: provider completo `window.truekeate` + alias `window.codecrypto`, mismo objeto no configurable, con `chainId`/`selectedAddress` cacheados y EIP-6963), M38 (`content-script.ts`, relay de los dos saltos con `targetOrigin` cerrado), M26/M27 (`sessions.ts`/`events.ts`) más `connections.ts`, M5/M3 (`rpc/client.ts` con la política cerrada de 4 llamadas, `rpc/pageMethods.ts`, `rpc/rateLimit.ts` y `rpc/internalMethods.ts`), M23 (`networks/catalog.ts`), M20 (`security/senderGuard.ts`), M44 (`SitesView.tsx`), M47 (`useBalancePolling.ts`) y M48/M49 (`connect/`)—; `npx tsc -b` **exit 0** con `strict: true`.
- [x] **Pruebas:** **Vitest 403 passed / 28 ficheros** y **Playwright 37 passed / 0 failed / 0 skipped / 0 flaky** contra **Anvil 31337** con la dApp autoservida por el `webServer`; los 23 E2E de H1/H2 siguen verdes.
- [x] **Criterios:** `CA-RF-13`, `CA-RF-14`/`CA-RF-45`, **`CA-RF-15` (cerrado tras D-H3-A)**, `CA-RF-16`/`CA-RF-36`, `CA-RF-17`/`CA-RF-25`, `CA-RF-18`, `CA-RF-24` (parte `accountsChanged`), `CA-RF-26`, `CA-RF-27` y RNF-10/RNF-11 **demostrados en el navegador**; los 6 métodos aprobables y `eth_sign` siguen respondiendo `4200` (llegan en **H4/H5**).
- [x] **Evidencia:** `RepoTecnico/evidencia/H3/` con los logs de la batería, el JSON de los flujos `07`, `08`, `09`, `13`, `14` y `28`, el log de `27-rpc-caido`, `cast-2026-09-11.log` y **`ACTA_H3.md`**.
- [x] **Memoria:** esta memoria en **v2.2** (fase, decisiones **DEC-57..DEC-61**, tabla §2, comandos verificados en §3.3 y próximos pasos) y `plan_desarrollo.md` **v1.3** (H3 marcado completado y §3.3.10 nueva).
- [x] **Puntos de cierre:** **D-H3-A** (`accountsChanged` al cambiar de cuenta activa), **D-H3-B** (semántica de la sesión por origen: **opción (a)**, equivalente a MetaMask), **D-H3-C** (`INTERNAL_METHODS` a un módulo hoja) y **D-H3-D** (`webServer` en el arnés: la suite autosirve la dApp), más la nota operativa de Anvil, en `plan_desarrollo.md` §3.3.10.
- [ ] **Pendiente por diseño (hitos posteriores), no bloquea H3:** los **6 métodos aprobables** y `eth_sign` → **H4/H5**; `chainChanged` y el cambio de red → **H5**; el log de actividad en `truekeate_logs` → **H5**; los **10 RF Should** → ciclo posterior **C1..C10**.

**H4 — Firma, aprobación y transacciones: ✅ COMPLETADO (2026-09-12).** Definición de terminado de `plan_desarrollo.md` §3.4.9, **alcanzada**; batería real de cierre en §3.4 de esta memoria:

- [x] **Código:** la ruta de los **6 métodos aprobables** queda **operativa en producción** (`rpc/catalog.ts` + `rpc/router.ts` → `approvals/dispatch.ts`: `buildApprovalPreview` M19 → `enqueueApprovalRequest` M14 con RMW/cardinalidad 8·1·6/64 KiB/ventana de tasa → `armExpiryAlarm` M15 → `showOldestPending` M18 → decisión M14.b → `nonce` recalculado + `getFeeData()` + firma tipo 2 M11 + difusión M7 con **hash al difundir** → `deliverApprovalResolution` M17), con la **correlación del salto 1** (`requestId`) y el **re-render de la ventana única** corregidos; `npx tsc -b` **exit 0** con `strict: true`.
- [x] **Pruebas:** **Vitest 587 passed / 41 ficheros** (los 403 de H1–H3 intactos) y **Playwright 44 passed / 0 failed / 0 skipped** (los 37 de H1–H3 siguen verdes; `18-concurrencia` y `29-sw-suspendido` pasan a verde).
- [x] **Criterios: los 13 CUMPLIDOS** — `CA-RF-08`, `CA-RF-19`, `CA-RF-20`, `CA-RF-21`, `CA-RF-35`, `CA-RF-37`, `CA-RF-41`, `CA-RF-42`, `CA-RF-43`, `CA-RF-11` (parte 2), RNF-08, RNF-25 y `CA-RT-11`; `CA-RF-35`, `CA-RF-41` y RNF-08 pasan de **PARCIALES** a **CUMPLIDOS**.
- [x] **Evidencia:** `RepoTecnico/evidencia/H4/` con los logs de los 6 comandos, `e2e-2026-09-12.{log,json}`, el JSON de los flujos `04`, `10`, `11` (×2), `18` y `29` (con su `.log`), las capturas de las ventanas y la **prueba real de extremo a extremo** (`extremo-a-extremo-2026-09-12.{json,png}`: hash `0x21f97614e3117d6e9083ba46b8b2be20c428008e5f6801adcc19fc00741833a6`, recibo `status 0x1`, cola vacía y marca en vuelo liberada), más **`ACTA_H4.md`**.
- [x] **Memoria:** esta memoria en **v2.4** (fase, decisiones **DEC-62..DEC-68**, tabla §2 con los artefactos de H4, comandos verificados en §3.4 y próximos pasos) y `plan_desarrollo.md` **v1.5** (§3.4.9 con el cierre y §3.4.10 con las desviaciones); `entornos_globales.md` **v2.2** con la corrección del comando de Anvil.
- [x] **Puntos de cierre (7):** **D-H4-E1** (cableado: era el catálogo, no el router), **D-H4-E2** (regresión de H3 verificada en verde), **D-H4-E3** (Anvil `--allow-origin`, no `--http.corsdomain`), **D-H4-E5**/**D-H4-E6** (ya corregidos), **D-H4-E9** (host de Vite) y **D-H4-E10** (transporte del relay + correlación), más los dos defectos medidos del re-render de la ventana única (**D-H4-E11**/**D-H4-E12**), en **DEC-62..DEC-68** y `plan_desarrollo.md` §3.4.10.
- [ ] **Pendiente por diseño (hitos posteriores), no bloquea H4:** el cambio y alta de redes, `chainChanged` a todas las pestañas y los 24 eventos de `truekeate_logs` → **H5**; identidad visual, accesibilidad y build limpio en Linux/WSL2 → **H6**; los **10 RF Should** → ciclo posterior **C1..C10**.

**H5 — Redes y observabilidad: ✅ COMPLETADO (2026-09-12).** Definición de terminado de `plan_desarrollo.md` §3.5.9, **alcanzada**; batería real de cierre en §3.5 de esta memoria:

- [x] **Código:** `truekeate_networks` completo (alta con validación de `chainId`/símbolo/`isTestnet`, M23), `wallet_switchEthereumChain` aprobable (M24: red ya activa ⇒ `null` **sin ventana**; a otra red ⇒ **1** `pending` y `chainChanged` a todas las pestañas) y `wallet_addEthereumChain` (M25: validación previa, coherencia contra el nodo, aprobación, **permiso de host en runtime** y persistencia con `isDefault: false` **sin activar** la red); la observabilidad completa (M30/M31/M32: 24 eventos, retención 500/200, cuota con 1 reintento y descarte visible, exportación JSON) con el **router escribiendo 1 traza por llamada**; `NetworksView` (M43) y `LogsView` (M45) con las pestañas «Redes» y «Actividad» (M39); `test.html` con los **7 flujos**; `npx tsc -b` **exit 0** con `strict: true`.
- [x] **Pruebas:** **Vitest 636 passed / 47 ficheros** (los 587 de H1–H4 intactos; +49 del hito: `networks.spec.ts` 9, más los specs de `logging/**` y los ajustes de `eip1193`/`storageQuota`/`loggerRetry`) y **Playwright 58 passed / 0 failed / 0 skipped** (los 44 de H1–H4 siguen verdes; +14: `12-redes` 5, `15-logs` 3, `21-eip6963` 1 y `22-dapp` 5).
- [x] **Criterios: los 10 CUMPLIDOS** — `CA-RF-22`, `CA-RF-23`, `CA-RF-24`, `CA-RF-28`, `CA-RF-29`, `CA-RF-30`, `CA-RF-31`, `CA-RF-46`, **RNF-16** y **`CA-RT-06`**; además RNF-23 (aviso de red no testnet) y RNF-09/RNF-14 en su parte de H5.
- [x] **Evidencia:** `RepoTecnico/evidencia/H5/` con los logs de los 6 comandos, `e2e-2026-09-12.{log,json}`, `e2e-global-setup-2026-09-12.{log,json}` (los **dos** nodos Anvil), los JSON de `12-redes`, `15-logs`, `15-logs-sw`, `logs-export`, `21-eip6963`, `22-dapp-flujos-1-4`, `22-dapp-flujos-5-7` y `22-dapp-4900`, las sondas de diagnóstico y **`ACTA_H5.md`**.
- [x] **Memoria:** esta memoria en **v2.5** (fase, decisiones **DEC-69..DEC-78**, tabla §2 con los artefactos de H5, comandos verificados en §3.5 y próximos pasos) y `plan_desarrollo.md` **v1.6** (§3.5.9 con el cierre y §3.5.10 con las desviaciones).
- [x] **Puntos de cierre (10):** **D-H5-A** (permiso de host desde el popup: el SW no tiene gesto), **D-H5-B** (ciclo aprobable de red sin exigir sesión), **D-H5-C** (saldo de la vista previa: **reportado**, `approvals/**` es de solo lectura en el terreno de H5), **D-H5-D** (traza por llamada en el router), **D-H5-E** (el descarte por cuota no re-persiste el lote), **D-H5-F**/**D-H5-G** (conteo de errores y modelado de los specs), **D-H5-H**..**D-H5-M** (correcciones del arnés E2E y medición de RNF-08), **D-H5-P** (cerrojo del catálogo: la siembra de arranque ya no puede pisar un alta) y **D-H5-Q** (espera observable en `07-provider`), en **DEC-69..DEC-78** y `plan_desarrollo.md` §3.5.10.
- [ ] **Reportado / no verificado, no bloquea H5:** **D-H5-N** — el alta nacida en una **dApp** no puede aportar el gesto de `permissions.request` (necesita la ventana de confirmación, `src/notification/**`, fuera del terreno de H5); su modo de fallo es el observable **`4001` sin persistir**. **D-H5-O** — el alta de una red **nueva** desde el popup no puede verificarse en `E2E_HEADLESS` (el aviso nativo de Chrome no es accionable), cubierto por `networks.spec.ts`. Identidad visual, accesibilidad y build limpio en Linux/WSL2 → **H6**; los **10 RF Should** → ciclo posterior **C1..C10**.

**H6 — Identidad visual, accesibilidad y entrega: ✅ COMPLETADO (2026-09-12).** Definición de terminado de `plan_desarrollo.md` §3.6.9, **alcanzada**; batería real de cierre en §3.6 de esta memoria y acta `evidencia/H6/ACTA_H6.md`:

- [x] **Código y documentación:** identidad de `tokens.css` en las **cuatro** superficies (popup, `connect.html`, `notification.html` y `test.html`) con contraste recalculado y **oro solo decorativo**; accesibilidad AA (teclado, foco ≥ 3:1, roles ARIA, zoom 200 %, `prefers-reduced-motion`) con **`axe-core` 0 violaciones**; los **tres avisos** de RNF-23 (primer arranque, «Acerca de», antes de la primera firma); formato y textos en español; **`README.md`** (puesta en marcha en **4 pasos** autosuficiente), **`INSTRUCCIONES.md`** (15 flujos y 4 superficies), **`LICENSE`** (MIT, **ANLU corporations**) y **`NOTICE`** (dependencias, Forge y las **3 tipografías OFL-1.1**); `npx tsc -b` **exit 0** con `strict: true`.
- [x] **JSDoc de contrato por módulo:** **`src/docs.spec.ts`** audita mecánicamente los **145 ficheros** `.ts`/`.tsx` de `src/` (**148 comprobaciones**) exigiendo bloque JSDoc con identificador `M1..M66` y referencia `RF/RNF/RT`; se completaron **35 cabeceras** (34 con la línea que faltaba y `src/manifest.ts` con su bloque `//` convertido a JSDoc). **Solo comentarios.**
- [x] **Pruebas:** **Vitest 1076 passed / 63 ficheros** (los 636 de H1–H5 intactos; **+245 de `test/oracle/**`**, **+148 del gate documental** y los del hito) y **Playwright 74 passed / 0 failed / 0 skipped** en ventana limpia (los 58 de H1–H5 siguen verdes; +16 con los **4 specs nuevos** del hito `17-i18n`, `23-marca`, `24-accesibilidad` y `26-avisos`); `forge:test` **10/10**; `check:mermaid` **27/27**; `lint:prohibited` **0 hallazgos**.
- [x] **Cobertura (RNF-17):** **umbrales declarados en configuración** (`coverage.thresholds` de `vite.config.ts`) y **cumplidos**: ramas globales **86,13 %** (3192/3706), `src/background/crypto/` **86,38 %** (279/323), `src/background/approvals/` **95,21 %** (1233/1295) y `src/shared/validation/` **91,09 %** (92/101); `npm run coverage` **exit 0**.
- [x] **Build limpio:** `npm ci` **exit 0** (**33 s**) y `npm run build` **exit 0** (**22 s**) desde cero en Windows, con **6 entradas** + `dist/manifest.json`; `grep` de `http` en `dist/` → **0 hosts de CDN** (34 coincidencias locales, `D-H6-G`).
- [x] **Evidencia:** `RepoTecnico/evidencia/H6/` con los logs de todos los comandos, la auditoría `RF Must → evidencia`, los JSON de los 4 flujos del hito, `instalacion-2026-09-12.md` y **`ACTA_H6.md`**.
- [x] **Memoria:** esta memoria en **v2.6** (fase, decisiones **DEC-79..DEC-87**, tabla §2 con los artefactos de H6, comandos verificados en §3.6 y próximos pasos) y `plan_desarrollo.md` **v1.7** (§3.6.9 con el cierre y §3.6.10 nueva con las desviaciones).
- [x] **Puntos de cierre (9):** **D-H6-A** (la cobertura de `approvals/` sube de **64,62 %** a **95,21 %** con **245 pruebas** en `test/oracle/**`, sin tocar `src/background/**`), **D-H6-B** (**cerrada**: los specs de contraste leen la paleta de `tokens.css` y `lint:prohibited` da **0 hallazgos**), **D-H6-C** (2 citas de evidencia desactualizadas: **corregidas en `plan_desarrollo.md` §6** y pendientes en `requerimientos.md` §9.2), **D-H6-D** (RNF-15 **solo Windows**: no hay WSL2 ni CI), **D-H6-E** (ensayo del Perfil B **no verificado por sujeto externo**), **D-H6-F** («Acerca de» por RNF-23; **RF-48 sigue *Should***), **D-H6-G** (`grep http dist/` = 34 coincidencias locales, **0 de CDN**), **D-H6-H** (35 cabeceras JSDoc completadas) y **D-H6-I** (la primera batería E2E se contaminó por dos suites simultáneas y se repitió en ventana limpia: **68/8 → 74/0**), en **DEC-79..DEC-87** y `plan_desarrollo.md` §3.6.10.
- [ ] **Declarado no verificado (no bloquea el cierre del hito, sí el de la Fase 4):** **D-H6-D** (**RNF-15** pendiente en Linux/CI) y **D-H6-E** (**ensayo del Perfil B** con un evaluador externo). Los **10 RF Should** siguen en el ciclo posterior **C1..C10**.

---

### 8.4 Fase 4 — Pruebas ✅ **COMPLETADA (2026-09-13)** — informe consolidado en `INFORME_PRUEBAS_FASE4.md`

Ejecutada con los **tres frentes** y cerrada en verde:

- [x] **Frente de unitarias+integración:** **Vitest 1185 passed / 71 ficheros · 0 failed** (`npm run test`, exit 0), con **31 pruebas nuevas** respecto de la pasada de auditoría (1154) y **sin** desactivar, saltar ni relajar ninguna.
- [x] **Frente E2E headless:** **Playwright 84 passed / 0 failed / 0 flaky / 0 skipped · 33 specs** (`npm run test:e2e`, exit 0) en ventana limpia, con Anvil 31337 y el secundario 31338.
- [x] **Frente de contrato y correspondencia:** **Forge 30 passed / 0 failed · 2 suites** (`forge test --root contracts`, exit 0), con batería adversaria, fuzz (256 y 512 vueltas) y comprobación de ausencia de estado.
- [x] **Cobertura de RNF-17 cumplida:** ramas global **86,11 %** (≥ 70 %), `crypto/` **86,98 %**, `approvals/` **95,60 %** y `shared/validation/` **93,13 %** (≥ 80 %), con `npm run coverage` exit 0.
- [x] **Puerta de calidad completa con exit 0:** `npx tsc -b`, `npm run build` y `npm run lint:prohibited` además de los siete comandos de prueba.
- [x] **15 defectos corregidos** (12 unitarias, 1 E2E, 1 del arnés y 2 documentales del contrato) **más 4** de concurrencia y retención, cada uno con causa raíz `archivo:línea`, solución y prueba de regresión (§4 del informe).
- [x] **4 flecos cerrados sin decisión de producto:** ventana de tasa **6/60 s** alineada con `plan_desarrollo.md` §3.13 y constantes muertas eliminadas; `validateAmount` con el error tipado `-32602 invalidAmount`; rechazo del nodo (`-32003`) registrado en `diccionario_datos.md` §4.3.4 y **no** confundido con «sin conexión»; **RMW serializado** en `sessions.ts`/`connections.ts` y **purga** de `truekeate_connect_request`.
- [x] **Trazabilidad del MVP completa:** **40/40 RF Must**, **25/25 RNF**, **13/13 RT** y **4/4 RE**, con evidencia por `CA-RF-*` en `RepoTecnico/evidencia/H1..H6/` y `RepoTecnico/evidencia/Fase4/`.
- [x] **Ciclo posterior declarado:** los **10 RF Should** con su estado real medido (varios ya implementados y verificados de hecho: RF-06, RF-40 y el mecanismo de RF-32) y su orden de recorte **C3 → C4 → C5**.
- [ ] **RNF-15 en Linux/CI** — **NO cumplido**: verificado **solo en Windows** (`D-H6-D`; no hay WSL2 ni CI en el equipo).
- [ ] **Ensayo del Perfil B con sujeto externo** — **NO cumplido**: solo el *dry-run* del autor (`D-H6-E`).

**Limitaciones declaradas, con motivo:** los **avisos nativos de Chrome no son verificables en headless** (el alta de red desde el popup queda NO VERIFICADA, `D-H5-O`); la **UI React tiene 0 % de líneas en Vitest** porque su contrato se prueba en el navegador (E2E), de modo que la cobertura de **líneas** (61,04 %) no es la medida útil —el umbral de RNF-17 se evalúa sobre **ramas**—; quedan **ramas defensivas inalcanzables** sin prueba artificial; y se documentan los **conflictos de especificación** hallados (`chainIdMismatch` reutiliza el literal de `chainNotRegistered` y §4.3.3 se creó al detectarlo; `validateAmount`, la ventana de tasa y el rechazo del nodo, los tres **resueltos** en esta pasada).

### 8.5 Fase 5 — Manuales 🔜 **SIGUIENTE PASO**

Habilitada por el cierre de la Fase 4. Parte ya entregada en H6 (`README.md`, `INSTRUCCIONES.md`, `LICENSE`, `NOTICE` y los 3 `LICENSE-*.txt`); queda por producir:

- [ ] **Manuales técnicos y literales** con su árbol (`RepoTecnico/Manuales/**` y `docs/**`) y su **versión PDF** descargable.
- [ ] **Sección de Ayuda** en la plataforma.
- [ ] **ZIP de entrega `apellido_nombre_wallet.zip`** (`requerimientos.md` §4.2) y **`video_demo.mp4`** opcional.
- [ ] **Guía de *troubleshooting* propia del proyecto** (`requerimientos.md` §4.6), que debe recoger al menos: la diferencia entre `4900` y el **rechazo del nodo** (§4.3.4 del diccionario), y la prohibición de **solapar dos suites pesadas** (causa de los dos fallos intermitentes medidos: `D-H5-J` y `D-H6-I`).
- [ ] **Ensayo del Perfil B** con sujeto externo, que valida justamente el material del manual de instalación.
- [ ] **Ciclo posterior (C1..C10)** de los 10 RF Should, **fuera del MVP** y con el orden de recorte ya decidido.

---

## 9. Alcance del MVP vs ciclo posterior (H-34 / DEC-26 / P-17 / P-18)

| Bloque | Alcance | Momento |
|---|---|---|
| **MVP (obligatorio)** | **40 RF Must** (incluye **RF-50**) + los RNF y RT asociados (40 de los 50 RF). **No depende del badge** (P-17) | Fases 3-4 |
| **Ciclo posterior** | **10 RF Should**: RF-06, RF-12, RF-32, RF-34, **RF-38**, **RF-39**, RF-40, RF-44, RF-47 y RF-48 (según H-01; **lista canónica en `requerimientos.md` §4.5 «MVP (40 RF Must) vs ciclo posterior (10 RF Should)»**) | tras el MVP, si el presupuesto lo permite |

> **P-17:** RF-38 (badge) y RF-39 (notificaciones) permanecen en el ciclo posterior y **no se promociona ninguno**; el oráculo del caso central de la cola de aprobaciones (CU-16) es «**2 entradas `pending` en `truekeate_pending_requests` + 1 transacción en vuelo por cuenta**». **P-18:** RF-50 entra en el MVP Must como requisito de recuperación (D-13). **P-21/P-22 (DEC-38/DEC-39):** el MVP usa **una sola ventana global** de `notification.html` con cola y contador (RF-35) y el alta de red **no activa** la red (RF-23). **DEC-44:** el permiso `notifications` ligado a RF-39 es **opcional** y no forma parte del conjunto obligatorio del manifest.
> La estimación «~40 h» queda **retirada**: el cronograma se fija por **hitos** en `plan_desarrollo.md` (Fase 3), con el MVP Must como compromiso mínimo y los Should como alcance ampliable. Si el presupuesto se agota, el recorte ya está decidido por diseño y no se negocia a mitad de la Fase 4.



---

## 11. Cierre del proyecto

> **Fecha:** 2026-09-13 · **Decisión del usuario:** **omitir la creación de los manuales** (Fase 5).

| Fase | Estado final |
|---|---|
| 1 — Concepto | ✅ Completada |
| 2 — Auditoría | ✅ Completada (127 hallazgos gestionados, 0 abiertos) |
| 3 — Desarrollo | ✅ Completada (6 hitos del MVP: 40/40 RF Must) |
| 4 — Pruebas | ✅ Completada (Vitest 1185 · Playwright 84/84 · Forge 30 · ramas 86,11 %) |
| 5 — Manuales | ⏭️ **Omitida por decisión del usuario** (no se generan imágenes, PDF ni sección de Ayuda) |

**Entregable efectivo:** el código de la extensión (`src/`, `dist/` reproducible con `npm ci && npm run build`), la dApp de pruebas (`test.html`), el contrato auxiliar (`contracts/`), las cuatro suites (`npm run test`, `npm run test:e2e`, `forge test --root contracts`, `npm run lint:prohibited`), los artefactos de entrega (`README.md`, `INSTRUCCIONES.md`, `LICENSE`, `NOTICE`) y la documentación completa del proyecto en `RepoTecnico/` (17 documentos de análisis + 42 de manuales ya generados antes de la decisión).

**Pendiente declarado (no cumplido, con motivo):**
- `RNF-15` — **verificado solo en Windows**; el entorno Linux/WSL2 no está disponible en esta máquina.
- Ensayo de instalación de **Perfil B** — no verificado con un sujeto externo.
- Avisos nativos de Chrome (`permissions.request`) — no verificables en modo headless.
- Los **10 RF Should** del ciclo posterior (unidades C1–C10) quedan fuera del MVP.

**Acciones del usuario pendientes:** crear los repositorios `chrome-wallet` en GitHub y GitLab.com (organización `anlucorporations`; los remotos `origin` y `gitlab` están configurados pero los repositorios no existen) y autorizar el `/push` de la rama `chrome-wallet-DSH`.