# Dependencias
node_modules/
.pnpm-store/

# Build de la extensión (se genera con `npm run build`)
dist/
dist-ssr/
*.local

# Entorno (nunca versionar secretos)
.env
.env.local
.env.*.local

# Logs y cachés
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.vite/
coverage/
playwright-report/
playwright/.cache/
blob-report/
test-results/

# Evidencia de pruebas: SÍ se versiona (documento_tecnico.md §7.4.1.f)
# (excepción a `*.log`, que de otro modo excluiría los .log de evidencia)
!RepoTecnico/evidencia/**/*.log
!RepoTecnico/evidencia/**/*.json
!RepoTecnico/evidencia/**/*.png

# Editores
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# SO
.DS_Store
Thumbs.db
desktop.ini

# Foundry (proyecto auxiliar contracts/: artefactos y cachés, nunca versionados)
contracts/cache/
contracts/out/
contracts/broadcast/
cache/
out/
broadcast/
